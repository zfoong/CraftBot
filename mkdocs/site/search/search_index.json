{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"CraftBot","text":"<p>Your personal AI assistant \u2014 lives inside your machine, works for you 24/7. CraftBot autonomously interprets tasks, plans actions, and executes them across the browser, filesystem, connected services, and the shell.</p> <ul> <li> <p> Get started</p> <p>Install CraftBot, run the onboarding wizard, and send your first task in under 5 minutes.</p> <p> Install \u00b7 Quickstart</p> </li> <li> <p> Concepts</p> <p>Learn the mental model: agent loop, triggers, actions, memory, context, skills.</p> <p> Concepts</p> </li> <li> <p> Extend CraftBot</p> <p>Build custom skills, actions, and subclassed agents. Plug in MCP servers.</p> <p> Develop</p> </li> </ul>"},{"location":"#what-craftbot-does","title":"What CraftBot does","text":"<ul> <li> <p> Reasons &amp; plans</p> <p>Breaks down complex tasks into todos, picks the right action at each step, and iterates until done.</p> </li> <li> <p> Remembers</p> <p>RAG-based memory with ChromaDB. Distills events at midnight and recalls them when relevant.</p> </li> <li> <p> Connects</p> <p>13 external services \u2014 Discord, Slack, Telegram, Notion, Google Workspace, LinkedIn, Zoom, WhatsApp, and more.</p> </li> <li> <p> Extensible</p> <p>Skills, custom actions, MCP servers, and subclassable agents. Build your own CraftBot.</p> </li> <li> <p> Proactive</p> <p>Learns your preferences and goals. Proposes tasks, reminds you, schedules follow-ups (with approval).</p> </li> <li> <p> Runs anywhere</p> <p>Browser, TUI, CLI, or desktop GUI mode. Cross-platform (Windows, Linux, macOS). Service-mode background daemon.</p> </li> </ul>"},{"location":"#start-here","title":"Start here","text":"<ul> <li>Getting started  \u2014 install, onboard, first task</li> <li>Task modes  \u2014 simple, complex, special, proactive</li> <li>Interfaces  \u2014 browser, TUI, CLI, GUI</li> <li>Connections  \u2014 connect Discord, Slack, Google, \u2026</li> <li>Configuration  \u2014 config.json, env vars</li> <li>Troubleshooting  \u2014 when things go wrong</li> </ul>"},{"location":"#project-status","title":"Project status","text":"<p>GUI mode is experimental</p> <p>GUI mode is still being hardened \u2014 expect issues when the agent switches to vision-based desktop control. Browser and TUI modes are stable.</p> <ul> <li>License: MIT. Free to use, host, and monetize (credit required for distribution).</li> <li>Community: GitHub \u00b7 Discord</li> <li>Maintainers: CraftOS, @zfoong, @ahmad-ajmal</li> </ul>"},{"location":"commands/","title":"Commands","text":"<p>Commands are slash-prefixed inputs (<code>/help</code>, <code>/cred status</code>, <code>/skill list</code>) that the UI intercepts before they reach the agent. They're how you configure connections, inspect state, and control the interface without writing a task.</p> <ul> <li> <p> Built-in commands</p> <p>Reference for every <code>/command</code> bundled with CraftBot.</p> </li> <li> <p> CLI-anything</p> <p>Have the agent run any shell command via the <code>run_shell</code> action.</p> </li> </ul>"},{"location":"commands/#how-commands-work","title":"How commands work","text":"<p>Commands are registered in the UI layer at startup. When you type <code>/foo ...</code>, the UI routes it to the matching command handler instead of the agent. Unrecognized <code>/</code> prefixes fall through to the agent as a regular message.</p> <p>See UI layer for how commands are dispatched and Custom command for how to add your own.</p>"},{"location":"commands/#related","title":"Related","text":"<ul> <li>Interfaces \u2014 where you type commands</li> <li>Connections \u2014 uses commands like <code>/google login</code> extensively</li> </ul>"},{"location":"commands/builtin/","title":"Built-in commands","text":"<p>Every slash command shipped with CraftBot. Type them in any interface \u2014 the UI layer intercepts them before they hit the agent.</p>"},{"location":"commands/builtin/#quick-start","title":"Quick start","text":"<pre><code>/help                  # list every command\n/help cred             # detailed help for one command\n</code></pre>"},{"location":"commands/builtin/#command-catalogue","title":"Command catalogue","text":"Command What it does <code>/help [name]</code> Show command help. No arg = list all. <code>/menu</code> Open the settings menu (models, integrations, skills, MCP). <code>/cred status</code> Show all active credentials. <code>/cred list</code> Same as <code>status</code>, terse. <code>/cred integrations</code> Show all integrations grouped by platform. <code>/cred &lt;provider&gt; connect</code> Start connect flow for one provider (Google, Slack, \u2026). <code>/integrations</code> Open the integrations panel. <code>/provider [name]</code> Switch LLM provider (<code>anthropic</code>, <code>openai</code>, <code>google</code>, <code>byteplus</code>, <code>remote</code>). <code>/skill list</code> List all skills and their enabled state. <code>/skill enable &lt;name&gt;</code> Enable a skill. <code>/skill disable &lt;name&gt;</code> Disable a skill. <code>/skill_invoke &lt;name&gt;</code> Invoke a skill directly. <code>/mcp list</code> List configured MCP servers. <code>/mcp add</code> Add an MCP server (interactive). <code>/mcp remove &lt;name&gt;</code> Remove an MCP server. <code>/reset</code> Reset the current session (clear task + events). <code>/clear</code> Clear the UI view (does not affect state). <code>/update</code> Check for CraftBot updates. <code>/exit</code> Shut down the agent cleanly. <code>/agent_command &lt;cmd&gt;</code> Send an agent-internal command (diagnostic)."},{"location":"commands/builtin/#per-connection-commands","title":"Per-connection commands","text":"<p>Each external integration ships its own set. Examples:</p> <pre><code>/google login\n/slack invite\n/telegram login-bot &lt;token&gt;\n/discord logout\n/notion status\n</code></pre> <p>See the relevant connection page for the full list per integration.</p>"},{"location":"commands/builtin/#dispatch-model","title":"Dispatch model","text":"<p>Command routing happens in three steps:</p> <ol> <li>UI intercepts input starting with <code>/</code>.</li> <li><code>CommandRegistry.resolve(name)</code> returns the handler.</li> <li>Handler runs with parsed args; output displays in the UI.</li> </ol> <p>Unrecognized <code>/</code> prefixes fall through to the agent as a regular message.</p>"},{"location":"commands/builtin/#registering-your-own","title":"Registering your own","text":"<pre><code>from app.ui_layer.commands.registry import register_command\n\n@register_command(name=\"greet\", description=\"Say hello\")\nasync def greet(args: list[str]) -&gt; str:\n    return f\"Hello {args[0] if args else 'world'}\"\n</code></pre> <p>Appears as <code>/greet</code> on next launch. See Custom command for full walkthrough (shares the patterns of custom actions).</p>"},{"location":"commands/builtin/#related","title":"Related","text":"<ul> <li>Commands overview</li> <li>CLI-anything \u2014 the <code>run_shell</code> escape hatch</li> <li>UI layer \u2014 where dispatch happens</li> <li>Credentials \u2014 the <code>/cred</code> family</li> </ul>"},{"location":"commands/cli-anything/","title":"CLI-anything","text":"<p>CraftBot ships with a single action called <code>run_shell</code> that gives the agent access to the host shell. With it, the agent can do anything the command line can do \u2014 install software, run build steps, query your database, poke APIs with <code>curl</code>, start a dev server, whatever.</p>"},{"location":"commands/cli-anything/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>One action. One schema. Universal power.</li> <li>The agent calls it like any other action: name + inputs (command, shell, timeout, cwd, env, background).</li> <li>It runs on the host OS's native shell: <code>cmd</code> on Windows, <code>zsh</code> on macOS, <code>bash</code> on Linux.</li> <li>Long-running processes (dev servers, watchers) must set <code>background: true</code>, or the task blocks.</li> </ul> <p><code>run_shell</code> is in the <code>core</code> action set, so it's available to every task by default. Disable by removing it from <code>core</code> if you want a sandboxed agent.</p>"},{"location":"commands/cli-anything/#inspect-it-now","title":"Inspect it now","text":"<p>Ask the agent to run something trivial:</p> <p>run <code>echo hello</code> in my shell</p> <pre><code>[ACTION] run_shell -&gt; ok (stdout=\"hello\\n\", return_code=0)\n</code></pre>"},{"location":"commands/cli-anything/#input-schema","title":"Input schema","text":"<pre><code>{\n  \"command\":    \"git status\",\n  \"shell\":      \"auto\",                 // or \"bash\" / \"zsh\" / \"cmd\" / explicit\n  \"timeout\":    60,                     // seconds; exceed = terminate\n  \"cwd\":        \"/home/user/my-repo\",\n  \"env\":        { \"DEBUG\": \"1\" },\n  \"background\": false\n}\n</code></pre>"},{"location":"commands/cli-anything/#output-schema","title":"Output schema","text":"<pre><code>{\n  \"status\":      \"success\",             // or \"timeout\" / \"error\"\n  \"stdout\":      \"...\",\n  \"stderr\":      \"\",\n  \"return_code\": 0,\n  \"message\":     \"Timed out after 60s.\",  // only on timeout/error\n  \"pid\":         12345                     // only when background=true\n}\n</code></pre>"},{"location":"commands/cli-anything/#platform-dispatch","title":"Platform dispatch","text":"<p>Three implementations live under the hood:</p> <ul> <li><code>shell_exec</code> (shared) \u2014 default</li> <li><code>shell_exec_windows</code> \u2014 Windows-specific (uses <code>cmd</code>)</li> <li><code>shell_exec_darwin</code> \u2014 macOS-specific (uses <code>zsh</code>)</li> </ul> <p>The action registry picks the right implementation based on <code>platform.system()</code>. The agent calls the logical name <code>run_shell</code> and gets the correct behavior.</p>"},{"location":"commands/cli-anything/#background-processes","title":"Background processes","text":"<p>For long-running things:</p> <pre><code>{\n  \"command\": \"npm run dev\",\n  \"background\": true,\n  \"cwd\": \"/home/user/my-app\"\n}\n</code></pre> <p>Returns <code>pid</code> immediately. The agent can then <code>curl localhost:3000</code> or read logs to verify the server is up. To stop the process later, the agent uses <code>kill &lt;pid&gt;</code> or <code>taskkill /PID &lt;pid&gt;</code>.</p>"},{"location":"commands/cli-anything/#timeouts","title":"Timeouts","text":"<p>Default <code>timeout: 60</code> seconds. If exceeded, the process is terminated and <code>status: \"timeout\"</code> is returned. Set a larger timeout for slow operations (Docker builds, large installs) up to a reasonable ceiling.</p>"},{"location":"commands/cli-anything/#security-considerations","title":"Security considerations","text":"<p>Giving an LLM shell access is powerful and risky. CraftBot's defaults:</p> <ul> <li>No sandbox \u2014 <code>run_shell</code> runs with the user's privileges.</li> <li>No confirmation \u2014 the agent can run it without user approval.</li> <li>Logged \u2014 every shell command appears in the event stream and logs.</li> </ul> <p>Review what the agent runs</p> <p>If you're running CraftBot on a production machine, consider:</p> <ul> <li>Removing <code>run_shell</code> from the <code>core</code> set (custom agent).</li> <li>Running the agent in a container.</li> <li>Adding approval tiers via a wrapper action.</li> </ul>"},{"location":"commands/cli-anything/#why-not-just-shell-execution-as-a-feature","title":"Why not just \"shell execution\" as a feature?","text":"<p>Because framing it as an action keeps everything uniform:</p> <ul> <li>Appears in the action router alongside <code>send_gmail</code> and <code>web_search</code>.</li> <li>Emits <code>action_start</code> / <code>action_end</code> events like anything else.</li> <li>Can be removed or restricted with the same mechanism as any other action (action sets, visibility mode).</li> </ul>"},{"location":"commands/cli-anything/#related","title":"Related","text":"<ul> <li>Actions \u2014 the primitive <code>run_shell</code> is an instance of</li> <li>Commands overview \u2014 slash commands (different layer)</li> <li>Built-in commands \u2014 UI-layer commands</li> <li>Custom action \u2014 build your own action</li> </ul>"},{"location":"concepts/","title":"Concepts","text":"<p>The core building blocks of CraftBot. Start here if you want a mental model of how the agent thinks, acts, and remembers \u2014 before touching config.</p> <p>Every page on this section opens with a one-sentence definition and a \"mental model\" beat you can hold in your head, then shows a command you can run right now to see the concept in action.</p> <ul> <li> <p> Agent loop</p> <p>The main cycle: react \u2192 select action \u2192 execute \u2192 observe.</p> </li> <li> <p> Triggers</p> <p>What starts a task \u2014 user messages, schedules, external events.</p> </li> <li> <p> Task sessions</p> <p>The unit of work. Each task has its own state, todos, and event stream.</p> </li> <li> <p> Event stream</p> <p>Pub/sub for task progress. Drives the UI and lets you observe what the agent is doing.</p> </li> <li> <p> Actions</p> <p>The things the agent can do. Library of ~80+ built-in actions; custom actions easy to add.</p> </li> <li> <p> Skill &amp; action selection</p> <p>How the agent picks the right action for each step.</p> </li> <li> <p> Prompts</p> <p>Prompt templates and the registry that lets you override them.</p> </li> <li> <p> Context engine</p> <p>Assembles the full prompt \u2014 system, history, task state, memory \u2014 with KV caching.</p> </li> <li> <p> Memory</p> <p>RAG memory over ChromaDB. Auto-indexed events, semantic recall.</p> </li> <li> <p> Agent file system</p> <p>The agent's persistent markdown knowledge base: AGENT.md, USER.md, MEMORY.md, and more.</p> </li> <li> <p> Logs</p> <p>What gets logged, where, and how to configure verbosity.</p> </li> </ul>"},{"location":"concepts/#further-reading","title":"Further reading","text":"<ul> <li>Task modes \u2014 how the agent loop varies by task type</li> <li>Develop \u2014 build your own actions, skills, and agents</li> <li>Configuration \u2014 where every knob lives</li> </ul>"},{"location":"concepts/action/","title":"Actions","text":"<p>An action is one thing the agent can do \u2014 send a message, read a file, query a database, post a Slack reply, run a shell command. The agent's entire vocabulary lives in the action library: if there's no action for something, the agent can't do it. (Unless you give it the <code>run_shell</code> action \u2014 then it can do anything.)</p>"},{"location":"concepts/action/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>An action is a named function with a JSON schema for its inputs and outputs.</li> <li>The action library is the registry of all built-in actions \u2014 ~80+ of them, from <code>send_message</code> to <code>memory_search</code> to <code>send_gmail</code>.</li> <li>The action router is the LLM-driven picker: given a user query, it selects one or more actions and fills in their inputs.</li> <li>Action sets group actions for task scoping \u2014 a task declares <code>\"action_sets\": [\"file_operations\", \"web_research\"]</code>, and only those actions are available during execution.</li> </ul> <p>An action run produces events on the stream: <code>action_start: &lt;name&gt;</code> \u2192 <code>action_end: &lt;name&gt; -&gt; ok (extras)</code>.</p>"},{"location":"concepts/action/#inspect-it-now","title":"Inspect it now","text":"<p>List all actions:</p> <pre><code>/cred integrations     # shows all integration actions grouped by platform\n</code></pre> <p>Or in Python, via the registry:</p> <pre><code>from agent_core import registry_instance\nfor name in registry_instance._registry:\n    print(name)\n</code></pre>"},{"location":"concepts/action/#anatomy","title":"Anatomy","text":"<p>Every action has:</p> <pre><code>@register_action(\n    name=\"send_gmail\",\n    description=\"Send an email via Gmail.\",\n    action_sets=[\"core\"],                # which sets contain it\n    platforms=[PLATFORM_ALL],            # linux / darwin / windows / all\n    visibility_mode=\"CLI\",               # CLI / GUI / ALL\n    input_schema={\n        \"to\":      {\"type\": \"string\", \"required\": True},\n        \"subject\": {\"type\": \"string\", \"required\": True},\n        \"body\":    {\"type\": \"string\", \"required\": True},\n    },\n    output_schema={\n        \"status\":     {\"type\": \"string\"},\n        \"message_id\": {\"type\": \"string\"},\n    },\n    default=True,                        # included even without explicit set selection\n)\nasync def send_gmail(to: str, subject: str, body: str) -&gt; dict:\n    ...\n</code></pre> <p>The metadata is what the router sees \u2014 it never reads the implementation. The <code>description</code> is the single most important field; it's what the LLM uses to decide when to pick this action.</p>"},{"location":"concepts/action/#the-seven-action-sets","title":"The seven action sets","text":"<p>Default set names \u2014 actions can belong to multiple:</p> Set Contains <code>core</code> <code>send_message</code>, <code>task_start</code>, <code>task_update_todos</code>, <code>task_complete</code>, set management \u2014 always available <code>file_operations</code> <code>read_file</code>, <code>write_file</code>, <code>edit_file</code>, <code>grep_files</code>, <code>stream_read</code>, list/search <code>web_research</code> <code>web_search</code>, <code>fetch_url</code>, <code>scrape_page</code> <code>document_processing</code> PDF/DOCX read/write, conversion <code>clipboard</code> <code>clipboard_read</code>, <code>clipboard_write</code> <code>shell</code> <code>run_shell</code>, <code>python_exec</code> <code>gui_interaction</code> <code>mouse_click</code>, <code>keyboard_input</code>, <code>screenshot</code> (only when GUI mode on) <p>Integrations (Discord, Slack, Google, \u2026) contribute additional actions that are enabled automatically once the integration is connected. See Connections overview.</p>"},{"location":"concepts/action/#selection","title":"Selection","text":"<p>The action router picks actions via one of four prompts depending on the current workflow:</p> Workflow Prompt Conversation <code>SELECT_ACTION_PROMPT</code> Simple task <code>SELECT_ACTION_IN_SIMPLE_TASK_PROMPT</code> Complex task <code>SELECT_ACTION_IN_TASK_PROMPT</code> GUI task <code>SELECT_ACTION_IN_GUI_PROMPT</code> <p>All four are overridable via the prompt registry.</p>"},{"location":"concepts/action/#execution","title":"Execution","text":"<p>Once picked, actions flow through a four-step beat (see Agent loop):</p> <ol> <li><code>_retrieve_and_prepare_actions</code> \u2014 resolve the action by name, bind inputs, wire <code>parent_id</code> so downstream events link back</li> <li><code>_execute_actions</code> \u2014 run the action (sync or async), catch exceptions</li> <li>Event logging \u2014 <code>action_start</code> + <code>action_end</code> events are added to the stream</li> <li>Output \u2014 the action's return dict flows back to the workflow, which may feed into the next iteration</li> </ol> <p>Multiple actions can run in parallel within one iteration if the router returns multiple decisions.</p>"},{"location":"concepts/action/#platform-dispatch","title":"Platform dispatch","text":"<p>An action can have per-platform implementations. <code>run_shell</code> has three variants: <code>shell_exec</code> (shared default), <code>shell_exec_windows</code>, <code>shell_exec_darwin</code>. When the registry looks up an action, it picks the platform-specific implementation first, falling back to <code>PLATFORM_ALL</code>. This is how CraftBot does \"run on macOS with zsh, Windows with cmd, Linux with bash\" from one logical action name.</p>"},{"location":"concepts/action/#adding-your-own","title":"Adding your own","text":"<p>See Custom action \u2014 one file, <code>@register_action</code> decorator, done. Your action appears in the registry on the next agent start.</p>"},{"location":"concepts/action/#related","title":"Related","text":"<ul> <li>Skill &amp; action selection \u2014 how the router chooses</li> <li>CLI-anything \u2014 the <code>run_shell</code> escape hatch</li> <li>Actions catalogue \u2014 every built-in action</li> <li>Custom action \u2014 write your own</li> <li>Skills \u2014 bundling multiple actions into a capability</li> </ul>"},{"location":"concepts/agent-file-system/","title":"Agent file system","text":"<p>The agent file system is CraftBot's persistent memory layout on disk \u2014 a directory of markdown files that the agent reads from and writes to. It's how the agent remembers who it is, who you are, what you've asked for, what it's done, and what's on its mind.</p>"},{"location":"concepts/agent-file-system/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>A folder at <code>agent_file_system/</code> in the project root.</li> <li>Roughly 10 markdown files, each with a distinct role.</li> <li>Plus a <code>workspace/</code> subdirectory for scratch/tmp files.</li> <li>The agent treats these files as its knowledge base \u2014 they're injected into prompts (via the context engine), indexed by memory, and modified by actions like <code>stream_edit</code> and <code>write_file</code>.</li> </ul> <p>Edit them, and the agent's behaviour changes. Most fields are filled by onboarding or the agent itself.</p>"},{"location":"concepts/agent-file-system/#inspect-it-now","title":"Inspect it now","text":"<pre><code>ls agent_file_system/\n</code></pre> <pre><code>AGENT.md                        # Agent identity, role, protocols\nUSER.md                         # User profile, preferences, goals\nMEMORY.md                       # Distilled long-term memories\nEVENT.md                        # Raw event log\nEVENT_UNPROCESSED.md            # Events awaiting memory distillation\nTASK_HISTORY.md                 # Completed tasks\nCONVERSATION_HISTORY.md         # User \u2194 agent transcripts\nPROACTIVE.md                    # Scheduled autonomous tasks\nSOUL.md                         # Persistent personality traits\nFORMAT.md                       # Message formatting standards\nMISSION_INDEX_TEMPLATE.md       # Template for multi-mission indexing\nworkspace/                      # Scratch dir for task outputs\n</code></pre>"},{"location":"concepts/agent-file-system/#the-files","title":"The files","text":"File Written by Read by Purpose AGENT.md Human / agent Every LLM call (via <code>AGENT_INFO_PROMPT</code>) Identity, role, error-handling protocol, self-improvement workflow USER.md Onboarding / user Every LLM call (via <code>USER_PROFILE_PROMPT</code>) Full name, email, timezone, communication preferences, life goals MEMORY.md Memory distillation Via memory_search retrieval Long-term important facts, decisions, history EVENT.md Agent Humans only (you) Raw chronological log of everything EVENT_UNPROCESSED.md Agent Memory distillation task Events awaiting promotion to MEMORY.md TASK_HISTORY.md Task manager Humans / memory search Finished task records with summaries CONVERSATION_HISTORY.md UI layer Context for follow-ups User \u2194 agent conversation transcripts PROACTIVE.md Human / planner Proactive heartbeat Scheduled recurring tasks with rubrics SOUL.md Human / agent Every LLM call (via <code>SOUL_PROMPT</code>) Persistent personality / character traits FORMAT.md Human Agent reads when formatting output Message format standards"},{"location":"concepts/agent-file-system/#workspace","title":"workspace/","text":"<p>The scratch directory. Tasks write temp files here (report drafts, downloaded PDFs, screenshots) when the output doesn't belong in the permanent file system. Path: <code>agent_file_system/workspace/</code>, exposed as <code>AGENT_WORKSPACE_ROOT</code> in code.</p> <p>Lifecycle: not auto-cleaned. You're free to periodically purge it. Per-task <code>temp_dir</code> directories inside workspace are cleaned when the task ends \u2014 but files the agent explicitly wrote to workspace are kept.</p>"},{"location":"concepts/agent-file-system/#how-the-files-feed-the-llm","title":"How the files feed the LLM","text":"<p>The context engine injects several files directly into the system prompt:</p> <pre><code>System prompt:\n  AGENT_ROLE_PROMPT\n+ AGENT_INFO_PROMPT (fills from AGENT.md)\n+ SOUL_PROMPT (fills from SOUL.md)\n+ AGENT_FILE_SYSTEM_CONTEXT_PROMPT (lists every file + schema)\n+ USER_PROFILE_PROMPT (fills from USER.md)\n+ POLICY_PROMPT\n+ ENVIRONMENTAL_CONTEXT_PROMPT\n</code></pre> <p><code>MEMORY.md</code> and the rest are not injected wholesale \u2014 the agent retrieves from them via <code>memory_search</code> when it needs to.</p>"},{"location":"concepts/agent-file-system/#editing-safely","title":"Editing safely","text":"<p>You can hand-edit any file. The agent picks up changes on the next trigger (no restart needed). Two patterns:</p> <ul> <li>AGENT.md / SOUL.md / PROACTIVE.md / FORMAT.md \u2014 you edit these by hand or ask the agent to.</li> <li>EVENT.md / EVENT_UNPROCESSED.md / TASK_HISTORY.md / CONVERSATION_HISTORY.md / MEMORY.md \u2014 the agent writes these. Editing by hand works but may get overwritten.</li> </ul> <p>Preserve HTML comment markers in PROACTIVE.md</p> <p><code>PROACTIVE.md</code> uses HTML comment markers like <code>&lt;!-- PROACTIVE_TASKS_START --&gt;</code> that the proactive parser depends on. Don't remove them.</p>"},{"location":"concepts/agent-file-system/#templates","title":"Templates","text":"<p>The first-run onboarding copies templates from <code>app/data/</code> into <code>agent_file_system/</code>. If you delete a file, the agent regenerates it from the template.</p>"},{"location":"concepts/agent-file-system/#agent-bundle-overrides","title":"Agent bundle overrides","text":"<p>A subclassed agent bundle can point to its own file system via <code>data_dir</code> in <code>config.yaml</code>:</p> <pre><code>data_dir: agents/dog_agent/data/\n</code></pre> <p>The bundled file system replaces the default one for that agent.</p>"},{"location":"concepts/agent-file-system/#related","title":"Related","text":"<ul> <li>Agent MD files reference \u2014 schema of each file</li> <li>Memory \u2014 how these files get indexed and retrieved</li> <li>Context engine \u2014 how they're injected into prompts</li> <li>Custom agent \u2014 per-agent file system overrides</li> </ul>"},{"location":"concepts/agent-loop/","title":"Agent loop","text":"<p>The agent loop is the cycle CraftBot runs every time something wakes it up: trigger \u2192 route \u2192 select action \u2192 execute \u2192 observe.</p>"},{"location":"concepts/agent-loop/#beginner-mental-model","title":"Beginner mental model","text":"<p>Think of the loop in three parts:</p> <ul> <li>The doorbell \u2014 a trigger fires (user message, schedule, external event, waiting-for-reply timeout).</li> <li>The dispatcher \u2014 CraftBot decides which of five workflows handles this trigger: memory, proactive, GUI task, complex task, simple task, or conversation.</li> <li>The worker \u2014 inside each workflow, a three-step beat: pick an action, execute it, record what happened onto the event stream. Repeat until the trigger is done.</li> </ul> <p>The agent does not run continuously. It sleeps until a trigger fires, then runs exactly one iteration of the loop. Long-running work happens by scheduling follow-up triggers.</p>"},{"location":"concepts/agent-loop/#inspect-it-now","title":"Inspect it now","text":"<p>Tail your logs and send a message. You'll see the loop's routing decisions:</p> <pre><code>tail -f logs/*.log | grep -E \"\\[REACT\\]|\\[WORKFLOW\"\n</code></pre>"},{"location":"concepts/agent-loop/#example-output","title":"Example output","text":"<pre><code>[REACT] starting...\n[STATE] session_id=None | current_task_id=None | current_task=None\n[WORKFLOW: CONVERSATION] Query: what's the weather in Tokyo\n</code></pre> <p>For a proactive heartbeat firing at midnight:</p> <pre><code>[PROACTIVE] Trigger fired: type=proactive_heartbeat, frequency=daily, scope=\n[PROACTIVE] Created unified heartbeat task: 4f2c1a...\n[REACT] starting...\n[WORKFLOW: SIMPLE TASK] Query: Execute due proactive tasks (3 daily)\n</code></pre>"},{"location":"concepts/agent-loop/#routing-rules","title":"Routing rules","text":"<p><code>react()</code> checks trigger type and session state in this order \u2014 the first match wins:</p> Order Condition Workflow 1 <code>trigger.payload.type == \"memory_processing\"</code> Memory \u2014 distils <code>EVENT_UNPROCESSED.md</code> into <code>MEMORY.md</code> 2 <code>trigger.payload.type</code> in <code>proactive_heartbeat</code> / <code>proactive_planner</code> Proactive \u2014 see Proactive mode 3 Session is active AND <code>STATE.gui_mode == True</code> GUI task \u2014 vision-driven desktop control 4 Session is active AND task is complex Complex task \u2014 todo-managed multi-step 5 Session is active AND task is simple Simple task \u2014 linear run-to-completion 6 fallthrough Conversation \u2014 no active task, reply or create one"},{"location":"concepts/agent-loop/#the-action-beat","title":"The action beat","text":"<p>Inside workflows #3\u2013#6, every iteration runs four phases:</p> <ol> <li><code>_select_action(trigger_data)</code> \u2014 LLM chooses one or more actions (and their inputs) from the library. See Skill &amp; action selection.</li> <li><code>_retrieve_and_prepare_actions(...)</code> \u2014 resolves each selected action by name, binds its inputs, and wires the <code>parent_id</code> so child events link to their parent.</li> <li><code>_execute_actions(...)</code> \u2014 runs them, collects outputs, records events onto the stream.</li> <li><code>_finalize_action_execution(...)</code> \u2014 updates the session pointer (a new <code>task_id</code> may have been created), cleans up caches.</li> </ol> <p>Conversation, simple, and complex modes all share this beat \u2014 they differ only in what caching strategy is used and whether todos are tracked. See Task modes for the differences.</p>"},{"location":"concepts/agent-loop/#knobs","title":"Knobs","text":"<p>Configuration that changes how the loop behaves lives in <code>app/config/settings.json</code>:</p> <pre><code>{\n  \"memory\":    { \"enabled\": true },\n  \"proactive\": { \"enabled\": true },\n  \"gui\":       { \"enabled\": false }\n}\n</code></pre> <ul> <li>Disable memory \u2192 memory workflow skipped, triggers typed <code>memory_processing</code> no-op.</li> <li>Disable proactive \u2192 proactive triggers logged then skipped.</li> <li>Disable gui \u2192 GUI mode branch never taken; vision tasks fall back to text.</li> </ul>"},{"location":"concepts/agent-loop/#waiting-for-reply-behavior","title":"Waiting-for-reply behavior","text":"<p>If a task is marked <code>waiting_for_user_reply</code> and a trigger fires with no user message, the loop re-schedules itself with a 3-hour delay. This lets long-running interactive tasks pause until the user replies, without blocking the agent.</p>"},{"location":"concepts/agent-loop/#error-handling","title":"Error handling","text":"<p>Every <code>react()</code> call is wrapped in try/except. Errors are caught by <code>_handle_react_error</code>, logged, and the session is cleaned up in <code>finally</code>. The next trigger starts from a clean slate.</p>"},{"location":"concepts/agent-loop/#related","title":"Related","text":"<ul> <li>Triggers \u2014 what wakes the loop up</li> <li>Task sessions \u2014 the unit the loop operates on</li> <li>Event stream \u2014 where the loop's output lands</li> <li>Task modes \u2014 the six workflow variants</li> <li>Actions \u2014 the vocabulary inside every workflow</li> </ul>"},{"location":"concepts/context-engine/","title":"Context engine","text":"<p>The context engine is the piece that assembles the prompt sent to the LLM for every call. It takes the static agent identity, the user profile, the environment, the agent file system, the live task, and the event stream, and stitches them into a <code>(system_prompt, user_prompt)</code> pair with a carefully designed cache boundary.</p>"},{"location":"concepts/context-engine/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>Every LLM call needs two pieces: a system prompt (who you are, rules, policies) and a user prompt (what to do, with current state).</li> <li>The engine builds the system prompt once per call from static pieces that rarely change (agent identity, policy, user profile, environment).</li> <li>The user prompt has a static template followed by dynamic content (event stream, memory hits, query) followed by output format.</li> <li>The split between static and dynamic is deliberate: everything before the dynamic content can be KV-cached by the LLM provider, turning subsequent calls into a cheap incremental append.</li> </ul> <p>Cache well \u2192 10\u00d7 faster follow-up calls. Don't cache \u2192 pay full price every time.</p>"},{"location":"concepts/context-engine/#inspect-it-now","title":"Inspect it now","text":"<p>Dump the next system prompt:</p> <pre><code>from agent_core import ContextEngineRegistry\nengine = ContextEngineRegistry.get()\nprint(engine.build_system_prompt())\n</code></pre>"},{"location":"concepts/context-engine/#system-prompt-ingredients","title":"System-prompt ingredients","text":"<p>Assembled in this fixed order (each from a named prompt in the registry):</p> # Prompt Source 1 <code>AGENT_ROLE_PROMPT</code> Subclass-overridable persona 2 <code>AGENT_INFO_PROMPT</code> + <code>SOUL_PROMPT</code> <code>AGENT.md</code> + <code>SOUL.md</code> 3 <code>AGENT_FILE_SYSTEM_CONTEXT_PROMPT</code> File list, schemas, what each <code>.md</code> does 4 <code>USER_PROFILE_PROMPT</code> <code>USER.md</code> 5 <code>POLICY_PROMPT</code> Safety/approval rules, language instructions 6 <code>ENVIRONMENTAL_CONTEXT_PROMPT</code> OS, timezone, current time, detected language <p>This whole block is static for the duration of a session. Providers that support prompt caching (Anthropic, Gemini, BytePlus) cache it after the first call \u2014 subsequent calls only pay for the user-prompt half.</p>"},{"location":"concepts/context-engine/#user-prompt-ingredients","title":"User-prompt ingredients","text":"Part Content Template <code>SELECT_ACTION_*_PROMPT</code> for the current workflow (see Skill &amp; action selection) Static block Available actions list, task instructions, selected skills Dynamic block Event stream snapshot (via <code>to_prompt_snapshot()</code>), memory hits, current query Output format JSON schema the LLM must match <p>Keeping the static block before the dynamic block lets providers cache through the template-plus-actions portion \u2014 only the last ~N events and the query change call-to-call.</p>"},{"location":"concepts/context-engine/#kv-caching-strategy","title":"KV caching strategy","text":"<p>CraftBot uses two levels of cache:</p> <ol> <li>Prefix cache \u2014 the system prompt. Applied by the provider automatically when the call reuses a cached prefix. Free after the first paid call.</li> <li>Session cache \u2014 per-workflow. The engine tracks which events have been synced to the provider cache via <code>mark_session_synced()</code>. Subsequent calls send only delta events via <code>get_delta_events()</code>, letting the cache grow incrementally.</li> </ol> <p>If the event stream summarizes (token threshold hit), the session cache is invalidated for all workflows \u2014 events past the sync point no longer exist.</p>"},{"location":"concepts/context-engine/#hooks","title":"Hooks","text":"<p>The engine exposes three optional hooks for runtimes that need extra context:</p> Hook Used for <code>get_conversation_history_hook</code> Enterprise chat-server runtime (WCA) <code>get_chat_target_info_hook</code> Enterprise chat-server runtime (WCA) <code>get_user_info_hook</code> Enterprise chat-server runtime (WCA) <p>CraftBot default: unset (returns empty string).</p>"},{"location":"concepts/context-engine/#message-source-block","title":"Message source block","text":"<p>When a message arrives from an external platform (Slack, Telegram, etc.), the engine injects a small XML block telling the LLM who is talking:</p> <pre><code>&lt;message_source&gt;\n  &lt;platform&gt;Slack&lt;/platform&gt;\n  &lt;contact_name&gt;Bob&lt;/contact_name&gt;\n  &lt;channel_name&gt;#engineering&lt;/channel_name&gt;\n&lt;/message_source&gt;\n</code></pre> <p>This lets the agent address replies correctly when multiple platforms route to the same task.</p>"},{"location":"concepts/context-engine/#memory-retrieval","title":"Memory retrieval","text":"<p>Before each LLM call, the engine asks memory for the top-K relevant pointers for the current query:</p> <pre><code>memory_context = context_engine.get_memory_context(query)\n</code></pre> <p>The result is a compact list of <code>[file_path] section_path \u2014 summary</code> pointers. The agent then calls <code>read_file</code> to pull the full content only if it needs to \u2014 this keeps context small while retaining recall.</p>"},{"location":"concepts/context-engine/#knobs","title":"Knobs","text":"<p>Most tuning lives in <code>settings.json</code> (memory enabled, model provider, etc.). Deeper customization \u2014 cache tier, memory top-K, custom prompt injection \u2014 is in the engine constructor and best changed via Custom agent.</p>"},{"location":"concepts/context-engine/#related","title":"Related","text":"<ul> <li>Prompts \u2014 the components being assembled</li> <li>Event stream \u2014 the dynamic half of the user prompt</li> <li>Memory \u2014 how retrieval feeds the engine</li> <li>LLM providers \u2014 which providers support prefix caching</li> </ul>"},{"location":"concepts/event-stream/","title":"Event stream","text":"<p>The event stream is the per-session narrative of everything that happened during a task: actions started, actions finished, messages sent, errors caught. It's what the UI renders, what the LLM sees as history, and what gets summarized into memory.</p>"},{"location":"concepts/event-stream/#beginner-mental-model","title":"Beginner mental model","text":"<p>Think of it as append-only logbook per task session, with an automatic summarizer:</p> <ul> <li>Every event is a <code>kind</code>, a <code>message</code>, and a <code>severity</code> (DEBUG / INFO / WARN / ERROR).</li> <li>Events accumulate in <code>tail_events</code> \u2014 recent, full-fidelity.</li> <li>When the token count crosses a threshold, the oldest events get rolled up into a textual <code>head_summary</code> by the LLM.</li> <li>The agent reads the stream via <code>to_prompt_snapshot()</code> which returns <code>head_summary + recent tail events</code>.</li> </ul> <p>One stream per session. Streams are created lazily when a task starts and cleared when it ends.</p>"},{"location":"concepts/event-stream/#inspect-it-now","title":"Inspect it now","text":"<p>Every interface renders the stream live. In the TUI it fills the main pane. In logs, filter for event lines:</p> <pre><code>tail -f logs/*.log | grep -E \"action_start|action_end|EventStream\"\n</code></pre>"},{"location":"concepts/event-stream/#example","title":"Example","text":"<pre><code>[EventStream] Triggering summarization: 31204 tokens &gt;= 30000 threshold\n[EventStream] Running synchronous summarization (31204 tokens)\n[EventStream] Summarization complete. Tokens: 9845\n</code></pre> <p>A compact event in <code>to_prompt_snapshot()</code> format looks like:</p> <pre><code>Recent Event:\n[2026-04-22 10:14:02] action_start: send_gmail\n[2026-04-22 10:14:04] action_end: send_gmail -&gt; ok (message_id=18a2f3e)\n[2026-04-22 10:14:04] action_start: memory_search\n[2026-04-22 10:14:06] action_end: memory_search -&gt; ok (hits=3)\n</code></pre>"},{"location":"concepts/event-stream/#what-gets-logged","title":"What gets logged","text":"<p>The agent logs four main kinds, plus anything you add:</p> Kind When <code>action_start</code> An action has been selected and is about to run <code>action_end</code> The action finished; message includes <code>status</code> and extras <code>user_message</code> A user message arrives (local or routed from Slack, Discord, etc.) <code>agent_message</code> The agent sends a reply <p>Custom event kinds are fine \u2014 any string is accepted.</p>"},{"location":"concepts/event-stream/#summarization","title":"Summarization","text":"<p>Auto-summarization kicks in when <code>_total_tokens &gt;= summarize_at_tokens</code> (default 30,000). The oldest events are packaged with the current <code>head_summary</code> (if any) and sent to the LLM via <code>EVENT_STREAM_SUMMARIZATION_PROMPT</code>. The LLM returns a new summary that replaces the old one; the summarized events are dropped. The tail is truncated so that the remaining events total \u2264 <code>tail_keep_after_summarize_tokens</code> (default 10,000).</p> <p>If the LLM is in a consecutive-failure state, summarization falls back to prune-without-summary \u2014 oldest events are simply dropped so the tail stays under the threshold. This prevents retry storms during provider outages.</p>"},{"location":"concepts/event-stream/#tunable-thresholds","title":"Tunable thresholds","text":"<p>Default thresholds live in the <code>EventStream</code> constructor:</p> <pre><code>EventStream(\n    llm=...,\n    summarize_at_tokens=30000,\n    tail_keep_after_summarize_tokens=10000,\n)\n</code></pre> <p>Raise <code>summarize_at_tokens</code> for longer live history (costs more tokens per call). Lower <code>tail_keep_after_summarize_tokens</code> for more aggressive compaction. To override per-agent, pass custom values when the stream is created, or subclass the agent (see Custom agent).</p> <p>Relationship constraint</p> <p><code>tail_keep_after_summarize_tokens + 2000</code> must be less than <code>summarize_at_tokens</code>. If you set it too high, the stream clamps it to <code>summarize_at_tokens - 2000</code> and logs a warning.</p>"},{"location":"concepts/event-stream/#long-message-externalization","title":"Long message externalization","text":"<p>Any single event message longer than 200,000 chars is written to a temp file instead of living in the stream. The event becomes a pointer:</p> <pre><code>Action grep_files completed. The output is too long therefore is saved in\n/tmp/event_grep_files_20260422_101432123.txt to save token.\n| keywords: user, authentication, redirect\n| To retrieve the content, agent MUST use the 'grep_files' action ...\n</code></pre> <p>The agent learns to read the pointer file via <code>stream_read</code> or <code>grep</code> when it needs the full content. This keeps context windows sane on actions that return huge outputs.</p>"},{"location":"concepts/event-stream/#session-cache-delta-tracking","title":"Session cache delta tracking","text":"<p>When CraftBot uses session-level prompt caching (see Context engine), only new events since the last sync need to be sent to the cache. The stream keeps per-call-type sync points (<code>_session_sync_points</code>) and <code>get_delta_events(call_type)</code> returns only events appended after the last sync. If summarization has occurred between syncs, the cache is invalidated and re-populated from scratch.</p>"},{"location":"concepts/event-stream/#severity","title":"Severity","text":"<p>Every event has a severity (<code>DEBUG</code>, <code>INFO</code>, <code>WARN</code>, <code>ERROR</code>, default <code>INFO</code>). The UI can filter by severity; logs can too. Severity doesn't affect summarization \u2014 the LLM summarizer sees everything.</p>"},{"location":"concepts/event-stream/#clearing-a-stream","title":"Clearing a stream","text":"<p><code>EventStream.clear()</code> wipes <code>head_summary</code>, <code>tail_events</code>, token count, and sync points. Used when a session id is reused for a new task, so no stale context leaks between runs.</p>"},{"location":"concepts/event-stream/#related","title":"Related","text":"<ul> <li>Agent loop \u2014 the producer</li> <li>Task sessions \u2014 one stream per session</li> <li>Context engine \u2014 how the stream is injected into prompts</li> <li>Memory \u2014 where summarized events end up long-term</li> <li>Event types reference \u2014 full <code>kind</code> catalogue</li> </ul>"},{"location":"concepts/logs/","title":"Logs","text":"<p>Every CraftBot run writes a timestamped log file to <code>logs/</code> at the project root. Logs are the first thing to read when something goes wrong \u2014 they capture trigger routing, action calls, LLM errors, summarization events, and connection flows.</p>"},{"location":"concepts/logs/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>CraftBot uses Loguru.</li> <li>Each run creates a fresh log file at <code>logs/&lt;YYYYMMDDhhmmss&gt;.log</code> \u2014 tail this, not the console.</li> <li>Logs rotate at 50 MB and are kept for 14 days.</li> <li>The console is muted by default \u2014 to see logs while running, tail the file.</li> </ul>"},{"location":"concepts/logs/#inspect-it-now","title":"Inspect it now","text":"<p>Find the latest log and tail it:</p> <pre><code>tail -f logs/*.log\n</code></pre> <p>Filter for what you need:</p> <pre><code>tail -f logs/*.log | grep -E \"\\[REACT\\]|\\[WORKFLOW|ERROR\"\n</code></pre>"},{"location":"concepts/logs/#log-location","title":"Log location","text":"<ul> <li>Directory: <code>logs/</code> at the project root.</li> <li>Format: <code>&lt;YYYYMMDDhhmmss&gt;.log</code> (no prefix), or <code>&lt;name&gt;_&lt;timestamp&gt;.log</code> when a custom name is passed.</li> <li>Rotation: automatic at 50 MB. Older files kept for 14 days.</li> </ul>"},{"location":"concepts/logs/#severity-format","title":"Severity &amp; format","text":"<p>Default severity: DEBUG for the file sink, ERROR for the (currently muted) console. Loguru format preserves tracebacks and local variables via <code>backtrace=True</code>, <code>diagnose=True</code>.</p> <p>Key log-line prefixes to know:</p> Prefix Emitter <code>[REACT]</code> Agent loop entering a reaction <code>[WORKFLOW: ...]</code> Which workflow handled the trigger <code>[STATE]</code> Session / current-task state snapshots <code>[PUT]</code> / <code>[GET]</code> / <code>[FIRE]</code> / <code>[TRIGGER QUEUE]</code> Trigger queue operations <code>[EventStream]</code> Summarization and stream lifecycle <code>[MEMORY]</code> Memory processing and retrieval <code>[PROACTIVE]</code> Proactive heartbeat / planner <code>[TaskManager]</code> Task persistence / recovery <code>[LLM]</code> LLM provider calls and errors <code>[OAuth]</code> / <code>[Credentials]</code> Integration auth flows"},{"location":"concepts/logs/#configuring-log-level","title":"Configuring log level","text":"<p>The logger is initialized with defaults at startup (via <code>define_log_level()</code>), but subclassed agents can customize:</p> <pre><code>from app.logger import define_log_level\n\nlogger = define_log_level(\n    print_level=\"INFO\",   # console threshold (currently muted by default)\n    logfile_level=\"DEBUG\",\n    name=\"my_agent\",       # filename prefix\n)\n</code></pre>"},{"location":"concepts/logs/#enabling-the-console-sink","title":"Enabling the console sink","text":"<p>The console output is commented out in <code>app/logger.py</code> by default. To see logs live in your terminal, uncomment the <code>_logger.add(sys.stderr, ...)</code> block and restart.</p>"},{"location":"concepts/logs/#what-doesnt-go-to-logs","title":"What doesn't go to logs","text":"<ul> <li>LLM prompts and responses \u2014 too large; check the event stream for these.</li> <li>User messages \u2014 tracked in agent_file_system/EVENT.md and in the event stream, not logs.</li> <li>Secrets \u2014 API keys and tokens are masked before logging. If you see one appear in logs, it's a bug; please report it.</li> </ul>"},{"location":"concepts/logs/#retention","title":"Retention","text":"<p>Default retention is 14 days. Change by re-initializing:</p> <pre><code>_logger.add(log_path, rotation=\"100 MB\", retention=\"30 days\", ...)\n</code></pre>"},{"location":"concepts/logs/#related","title":"Related","text":"<ul> <li>Troubleshooting \u2014 symptom \u2192 log-line patterns</li> <li>Event stream \u2014 the agent's per-task narrative (distinct from logs)</li> <li>Configuration \u2014 where <code>logs/</code> lives among other paths</li> </ul>"},{"location":"concepts/memory/","title":"Memory","text":"<p>CraftBot's memory is a RAG system over the agent file system. It chunks every markdown file into semantic sections, embeds them in ChromaDB, and returns pointers (not content) when the agent searches. Short-term events accumulate in <code>EVENT_UNPROCESSED.md</code>, get distilled daily into <code>MEMORY.md</code>, and become part of the searchable corpus.</p>"},{"location":"concepts/memory/#beginner-mental-model","title":"Beginner mental model","text":"<p>Memory has three moving parts:</p> <ul> <li>Live stream \u2014 events stack into <code>EVENT_UNPROCESSED.md</code> as the agent works.</li> <li>Distillation \u2014 once a day (default hour <code>3</code> AM), a memory-processing task reads <code>EVENT_UNPROCESSED.md</code>, scores each event, writes important ones to <code>MEMORY.md</code>, and clears the unprocessed file.</li> <li>Retrieval \u2014 the context engine asks memory for the top-K pointers relevant to the current query. Pointers are lightweight (<code>file_path</code>, <code>section_path</code>, <code>summary</code>), so the agent can decide whether to pull the full content with <code>read_file</code>.</li> </ul> <p>Memory never returns raw content. It returns \"where to look.\" This is the key design choice \u2014 it keeps the context window small while preserving recall across long-running agents.</p>"},{"location":"concepts/memory/#inspect-it-now","title":"Inspect it now","text":"<p>Search memory from the agent:</p> <pre><code>/memory_search authentication redirect URI\n</code></pre> <p>Or from Python:</p> <pre><code>from agent_core import MemoryManager\nmanager = MemoryManager()  # or retrieve from registry\npointers = manager.retrieve(\"authentication redirect URI\", top_k=5)\nfor p in pointers:\n    print(p)\n</code></pre>"},{"location":"concepts/memory/#example-output","title":"Example output","text":"<pre><code>[USER.md] ## Identity - Email: ... Location: Tokyo, Timezone: JST\n[MEMORY.md] ### Auth migration (2026-04-10) - Migrated OAuth redirect URIs from localhost:8080 to localhost:8765\n[AGENT.md] ## Self-Improvement Protocol - Search MCP servers when capability is missing\n</code></pre>"},{"location":"concepts/memory/#anatomy-of-a-chunk","title":"Anatomy of a chunk","text":"<pre><code>@dataclass\nclass MemoryChunk:\n    chunk_id: str          # UUID\n    file_path: str         # Relative to agent_file_system/\n    section_path: str      # \"## Header &gt; ### Sub-header\"\n    title: str             # Last header in path\n    content: str           # Full section content\n    summary: str           # First ~150 chars \u2014 what the pointer shows\n    content_hash: str      # For change detection\n    file_modified_at: str\n    indexed_at: str\n</code></pre> <p>Chunks are semantic \u2014 one per markdown header. A file with 5 sections becomes 5 chunks, each independently retrievable.</p>"},{"location":"concepts/memory/#indexing","title":"Indexing","text":"<p>Memory auto-indexes every markdown file under <code>agent_file_system/</code> plus user-supplied RAG docs under <code>agents/&lt;agent&gt;/rag_docs/</code>. Indexing is incremental:</p> <ul> <li>On startup, the <code>MemoryFileWatcher</code> scans for files newer than their last-indexed timestamp.</li> <li>Only changed sections are re-embedded. Unchanged chunks keep their existing vectors.</li> <li>File deletions remove their chunks from the store.</li> </ul> <p>Vectors live in <code>chroma_db_memory/</code> by default.</p>"},{"location":"concepts/memory/#daily-distillation","title":"Daily distillation","text":"<p>A scheduled trigger fires at <code>MEMORY_PROCESSING_SCHEDULE_HOUR</code> (default <code>3</code> AM local). The resulting memory-processing task uses the <code>memory-processor</code> skill and runs this loop:</p> <ol> <li>Read <code>EVENT_UNPROCESSED.md</code> for the day's events</li> <li>For each event, score its importance for long-term recall</li> <li>Use <code>memory_search</code> to detect duplicates</li> <li>Write important, unique events to <code>MEMORY.md</code> as new sections</li> <li>Clear <code>EVENT_UNPROCESSED.md</code></li> </ol> <p>See Special workflows for the workflow that runs this task.</p>"},{"location":"concepts/memory/#the-memory_search-action","title":"The memory_search action","text":"<p>The agent itself has a <code>memory_search</code> action available in every task. Its inputs are a query string and <code>top_k</code>; its output is a list of pointers. Typical pattern:</p> <pre><code>1. memory_search(\"budget Q3\")     \u2192  3 pointers\n2. read_file(\"MEMORY.md\", ...)     \u2192  full section of the best hit\n3. act on the retrieved info\n</code></pre>"},{"location":"concepts/memory/#master-switch","title":"Master switch","text":"<pre><code>// settings.json\n{ \"memory\": { \"enabled\": true } }\n</code></pre> <p>When disabled:</p> <ul> <li>Memory-processing triggers are logged and skipped.</li> <li><code>memory_search</code> returns empty.</li> <li>The context engine's memory retrieval returns an empty string \u2014 the agent falls back to working from the event stream only.</li> </ul>"},{"location":"concepts/memory/#startup-processing","title":"Startup processing","text":"<p>By default, <code>PROCESS_MEMORY_AT_STARTUP</code> is <code>False</code> \u2014 the agent does not re-process the previous session's events on launch. Flip it to <code>True</code> in <code>app/config.py</code> if you want every startup to distill yesterday's events immediately.</p>"},{"location":"concepts/memory/#privacy","title":"Privacy","text":"<p>Memory is local. ChromaDB runs embedded. Embeddings are computed locally (ChromaDB's built-in) unless a custom embedding provider is configured. No memory content leaves the machine.</p>"},{"location":"concepts/memory/#related","title":"Related","text":"<ul> <li>Agent file system \u2014 the corpus being indexed</li> <li>Context engine \u2014 the consumer of memory retrieval</li> <li>Event stream \u2014 the live \"before it becomes memory\" buffer</li> <li>Special workflows \u2014 where the daily distillation runs</li> <li>Custom agent \u2014 how to add your own RAG docs via <code>rag_dir</code> in <code>config.yaml</code></li> </ul>"},{"location":"concepts/prompt/","title":"Prompts","text":"<p>Prompts are the text templates CraftBot sends to the LLM. They're where the agent's voice lives \u2014 how it reasons about actions, how it talks about itself, what policies it follows. Every prompt is registered in a central registry so you can override any of them without forking the codebase.</p>"},{"location":"concepts/prompt/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>A prompt is a named template string with <code>{placeholders}</code>.</li> <li>The prompt registry stores every built-in prompt by name.</li> <li>The context engine pulls prompts from the registry, fills placeholders, and assembles the final LLM input.</li> <li>You can override any prompt by registering a new version with the same name \u2014 the registry uses last-write-wins.</li> </ul> <p>Prompts are text \u2014 they don't execute code. They're how you shape behaviour without writing Python.</p>"},{"location":"concepts/prompt/#inspect-it-now","title":"Inspect it now","text":"<p>List every registered prompt from Python:</p> <pre><code>from app.prompt import prompt_registry\nfor name in prompt_registry.list_names():\n    print(name)\n</code></pre>"},{"location":"concepts/prompt/#the-prompt-families","title":"The prompt families","text":"Family Prompts Used by Event stream <code>EVENT_STREAM_SUMMARIZATION_PROMPT</code> Event stream rollup Action selection <code>SELECT_ACTION_PROMPT</code>, <code>SELECT_ACTION_IN_TASK_PROMPT</code>, <code>SELECT_ACTION_IN_SIMPLE_TASK_PROMPT</code>, <code>SELECT_ACTION_IN_GUI_PROMPT</code> Action router Context <code>AGENT_ROLE_PROMPT</code>, <code>AGENT_INFO_PROMPT</code>, <code>POLICY_PROMPT</code>, <code>USER_PROFILE_PROMPT</code>, <code>ENVIRONMENTAL_CONTEXT_PROMPT</code>, <code>AGENT_FILE_SYSTEM_CONTEXT_PROMPT</code> Context engine system-prompt assembly Routing <code>ROUTE_TO_SESSION_PROMPT</code> Trigger queue \u2014 match incoming triggers to sessions GUI <code>GUI_REASONING_PROMPT</code>, <code>GUI_REASONING_PROMPT_OMNIPARSER</code>, <code>GUI_QUERY_FOCUSED_PROMPT</code>, <code>GUI_PIXEL_POSITION_PROMPT</code>, <code>GUI_ACTION_SPACE_PROMPT</code> GUI / Vision mode Skill selection <code>SKILLS_AND_ACTION_SETS_SELECTION_PROMPT</code>, <code>SKILL_SELECTION_PROMPT</code>, <code>ACTION_SET_SELECTION_PROMPT</code> Task creation \u2014 picks skills + action sets <p>All of them are <code>import</code>-able from <code>app.prompt</code> for reading or overriding.</p>"},{"location":"concepts/prompt/#the-registry","title":"The registry","text":"<pre><code>from app.prompt import register_prompt, get_prompt\n\n# Read the current version\ncurrent = get_prompt(\"SELECT_ACTION_PROMPT\")\n\n# Override it\nregister_prompt(\n    \"SELECT_ACTION_PROMPT\",\n    \"\"\"Your custom prompt here with {placeholders}...\"\"\",\n)\n</code></pre> <p>Overrides take effect immediately \u2014 no restart needed. The registry is shared across the whole agent.</p>"},{"location":"concepts/prompt/#assembly-system-prompt-vs-user-prompt","title":"Assembly: system prompt vs user prompt","text":"<p>Every LLM call uses two pieces:</p> <ul> <li>System prompt \u2014 built once per call by the context engine, concatenating: <code>AGENT_ROLE_PROMPT</code>, <code>AGENT_INFO_PROMPT</code>, <code>POLICY_PROMPT</code>, <code>USER_PROFILE_PROMPT</code>, <code>ENVIRONMENTAL_CONTEXT_PROMPT</code>, <code>AGENT_FILE_SYSTEM_CONTEXT_PROMPT</code>. This part is KV-cached across calls.</li> <li>User prompt \u2014 the specific question: <code>SELECT_ACTION_PROMPT</code> + event stream snapshot + the trigger's <code>next_action_description</code>. This part changes every call.</li> </ul> <p>Splitting the boundary this way lets the provider cache the system half (see Context engine), cutting cost and latency dramatically.</p>"},{"location":"concepts/prompt/#placeholders","title":"Placeholders","text":"<p>Each prompt declares its own <code>{placeholders}</code>. Common ones:</p> Placeholder Filled with <code>{agent_name}</code> From <code>general.agent_name</code> in settings.json <code>{user_profile}</code> From <code>agent_file_system/USER.md</code> <code>{current_task}</code> The running task's <code>instruction</code> + todos <code>{event_stream}</code> Output of <code>EventStream.to_prompt_snapshot()</code> <code>{available_actions}</code> Compiled from the task's <code>action_sets</code> <code>{selected_skills}</code> Skill instructions injected from <code>selected_skills</code> <code>{os_language}</code> Detected OS language code"},{"location":"concepts/prompt/#prompt-overrides-for-custom-agents","title":"Prompt overrides for custom agents","text":"<p>A subclassed agent (see Custom agent) typically overrides just two prompts:</p> <ul> <li><code>AGENT_ROLE_PROMPT</code> \u2014 the persona (\"You are a no-nonsense research assistant specializing in \u2026\")</li> <li><code>POLICY_PROMPT</code> \u2014 the rules (\"Never execute actions that modify X without user approval \u2026\")</li> </ul> <p>Example:</p> <pre><code># agents/my_agent/agent.py\nclass MyAgent(AgentBase):\n    def _load_extra_system_prompt(self) -&gt; str:\n        return \"You speak like a 1920s detective. Be terse.\"\n</code></pre>"},{"location":"concepts/prompt/#related","title":"Related","text":"<ul> <li>Context engine \u2014 the consumer of the prompt registry</li> <li>Skill &amp; action selection \u2014 four action-selection prompt variants</li> <li>Event stream \u2014 uses <code>EVENT_STREAM_SUMMARIZATION_PROMPT</code></li> <li>Custom agent \u2014 the usual place to override prompts</li> </ul>"},{"location":"concepts/skill-selection/","title":"Skill &amp; action selection","text":"<p>Action selection is the decision the LLM makes every iteration: given what the user asked and what's happened so far, which action (or actions) do I run next? Getting this right is the difference between an agent that helps and one that spins.</p>"},{"location":"concepts/skill-selection/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>The action router is a class with four selection methods \u2014 one per workflow.</li> <li>Each method builds a prompt from (a) a template, (b) the current event stream, (c) the relevant memory for the query, and (d) a filtered list of action candidates.</li> <li>The LLM returns a list of action decisions in JSON: <code>[{\"action_name\": \"X\", \"parameters\": {...}, \"reasoning\": \"...\"}, ...]</code>. Multiple actions can run in parallel within one iteration.</li> <li>If the LLM returns bad JSON, the router retries up to 3 times with augmented error feedback before giving up.</li> </ul>"},{"location":"concepts/skill-selection/#inspect-it-now","title":"Inspect it now","text":"<p>The router logs every selection:</p> <pre><code>tail -f logs/*.log | grep -E \"PARALLEL|ACTION|FORMAT ERROR\"\n</code></pre>"},{"location":"concepts/skill-selection/#example-output","title":"Example output","text":"<pre><code>[PARALLEL] Conversation mode selected 1 action(s): ['task_start']\n[PARALLEL] Complex task mode selected 2 action(s): ['web_search', 'task_update_todos']\n</code></pre>"},{"location":"concepts/skill-selection/#four-selection-methods","title":"Four selection methods","text":"<p>The router has one method per workflow, each with its own prompt and candidate filtering:</p> Method Workflow Candidates Prompt <code>select_action</code> Conversation <code>send_message</code>, <code>task_start</code>, <code>ignore</code>, integration mgmt actions, + messaging actions for connected platforms <code>SELECT_ACTION_PROMPT</code> <code>select_action_in_simple_task</code> Simple task Actions from the task's <code>action_sets</code> <code>SELECT_ACTION_IN_SIMPLE_TASK_PROMPT</code> <code>select_action_in_task</code> Complex task Actions from the task's <code>action_sets</code> <code>SELECT_ACTION_IN_TASK_PROMPT</code> <code>select_action_in_GUI</code> GUI task GUI-visible actions + <code>GUI_ACTION_SPACE_PROMPT</code> <code>SELECT_ACTION_IN_GUI_PROMPT</code> <p>Each prompt is in the prompt registry \u2014 override it to change behaviour.</p>"},{"location":"concepts/skill-selection/#candidate-filtering","title":"Candidate filtering","text":"<p>Actions are filtered by visibility mode before being shown to the LLM:</p> Action <code>visibility_mode</code> Shown when <code>\"CLI\"</code> Non-GUI workflows only <code>\"GUI\"</code> GUI workflow only <code>\"ALL\"</code> / <code>None</code> Both <p>This prevents the LLM from selecting e.g. <code>mouse_click</code> during a conversation task.</p>"},{"location":"concepts/skill-selection/#dynamic-messaging-actions","title":"Dynamic messaging actions","text":"<p>In conversation mode, connected external integrations automatically contribute send/read actions to the candidate list. Example: after <code>/slack login</code>, <code>send_slack_message</code> appears alongside <code>send_message</code>. Disconnect the integration (<code>/slack logout</code>) and the action disappears.</p>"},{"location":"concepts/skill-selection/#parallel-action-execution","title":"Parallel action execution","text":"<p>The router can return multiple actions per iteration. These run in parallel via <code>_execute_actions</code> (see Agent loop). Useful for e.g. \"update my todos AND read the next doc\" \u2014 one turn, two actions.</p>"},{"location":"concepts/skill-selection/#format-error-retry","title":"Format-error retry","text":"<p>The LLM sometimes returns malformed JSON. The router retries up to 3 times with an augmented prompt that includes the previous attempt and the parse error. If all three fail, it raises <code>ValueError</code> and the task aborts to prevent token burn.</p> <pre><code>[FORMAT ERROR] Conversation mode attempt 2/3: Expected JSON array, got string\n</code></pre>"},{"location":"concepts/skill-selection/#skills-inject-instructions-not-actions","title":"Skills inject instructions, not actions","text":"<p>Skills (see Write a CraftBot skill) are orthogonal to action selection. A skill is a prompt-fragment + metadata bundle that gets injected into the system prompt when its parent task has <code>selected_skills: [\"my-skill\"]</code>. Skills don't add actions to the candidate list \u2014 they add guidance about how to use existing actions.</p> <p>Example: the <code>heartbeat-processor</code> skill tells the agent \"read PROACTIVE.md first, then filter by time/day, then execute\" \u2014 it doesn't provide any new actions, it provides a strategy over existing ones.</p>"},{"location":"concepts/skill-selection/#adding-your-own-decision-logic","title":"Adding your own decision logic","text":"<p>Three paths to influence selection:</p> <ol> <li>Override the prompt \u2014 change <code>SELECT_ACTION_IN_TASK_PROMPT</code> via <code>register_prompt()</code>.</li> <li>Add a skill \u2014 inject strategy into tasks that opt in via <code>selected_skills</code>.</li> <li>Subclass the agent \u2014 override <code>_select_action()</code> in your agent bundle for total control.</li> </ol>"},{"location":"concepts/skill-selection/#related","title":"Related","text":"<ul> <li>Agent loop \u2014 where selection fits in the cycle</li> <li>Actions \u2014 the vocabulary the router chooses from</li> <li>Prompts \u2014 the four selection prompts and the registry</li> <li>Skills \u2014 strategy injection into prompts</li> </ul>"},{"location":"concepts/task-session/","title":"Task sessions","text":"<p>A task session is the unit of work inside CraftBot. Every ask \u2014 \"schedule a meeting\", \"find me a flight\", \"review my PR\" \u2014 becomes a task, with its own id, todos, event stream, temp workspace, and triggers pointing at it.</p>"},{"location":"concepts/task-session/#beginner-mental-model","title":"Beginner mental model","text":"<p>A task is a dataclass with three jobs:</p> <ul> <li>Identity \u2014 a unique <code>id</code> that all triggers, events, and streams for this work reference.</li> <li>Progress \u2014 a list of <code>todos</code> (simple checkboxes) so the agent (and you) can see what's done and what's next.</li> <li>Scope \u2014 an <code>action_sets</code> list that limits which actions the agent can call, plus a <code>selected_skills</code> list that injects domain instructions into its prompt.</li> </ul> <p>Tasks sleep and resume. A task can pause waiting for a user reply, resume 3 hours later, and continue where it left off \u2014 the <code>id</code> keeps the stream and todos intact.</p>"},{"location":"concepts/task-session/#inspect-it-now","title":"Inspect it now","text":"<p>Use the <code>/menu</code> command (in TUI or browser) to see active sessions, or tail logs:</p> <pre><code>tail -f logs/*.log | grep -E \"\\[TASK|task_id|session_id\"\n</code></pre>"},{"location":"concepts/task-session/#example-task","title":"Example task","text":"<pre><code>{\n  \"id\": \"4f2c1a9b-...\",\n  \"name\": \"Research Q2 earnings\",\n  \"instruction\": \"Summarize our Q2 earnings, compare to Q1, DM Bob the summary\",\n  \"mode\": \"complex\",\n  \"status\": \"running\",\n  \"todos\": [\n    {\"content\": \"Fetch Q2 earnings report\", \"status\": \"completed\"},\n    {\"content\": \"Fetch Q1 earnings for comparison\", \"status\": \"in_progress\"},\n    {\"content\": \"Draft summary with key deltas\", \"status\": \"pending\"},\n    {\"content\": \"DM Bob on Slack\", \"status\": \"pending\"}\n  ],\n  \"action_sets\": [\"file_operations\", \"web_research\"],\n  \"selected_skills\": [\"financial-analyst\"],\n  \"waiting_for_user_reply\": false,\n  \"source_platform\": \"CraftBot Interface\"\n}\n</code></pre>"},{"location":"concepts/task-session/#lifecycle","title":"Lifecycle","text":"<pre><code>created  \u2192  running  \u21c4  paused     \u2192  completed\n                    \u2198\n                     waiting_for_user_reply\n                    \u2199\n                 error | cancelled\n</code></pre> <p>Status transitions happen through the agent loop. <code>waiting_for_user_reply</code> is special: it keeps the task <code>running</code> but suspends trigger scheduling until the user replies (a fresh trigger with <code>fire_at=now</code> re-wakes it via <code>fire()</code>).</p>"},{"location":"concepts/task-session/#fields","title":"Fields","text":"Field Purpose <code>id</code> Session id \u2014 shared by triggers, events, and streams <code>name</code> / <code>instruction</code> Human-readable label + the original user ask <code>mode</code> <code>\"simple\"</code> or <code>\"complex\"</code> \u2014 drives which mode workflow handles it <code>status</code> <code>running</code> / <code>completed</code> / <code>error</code> / <code>paused</code> / <code>cancelled</code> <code>todos</code> [TodoItem] \u2014 content, status (<code>pending</code> / <code>in_progress</code> / <code>completed</code>), active form, uuid <code>action_sets</code> Whitelist of action set names (<code>file_operations</code>, <code>web_research</code>, etc.) <code>compiled_actions</code> Cached flat list of action names compiled from the sets <code>selected_skills</code> Skills whose instructions inject into the prompt <code>temp_dir</code> Per-task scratch directory (outputs too large for the event stream live here) <code>waiting_for_user_reply</code> Pauses trigger scheduling until <code>fire()</code> <code>source_platform</code> Where outbound messages go \u2014 <code>\"Slack\"</code>, <code>\"Telegram\"</code>, or <code>\"CraftBot Interface\"</code> <code>final_summary</code> Populated at task end; becomes the observable outcome"},{"location":"concepts/task-session/#action-sets-scoping","title":"Action sets (scoping)","text":"<p>Action sets limit what a task can do. Default sets:</p> Set Contains <code>core</code> Always included \u2014 <code>send_message</code>, task management, set management <code>file_operations</code> File I/O \u2014 read, write, search, edit <code>web_research</code> Web search, fetch URLs <code>document_processing</code> PDF and office document handling <code>clipboard</code> Clipboard read/write <code>shell</code> <code>run_shell</code> and Python exec <code>gui_interaction</code> Mouse, keyboard, screen (only if GUI mode is on) <p>Action sets are chosen by the LLM when the task is created (via <code>task_start</code> action). You can also force a set with a directive when starting a task.</p>"},{"location":"concepts/task-session/#multi-tasking","title":"Multi-tasking","text":"<p>Multiple tasks can run concurrently. Each one gets its own event stream via <code>on_stream_create(task_id, temp_dir)</code> \u2014 critical to prevent event leakage between sessions. The trigger queue uses <code>session_id</code> to keep triggers scoped to their owning task.</p>"},{"location":"concepts/task-session/#persistence-recovery","title":"Persistence &amp; recovery","text":"<p>Tasks are persisted to disk on every change via <code>on_task_persist</code> / <code>on_task_remove_persist</code> hooks, so a crash doesn't lose them. On restart, running tasks are rehydrated and their triggers re-queued.</p>"},{"location":"concepts/task-session/#simple-vs-complex","title":"Simple vs. complex","text":"<ul> <li>Simple \u2014 streamlined, no visible todos, runs to completion after delivering a result. Best for \"send X\", \"look up Y\". See Simple task.</li> <li>Complex \u2014 full todo loop with planning. Best for multi-step work. See Complex task.</li> </ul> <p>The LLM picks the mode when creating the task, based on the user instruction and examples from the prompt.</p>"},{"location":"concepts/task-session/#related","title":"Related","text":"<ul> <li>Agent loop \u2014 how tasks are executed</li> <li>Triggers \u2014 how tasks are woken up</li> <li>Event stream \u2014 the per-task narrative</li> <li>Task modes \u2014 simple vs complex vs GUI vs proactive</li> <li>Actions catalogue \u2014 action sets and their contents</li> </ul>"},{"location":"concepts/trigger/","title":"Triggers","text":"<p>A trigger is the unit of input that wakes the agent up \u2014 a small record saying \"react to this, at this time, with this context\". Every reaction \u2014 user messages, schedules, memory processing, proactive heartbeats, waiting-for-reply timeouts \u2014 starts as a trigger on the priority queue.</p>"},{"location":"concepts/trigger/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>The agent sleeps on an async queue.</li> <li>Anything that should cause a reaction \u2014 a typed message, a cron tick, an incoming Slack DM, a scheduled planner \u2014 is wrapped in a <code>Trigger</code> and dropped onto the queue.</li> <li>The queue is a priority heap: the trigger with the nearest <code>fire_at</code> timestamp and highest priority (lowest number) wins.</li> <li>One trigger in \u2192 one iteration of the agent loop out.</li> </ul> <p>Triggers can be scheduled in the future (<code>fire_at</code> later than now). They sleep in the queue until their time comes.</p>"},{"location":"concepts/trigger/#inspect-it-now","title":"Inspect it now","text":"<p>Set the log level to DEBUG and watch the queue:</p> <pre><code>tail -f logs/*.log | grep -E \"TRIGGER|PUT|GET\"\n</code></pre>"},{"location":"concepts/trigger/#example-output","title":"Example output","text":"<pre><code>[PUT] Incoming trigger for session=None (skip_merge=False)\n[TRIGGER QUEUE] BEFORE PUT\n(empty)\n[PUT] No existing session trigger \u2192 pushing normally\n[TRIGGER QUEUE] AFTER PUT\n1. session_id=None | prio=10 | fire_at=1713745328.85 | delta=0.00s\n   desc=what's the weather in Tokyo\n\n[GET] CALLED\n[TRIGGER FIRED] session=None | desc=what's the weather in Tokyo\n</code></pre>"},{"location":"concepts/trigger/#anatomy","title":"Anatomy","text":"<p>A trigger is a small dataclass:</p> Field Type Purpose <code>fire_at</code> float (unix timestamp) When the trigger is eligible to run <code>priority</code> int Ordering within a <code>fire_at</code> tie. Lower = higher priority <code>next_action_description</code> str Human-readable hint used when routing/merging <code>payload</code> dict Arbitrary context carried to the workflow <code>session_id</code> str | None Which task/session this trigger belongs to <code>waiting_for_reply</code> bool True if the trigger is a wait-for-user timeout <p><code>payload[\"type\"]</code> is the single most important field. It tells <code>react()</code> which workflow to run:</p> <code>payload[\"type\"]</code> Workflow <code>\"memory_processing\"</code> Memory distillation <code>\"proactive_heartbeat\"</code> Proactive due-task sweep <code>\"proactive_planner\"</code> Daily / weekly / monthly planner <code>\"task_execution\"</code> / <code>\"scheduled\"</code> / (unset) Regular conversation / task flow"},{"location":"concepts/trigger/#the-queue-put-get-fire","title":"The queue: put / get / fire","text":"<p><code>put(trigger)</code> \u2014 insert a trigger. If another trigger already exists for the same <code>session_id</code>, the newer trigger replaces the older ones (prefer-newest). If no <code>session_id</code> is set, the queue asks the LLM to route the trigger to an existing session or open a new one. System triggers (<code>memory_processing</code>, <code>task_execution</code>, <code>scheduled</code>) skip LLM routing.</p> <p><code>get()</code> \u2014 block until a trigger's <code>fire_at</code> has arrived. Multiple ready triggers for the same session are merged: their descriptions are concatenated (deduped), payloads are merged, and the highest-priority <code>fire_at</code> wins.</p> <p><code>fire(session_id, message=..., platform=...)</code> \u2014 bring an existing trigger's <code>fire_at</code> forward to now. Used when a user replies while their task is waiting. If a message is passed, it's attached to the trigger's payload as <code>pending_user_message</code> and picked up by <code>react()</code> so the LLM sees it.</p>"},{"location":"concepts/trigger/#llm-based-session-routing","title":"LLM-based session routing","text":"<p>When <code>put()</code> receives a trigger with no <code>session_id</code> and there are running tasks, it prompts the LLM with:</p> <ul> <li>a list of running sessions (task name, instruction, todo progress, recent events, platform, conversation id),</li> <li>the recent conversation history (last 10 events),</li> <li>the incoming trigger's description,</li> </ul> <p>and asks: \"does this trigger belong to one of these sessions, or is it new?\" The response is JSON \u2014 either <code>{\"action\": \"route\", \"session_id\": \"...\"}</code> or <code>{\"action\": \"new\"}</code>. The prompt template is <code>ROUTE_TO_SESSION_PROMPT</code> and is configurable.</p>"},{"location":"concepts/trigger/#merging-active-triggers","title":"Merging active triggers","text":"<p>If two triggers land on the same session while one is already being processed, the queue tracks them in the <code>_active</code> dict and the second one's payload gets folded into the first. This is how a user message mid-task gets surfaced to the LLM without a race condition \u2014 <code>pop_pending_user_message(session_id)</code> extracts the queued message when the next iteration runs.</p>"},{"location":"concepts/trigger/#creating-triggers","title":"Creating triggers","text":"<p>Components create triggers by calling <code>self.triggers.put(Trigger(...))</code>:</p> <ul> <li>User message \u2192 priority 10, <code>fire_at=now</code>, session via LLM routing</li> <li>Scheduled task \u2192 priority 50, <code>fire_at=&lt;scheduled time&gt;</code>, <code>payload.type=\"scheduled\"</code></li> <li>Proactive heartbeat \u2192 priority 50, <code>fire_at=&lt;cron tick&gt;</code>, <code>payload.type=\"proactive_heartbeat\"</code></li> <li>Wait-for-reply \u2192 priority 100, <code>fire_at=&lt;now + 3 hours&gt;</code>, <code>waiting_for_reply=True</code></li> <li>External chat event (Slack/Discord/Telegram) \u2192 priority 10, <code>fire_at=now</code>, <code>payload.platform=\"&lt;platform&gt;\"</code>, <code>payload.contact_id</code>, <code>payload.channel_id</code></li> </ul> <p>Custom triggers are how you integrate CraftBot with anything outside the box \u2014 see Custom action and MCP servers.</p>"},{"location":"concepts/trigger/#related","title":"Related","text":"<ul> <li>Agent loop \u2014 the consumer of triggers</li> <li>Event stream \u2014 what runs during a trigger</li> <li>Proactive \u2014 the largest source of scheduled triggers</li> <li>Prompts \u2014 the <code>ROUTE_TO_SESSION_PROMPT</code> template</li> </ul>"},{"location":"configuration/","title":"Configuration","text":"<p>Three surfaces control every CraftBot knob:</p> <ul> <li> <p> config.json</p> <p>Runtime flags: GUI mode, conda usage, memory, proactive, model selection.</p> </li> <li> <p> Agent bundle config.yaml</p> <p>Per-agent bundle: role, data_dir, rag_dir, LLM provider.</p> </li> <li> <p> Environment variables</p> <p>API keys, OAuth secrets, paths. Via <code>.env</code> or OS env.</p> </li> </ul>"},{"location":"configuration/#precedence-highest-to-lowest","title":"Precedence (highest to lowest)","text":"<ol> <li><code>.env</code> file in the project root \u2014 loaded first at startup</li> <li>OS environment variables \u2014 overlay on top of <code>.env</code></li> <li><code>config.json</code> \u2014 runtime flags not covered by env</li> <li>Onboarding wizard answers \u2014 written into <code>config.json</code> and credential store on first run</li> </ol>"},{"location":"configuration/#related","title":"Related","text":"<ul> <li>Onboarding \u2014 first-run wizard that writes much of this for you</li> <li>Credentials \u2014 where tokens and OAuth refresh live</li> </ul>"},{"location":"configuration/agent-config-yaml/","title":"Agent bundle config.yaml","text":"<p>Every custom agent bundle under <code>agents/&lt;name&gt;/</code> includes a <code>config.yaml</code> that overrides per-agent paths and model choices. It's how one repo hosts multiple specialised agents (personal assistant, dog agent, \u2026) with different personalities, RAG docs, and LLM providers.</p>"},{"location":"configuration/agent-config-yaml/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>One YAML file per agent bundle.</li> <li>Loaded by the agent's <code>from_bundle()</code> classmethod at startup.</li> <li>Most fields are optional \u2014 anything unset falls back to the root <code>settings.json</code>.</li> <li>Edit the file, restart the agent, changes take effect.</li> </ul>"},{"location":"configuration/agent-config-yaml/#inspect-it-now","title":"Inspect it now","text":"<pre><code>cat agents/personal_assistant/config.yaml\ncat agents/dog_agent/config.yaml\n</code></pre>"},{"location":"configuration/agent-config-yaml/#example","title":"Example","text":"<pre><code># agents/personal_assistant/config.yaml\ndata_dir: core/data\nrag_dir: rag_docs\nrag_namespace: personal_assistant_knowledge\n\nmodel: gpt-4o\nmax_tokens: 16000\n</code></pre> <pre><code># agents/dog_agent/config.yaml\ndata_dir: agents/dog_agent/data/\nrag_dir: rag_docs\nrag_namespace: dog_agent_knowledge\n\nmax_tokens: 16000\nllm_provider: byteplus\n</code></pre>"},{"location":"configuration/agent-config-yaml/#fields","title":"Fields","text":"Field Default Purpose <code>data_dir</code> <code>agent_file_system/</code> Per-agent file system root \u2014 overrides the shared AGENT_FILE_SYSTEM_PATH <code>rag_dir</code> (none) Subdir of the bundle holding RAG docs. Indexed into memory at startup <code>rag_namespace</code> bundle name Namespace for the ChromaDB collection \u2014 keeps per-agent vectors isolated <code>llm_provider</code> from <code>settings.json</code> \u2192 <code>model.llm_provider</code> Override LLM provider for this agent <code>model</code> from <code>settings.json</code> \u2192 <code>model.llm_model</code> Model name override (e.g. <code>gpt-4o</code>, <code>claude-sonnet-4-6</code>) <code>max_tokens</code> provider default Max tokens per response"},{"location":"configuration/agent-config-yaml/#where-paths-are-resolved","title":"Where paths are resolved","text":"<p>Relative paths resolve against the bundle's own directory, not the project root:</p> In <code>config.yaml</code> Resolves to <code>data_dir: core/data</code> <code>&lt;repo&gt;/core/data</code> (if project-relative override is intended) <code>rag_dir: rag_docs</code> <code>&lt;bundle&gt;/rag_docs/</code> <code>data_dir: agents/dog_agent/data/</code> <code>&lt;repo&gt;/agents/dog_agent/data/</code> (absolute-from-root) <p><code>personal_assistant</code> uses project-root-relative; <code>dog_agent</code> spells out the full path. Both work.</p>"},{"location":"configuration/agent-config-yaml/#rag-docs-layout","title":"RAG docs layout","text":"<p>When <code>rag_dir</code> is set, the bundle ships its own corpus:</p> <pre><code>agents/personal_assistant/\n\u251c\u2500\u2500 agent.py            # The subclass\n\u251c\u2500\u2500 config.yaml         # This file\n\u251c\u2500\u2500 rag_docs/           # Indexed into memory namespace\n\u2502   \u251c\u2500\u2500 calendar_conventions.md\n\u2502   \u251c\u2500\u2500 email_templates.md\n\u2502   \u2514\u2500\u2500 ...\n\u2514\u2500\u2500 rag_docs_taskdocs/  # Optional: task-example docs\n</code></pre> <p>Memory indexes <code>rag_docs/</code> into a separate namespace (<code>rag_namespace</code>) so it doesn't mix with the shared <code>agent_file_system/</code> memory.</p>"},{"location":"configuration/agent-config-yaml/#precedence","title":"Precedence","text":"<pre><code>settings.json (root)\n    \u2193\nconfig.yaml (bundle)        \u2190 overrides provider, model, data paths\n    \u2193\nruntime agent instance      \u2190 uses final values\n</code></pre> <p>Environment variables still apply on top (API keys, OAuth credentials) \u2014 they're read by the provider clients regardless of bundle.</p>"},{"location":"configuration/agent-config-yaml/#loading","title":"Loading","text":"<p>Agents are loaded via their <code>from_bundle()</code> classmethod:</p> <pre><code>from agents.personal_assistant.agent import PersonalAssistantAgent\n\nagent = PersonalAssistantAgent.from_bundle(\n    bundle_path=Path(\"agents/personal_assistant\"),\n)\n</code></pre> <p>The method reads <code>config.yaml</code>, sets up data/rag dirs, instantiates the LLM interface with the right provider, and returns the agent.</p>"},{"location":"configuration/agent-config-yaml/#related","title":"Related","text":"<ul> <li>Custom agent \u2014 full walkthrough on building your own bundle</li> <li>settings.json \u2014 the root-level fallback</li> <li>LLM providers \u2014 valid <code>llm_provider</code> values</li> <li>Memory \u2014 how <code>rag_dir</code> gets indexed</li> </ul>"},{"location":"configuration/config-json/","title":"settings.json","text":"<p>CraftBot's main runtime configuration is <code>app/config/settings.json</code>. It holds the agent name, model provider selection, API keys, feature toggles for memory/proactive/GUI, and endpoint overrides.</p> <p>Two config files</p> <p>There's also a small root-level <code>config.json</code> (59 bytes) with install-time flags (<code>use_conda</code>, <code>gui_mode_enabled</code>). That's for <code>install.py</code> and <code>run.py</code> \u2014 not the runtime config.</p>"},{"location":"configuration/config-json/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>One file, JSON.</li> <li>Caches on first read \u2014 <code>get_settings()</code> caches the file in memory. Call <code>invalidate_settings_cache()</code> or <code>reload_settings()</code> to re-read from disk.</li> <li>Last-write-wins \u2014 <code>save_settings(settings)</code> overwrites the file.</li> <li>Most fields can be edited with the onboarding wizard \u2014 you rarely need to hand-edit unless scripting.</li> </ul>"},{"location":"configuration/config-json/#inspect-it-now","title":"Inspect it now","text":"<pre><code>cat app/config/settings.json\n</code></pre> <p>Or in Python:</p> <pre><code>from app.config import get_settings\nprint(get_settings())\n</code></pre>"},{"location":"configuration/config-json/#full-schema","title":"Full schema","text":"<pre><code>{\n  \"version\": \"0.0.0\",\n  \"general\": {\n    \"agent_name\": \"CraftBot\",\n    \"os_language\": \"en\"\n  },\n  \"proactive\": { \"enabled\": true },\n  \"memory\":    { \"enabled\": true },\n  \"model\": {\n    \"llm_provider\": \"anthropic\",\n    \"vlm_provider\": \"anthropic\",\n    \"llm_model\": null,\n    \"vlm_model\": null,\n    \"slow_mode\": false,\n    \"slow_mode_tpm_limit\": 30000\n  },\n  \"api_keys\": {\n    \"openai\":    \"\",\n    \"anthropic\": \"\",\n    \"google\":    \"\",\n    \"byteplus\":  \"\"\n  },\n  \"endpoints\": {\n    \"remote_model_url\":     \"\",\n    \"byteplus_base_url\":    \"https://ark.ap-southeast.bytepluses.com/api/v3\",\n    \"google_api_base\":      \"\",\n    \"google_api_version\":   \"\"\n  },\n  \"web_search\": {\n    \"google_cse_id\": \"\"\n  },\n  \"gui\": {\n    \"enabled\":        true,\n    \"use_omniparser\": false,\n    \"omniparser_url\": \"http://127.0.0.1:7861\"\n  }\n}\n</code></pre>"},{"location":"configuration/config-json/#top-level-sections","title":"Top-level sections","text":"Section Purpose <code>version</code> App version (auto-written on upgrade) <code>general.agent_name</code> Displayed name in the UI and injected into prompts <code>general.os_language</code> Detected on first launch (<code>en</code>, <code>ja</code>, <code>zh</code>, \u2026) \u2014 used in prompts <code>proactive.enabled</code> Master switch for proactive mode <code>memory.enabled</code> Master switch for memory <code>model.*</code> Which LLM &amp; VLM provider to use, model overrides, slow-mode rate limiting <code>api_keys.*</code> Keys per provider. See Environment variables for env-based alternatives <code>endpoints.*</code> Base URL overrides (Ollama, BytePlus, custom Google API endpoint) <code>web_search.google_cse_id</code> Custom search engine ID for the <code>web_search</code> action <code>gui.*</code> GUI mode toggle and OmniParser endpoint"},{"location":"configuration/config-json/#slow-mode","title":"Slow mode","text":"<p><code>model.slow_mode</code> adds rate limiting at <code>model.slow_mode_tpm_limit</code> tokens per minute. Useful for providers with strict quotas.</p> <pre><code>{ \"model\": { \"slow_mode\": true, \"slow_mode_tpm_limit\": 30000 } }\n</code></pre>"},{"location":"configuration/config-json/#constants-not-in-json","title":"Constants (not in JSON)","text":"<p>Some limits live in <code>app/config.py</code> as Python constants. Change them by editing the file:</p> Constant Default Purpose <code>MAX_ACTIONS_PER_TASK</code> <code>500</code> Cap to prevent runaway tasks <code>MAX_TOKEN_PER_TASK</code> <code>12_000_000</code> Per-task token budget <code>PROCESS_MEMORY_AT_STARTUP</code> <code>False</code> Whether to run memory processing on launch <code>MEMORY_PROCESSING_SCHEDULE_HOUR</code> <code>3</code> Hour of day (0-23) for daily memory distillation"},{"location":"configuration/config-json/#precedence","title":"Precedence","text":"<p>For API keys and OAuth credentials, precedence is:</p> <ol> <li>Embedded credentials (bundled release builds)</li> <li>OS environment variable (e.g. <code>OPENAI_API_KEY</code>)</li> <li><code>settings.json</code> \u2192 <code>api_keys</code></li> <li>Empty string (no provider available)</li> </ol> <p>See Environment variables for the env-based approach and Credentials for OAuth handling.</p>"},{"location":"configuration/config-json/#hot-reload","title":"Hot reload","text":"<p>Changes to <code>settings.json</code> are picked up within seconds by the config watcher (<code>agent_core.config_watcher</code>). You do not need to restart the agent for most changes \u2014 though switching LLM provider mid-task can cause odd behaviour.</p> <p>Editing safely</p> <p>Use the <code>/menu</code> command or onboarding re-run instead of hand-editing. The wizard validates fields and writes atomically.</p>"},{"location":"configuration/config-json/#related","title":"Related","text":"<ul> <li>Environment variables \u2014 API keys, OAuth secrets via env</li> <li>LLM providers \u2014 provider names and defaults</li> <li>Agent bundle config.yaml \u2014 per-agent bundle overrides</li> <li>Onboarding \u2014 wizard that writes this file</li> </ul>"},{"location":"configuration/env-vars/","title":"Environment variables","text":"<p>CraftBot reads environment variables for LLM API keys, optional endpoint URLs, and OAuth client credentials. Env vars are the highest-precedence override after embedded credentials \u2014 they win over <code>settings.json</code> entries of the same name.</p>"},{"location":"configuration/env-vars/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>Drop an <code>.env</code> file at the project root (copy from <code>.env.example</code>) \u2014 CraftBot loads it at startup.</li> <li>Or set them as real OS environment variables \u2014 equivalent effect.</li> <li>Anything set to an empty string is treated as unset and falls back to <code>settings.json</code> or embedded credentials.</li> </ul>"},{"location":"configuration/env-vars/#inspect-it-now","title":"Inspect it now","text":"<pre><code>cat .env.example\n</code></pre> <p>Or view what the app will actually use:</p> <pre><code>from app.config import get_api_key\nprint(get_api_key(\"anthropic\"))\n</code></pre>"},{"location":"configuration/env-vars/#llm-provider-keys","title":"LLM provider keys","text":"<p>Set at least one:</p> Env var Provider <code>OPENAI_API_KEY</code> OpenAI \u2014 see LLM providers <code>GOOGLE_API_KEY</code> Google Gemini \u2014 see LLM providers <code>ANTHROPIC_API_KEY</code> Anthropic Claude \u2014 see LLM providers <code>BYTEPLUS_API_KEY</code> BytePlus (Volc Engine) \u2014 see LLM providers <code>REMOTE_MODEL_URL</code> Ollama server URL (e.g. <code>http://192.168.1.10:11434</code>)"},{"location":"configuration/env-vars/#gui-vision","title":"GUI / Vision","text":"Env var Purpose <code>OMNIPARSER_BASE_URL</code> Gradio URL for OmniParser. Leave unset for <code>http://localhost:7861</code>"},{"location":"configuration/env-vars/#oauth-client-credentials","title":"OAuth client credentials","text":"<p>Embedded credentials ship with release builds. Set these only if you're running your own OAuth apps or the embedded client expired.</p> Env var Used by <code>GOOGLE_CLIENT_ID</code> / <code>GOOGLE_CLIENT_SECRET</code> Google Workspace (PKCE \u2014 only client_id required) <code>LINKEDIN_CLIENT_ID</code> / <code>LINKEDIN_CLIENT_SECRET</code> LinkedIn <code>OUTLOOK_CLIENT_ID</code> Outlook (PKCE \u2014 only client_id) <code>SLACK_SHARED_CLIENT_ID</code> / <code>SLACK_SHARED_CLIENT_SECRET</code> Slack <code>NOTION_SHARED_CLIENT_ID</code> / <code>NOTION_SHARED_CLIENT_SECRET</code> Notion <code>TELEGRAM_SHARED_BOT_TOKEN</code> / <code>TELEGRAM_SHARED_BOT_USERNAME</code> Telegram (Bot) <code>TELEGRAM_API_ID</code> / <code>TELEGRAM_API_HASH</code> Telegram (User)"},{"location":"configuration/env-vars/#env-file-layout","title":"<code>.env</code> file layout","text":"<p>CraftBot reads the root-level <code>.env</code> (copy from <code>.env.example</code>):</p> <pre><code># Copy the template\ncp .env.example .env\n# Edit it\n$EDITOR .env\n</code></pre> <p>Each line is <code>KEY=value</code>. Blank lines and <code>#</code> comments are allowed. Quote values that contain spaces or special chars:</p> <pre><code>ANTHROPIC_API_KEY=\"sk-ant-api03-...\"\nGOOGLE_CLIENT_ID=\"123456789.apps.googleusercontent.com\"\n</code></pre>"},{"location":"configuration/env-vars/#precedence","title":"Precedence","text":"<p>For any piece of configuration that has multiple sources, CraftBot checks in order:</p> <ol> <li>Embedded credentials (only for OAuth clients, bundled with release builds)</li> <li>OS environment variable (including <code>.env</code> file)</li> <li><code>settings.json</code> \u2192 <code>api_keys</code> / <code>endpoints</code></li> <li>Empty / unset \u2192 feature disabled</li> </ol>"},{"location":"configuration/env-vars/#where-env-vars-are-read","title":"Where env vars are read","text":"<ul> <li>LLM keys via <code>app/config.py</code> \u2014 checks env first, then settings.</li> <li>OAuth creds via <code>app/config.py</code> \u2014 checks embedded, then env, then returns empty.</li> <li>Ollama URL via <code>app/config.py</code> \u2014 falls back to <code>http://localhost:11434</code>.</li> </ul>"},{"location":"configuration/env-vars/#env-vs-settingsjson-which-to-use","title":"<code>.env</code> vs settings.json \u2014 which to use?","text":"Use env vars for Use settings.json for Secrets (API keys, tokens) User-facing preferences Things you don't want committed to git Model/provider selection CI / deployment Integration-specific options Per-user local overrides Things the onboarding wizard writes <p>Both are git-ignored by default.</p>"},{"location":"configuration/env-vars/#related","title":"Related","text":"<ul> <li><code>settings.json</code> \u2014 the JSON-based counterpart</li> <li>Credentials \u2014 OAuth flow specifics</li> <li>LLM providers \u2014 per-provider key requirements</li> </ul>"},{"location":"connections/","title":"Connections","text":"<p>CraftBot connects to external services in two ways:</p> <ul> <li>Invite (easy) \u2014 add the CraftOS-hosted bot to your workspace. No keys.</li> <li>Login (advanced) \u2014 bring your own bot token, API key, or OAuth credentials.</li> </ul> <p>Run <code>/cred status</code> to see every active connection at a glance.</p>"},{"location":"connections/#start-with","title":"Start with","text":"<ul> <li> <p> Credentials</p> <p>How CraftBot stores tokens, OAuth refresh, and env-var fallbacks.</p> </li> <li> <p> MCP servers</p> <p>Plug Model Context Protocol tools into CraftBot as native actions.</p> </li> </ul>"},{"location":"connections/#chat-messaging","title":"Chat &amp; messaging","text":"<ul> <li>Discord</li> <li>Slack</li> <li>Telegram (Bot)</li> <li>Telegram (User)</li> <li>WhatsApp Web</li> <li>WhatsApp Business</li> </ul>"},{"location":"connections/#productivity-collaboration","title":"Productivity &amp; collaboration","text":"<ul> <li>Google Workspace</li> <li>Outlook</li> <li>Notion</li> <li>LinkedIn</li> <li>GitHub</li> <li>Jira</li> <li>Twitter</li> </ul>"},{"location":"connections/#related","title":"Related","text":"<ul> <li>Environment variables \u2014 every OAuth variable across integrations</li> <li>Develop :: Custom agent \u2014 bundle integrations into a subclassed agent</li> </ul>"},{"location":"connections/credentials/","title":"Credentials","text":"<p>All connection tokens, OAuth refresh tokens, API keys, and bot secrets live in a single credential store managed by CraftBot. This page explains where credentials live, how they're loaded, and the precedence across the four possible sources.</p>"},{"location":"connections/credentials/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>Embedded credentials \u2014 bundled with release builds. Let users connect Google, Slack, Notion, LinkedIn, and others without creating their own OAuth apps.</li> <li>Environment variables \u2014 override embedded for self-hosted setups. Highest priority after embedded.</li> <li>Connection commands \u2014 <code>/google login</code>, <code>/slack invite</code>, etc. trigger OAuth or ask for a token. Results are stored in the credential store.</li> <li>Credential store \u2014 JSON files on disk, one per platform. Managed by <code>agent_core</code> credential helpers.</li> </ul>"},{"location":"connections/credentials/#inspect-it-now","title":"Inspect it now","text":"<pre><code>/cred status       # list active connections\n/cred integrations # group by platform\n</code></pre>"},{"location":"connections/credentials/#precedence","title":"Precedence","text":"<p>For OAuth client credentials (the app, not the user token):</p> <ol> <li>Embedded (release builds) \u2014 shipped with CraftBot</li> <li>Environment variable \u2014 <code>GOOGLE_CLIENT_ID</code>, <code>SLACK_SHARED_CLIENT_ID</code>, etc. See Environment variables</li> <li><code>settings.json</code> \u2192 <code>api_keys.*</code></li> <li>(nothing) \u2014 connection unavailable</li> </ol> <p>For user tokens (your Gmail access token, Slack bot token):</p> <ol> <li>Credential store (<code>app/credentials/</code> plus OS keychain where available)</li> <li>(nothing) \u2014 you need to run <code>/xxx login</code> again</li> </ol>"},{"location":"connections/credentials/#storage","title":"Storage","text":"<p>Credentials are stored locally in two places:</p> <ul> <li>Token files \u2014 <code>app/credentials/&lt;platform&gt;/&lt;user_id&gt;.json</code> (OAuth refresh tokens, bot tokens, session strings)</li> <li>OS keychain (when available) \u2014 for secrets the platform deems high-risk (e.g. WhatsApp session cookies)</li> </ul> <p>Nothing is synced to the cloud unless you explicitly configure it.</p>"},{"location":"connections/credentials/#the-oauth-flow","title":"The OAuth flow","text":"<p>Most integrations use OAuth 2.0 with PKCE:</p> <ol> <li>You type <code>/google login</code> (or click a button in the UI).</li> <li>CraftBot generates a PKCE <code>code_verifier</code> and <code>code_challenge</code>.</li> <li>Your browser opens to the provider's auth page.</li> <li>You approve.</li> <li>Provider redirects to <code>http://localhost:8765</code> with an auth code.</li> <li>CraftBot's local HTTP listener grabs the code and exchanges it for tokens.</li> <li>Tokens are saved to the credential store.</li> </ol> <p>Redirect URI is hardcoded to <code>http://localhost:8765</code> (HTTPS variant <code>https://localhost:8765</code> is used when the provider requires it, e.g. Slack).</p>"},{"location":"connections/credentials/#token-types-per-integration","title":"Token types per integration","text":"Integration Auth Client secret needed? Google Workspace PKCE No Outlook PKCE No Slack OAuth 2.0 Yes Notion OAuth 2.0 Yes LinkedIn OAuth 2.0 Yes Discord Bot token or OAuth invite No (token) / Yes (invite) Telegram (Bot) Bot token from @BotFather No Telegram (User) MTProto API (api_id + api_hash) \u2014 WhatsApp Web QR scan via Playwright \u2014 WhatsApp Business Meta API token \u2014 GitHub Personal access token \u2014 Jira API token + workspace URL \u2014 Twitter API key + secret (v2) \u2014"},{"location":"connections/credentials/#managing-credentials","title":"Managing credentials","text":"Command Effect <code>/&lt;platform&gt; login [args]</code> Start auth flow or set a token <code>/&lt;platform&gt; logout [id]</code> Remove a stored credential <code>/&lt;platform&gt; status</code> Check whether the integration is connected <code>/&lt;platform&gt; invite</code> Use CraftOS-hosted app (where supported) <code>/cred status</code> List every integration + state <code>/cred &lt;platform&gt; connect</code> Alias for <code>/xxx login</code> from the universal credentials panel"},{"location":"connections/credentials/#rotating-credentials","title":"Rotating credentials","text":"<ul> <li>OAuth tokens refresh automatically via the stored refresh token. Rotate by running <code>/xxx logout</code> then <code>/xxx login</code>.</li> <li>Bot tokens (Discord, Telegram Bot): revoke the old token in the provider's dashboard, generate a new one, run <code>/xxx login &lt;new_token&gt;</code>.</li> <li>API keys (GitHub, Jira, Twitter, Recall.ai): re-run <code>login</code> with the new key.</li> </ul>"},{"location":"connections/credentials/#security-considerations","title":"Security considerations","text":"<ul> <li><code>app/credentials/</code> is gitignored \u2014 never commit it.</li> <li>Tokens are masked in logs \u2014 if a secret leaks into a log, that's a bug; please report.</li> <li>Use env vars (or the keychain path) instead of <code>api_keys</code> in <code>settings.json</code> for better security posture.</li> </ul>"},{"location":"connections/credentials/#related","title":"Related","text":"<ul> <li>Environment variables \u2014 the env-based source</li> <li><code>settings.json</code> \u2014 the <code>api_keys</code> source</li> <li>Connections overview</li> </ul>"},{"location":"connections/discord/","title":"Discord","text":"<p>Send messages to channels, DM users, read messages, list guilds and channels, add reactions.</p>"},{"location":"connections/discord/#available-actions","title":"Available actions","text":"<ul> <li><code>send_discord_message</code> \u2014 post to a channel</li> <li><code>send_discord_dm</code> \u2014 direct message a user</li> <li><code>get_discord_messages</code> \u2014 read messages from a channel</li> <li><code>list_discord_guilds</code> \u2014 list your servers</li> <li><code>get_discord_channels</code> \u2014 list channels in a guild</li> <li><code>add_discord_reaction</code> \u2014 react to a message</li> </ul>"},{"location":"connections/discord/#connect","title":"Connect","text":"Command What it does <code>/discord invite</code> Add the CraftOS bot to your server (opens browser) <code>/discord invite &lt;guild_id&gt; [name]</code> Register a guild after adding the bot <code>/discord login &lt;bot_token&gt;</code> Connect your own Discord bot <code>/discord login-user &lt;user_token&gt;</code> Connect a Discord user account <code>/discord status</code> Show all Discord connections <code>/discord logout [id]</code> Remove a connection"},{"location":"connections/discord/#prerequisites","title":"Prerequisites","text":"Invite (easy)Login (your own bot) <p>Requires <code>DISCORD_SHARED_BOT_ID</code> env var. Release builds have it embedded.</p> <ol> <li>Go to discord.com/developers</li> <li>Create a new application</li> <li>In Bot tab, create a bot, copy the token</li> <li>Enable \"Message Content Intent\" under Privileged Gateway Intents</li> <li>Run <code>/discord login &lt;bot_token&gt;</code></li> </ol>"},{"location":"connections/discord/#troubleshooting","title":"Troubleshooting","text":"<p>Bot sends messages but can't read replies \u2014 enable \"Message Content Intent\" in the Discord developer portal.</p> <p>\"Missing Access\" errors \u2014 the bot needs to be invited to the server AND have read/write perms in the channel.</p>"},{"location":"connections/discord/#related","title":"Related","text":"<ul> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"connections/github/","title":"GitHub","text":"<p>Interact with GitHub repositories \u2014 issues, pull requests, files, and workflows.</p>"},{"location":"connections/github/#available-actions","title":"Available actions","text":"<p>Typical GitHub operations (exact names may vary \u2014 see Actions catalogue):</p> <ul> <li>List / read / create / close / comment on issues</li> <li>List / read / create / review / merge pull requests</li> <li>Read / write files in a branch</li> <li>Search code / issues / commits</li> <li>Trigger / view workflow runs</li> </ul>"},{"location":"connections/github/#connect","title":"Connect","text":"Command What it does <code>/github login &lt;personal_access_token&gt;</code> Connect with a PAT <code>/github status</code> Show connected accounts <code>/github logout</code> Remove credentials"},{"location":"connections/github/#prerequisites","title":"Prerequisites","text":"<ol> <li>github.com/settings/tokens \u2192 Generate new token (classic or fine-grained)</li> <li>For classic: scopes <code>repo</code>, <code>workflow</code>, <code>read:org</code>, <code>read:user</code></li> <li>For fine-grained: select the repos you want the agent to access, enable Contents R/W, Issues R/W, PRs R/W, Actions read</li> <li>Run <code>/github login &lt;token&gt;</code></li> </ol>"},{"location":"connections/github/#token-tips","title":"Token tips","text":"<ul> <li>Never commit a PAT. Use <code>.env</code> or the credential store.</li> <li>Fine-grained tokens are strongly preferred \u2014 they scope to specific repos and expire.</li> <li>For org access, the org must allow fine-grained tokens (Settings \u2192 Developer settings).</li> </ul>"},{"location":"connections/github/#troubleshooting","title":"Troubleshooting","text":"<p>\"Bad credentials\" \u2014 token is expired or revoked. Regenerate on GitHub and <code>/github login &lt;new&gt;</code>.</p> <p>\"Resource not accessible by integration\" \u2014 fine-grained token lacks the needed permission. Regenerate with the right scope.</p> <p>Rate limited \u2014 GitHub allows 5000 authenticated requests/hour per user. High-volume tasks may hit the limit; back off or batch.</p>"},{"location":"connections/github/#related","title":"Related","text":"<ul> <li>Jira \u2014 issue tracker alternative</li> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"connections/google-workspace/","title":"Google Workspace","text":"<p>Gmail, Google Calendar, Google Drive, Google Meet, Google Contacts \u2014 one OAuth flow connects all of them.</p>"},{"location":"connections/google-workspace/#available-actions","title":"Available actions","text":"<p>Gmail:</p> <ul> <li><code>send_gmail</code> \u2014 compose + send</li> <li><code>list_gmail</code> \u2014 list messages by query</li> <li><code>get_gmail</code> \u2014 read one message</li> <li><code>read_top_emails</code> \u2014 inbox summary</li> </ul> <p>Calendar:</p> <ul> <li><code>create_google_meet</code> \u2014 event with Meet link</li> <li><code>check_calendar_availability</code> \u2014 busy/free slots</li> </ul> <p>Drive:</p> <ul> <li><code>list_drive_files</code> \u2014 list files/folders</li> <li><code>create_drive_folder</code> \u2014 create a folder</li> <li><code>move_drive_file</code> \u2014 move a file or folder</li> </ul>"},{"location":"connections/google-workspace/#connect","title":"Connect","text":"Command What it does <code>/google login</code> Authenticate via Google OAuth (opens browser) <code>/google status</code> Show connected accounts <code>/google logout [email]</code> Remove an account"},{"location":"connections/google-workspace/#prerequisites","title":"Prerequisites","text":"<p>Uses PKCE \u2014 only a Client ID is required (no secret).</p> Embedded (easy)Your own OAuth app <p>Release builds include a CraftOS Client ID. Just run <code>/google login</code>.</p> <ol> <li>Google Cloud Console \u2192 Credentials</li> <li>\"Create Credentials\" \u2192 OAuth client ID \u2192 Desktop application type</li> <li>Copy the Client ID</li> <li>Enable Gmail, Calendar, Drive, People APIs</li> <li>Set <code>GOOGLE_CLIENT_ID</code> in <code>.env</code> (skip the secret \u2014 PKCE doesn't need it)</li> <li>Run <code>/google login</code></li> </ol> <p>Scopes requested: Gmail modify, Calendar, Drive, Contacts (read-only), UserInfo. Review the consent screen carefully.</p>"},{"location":"connections/google-workspace/#troubleshooting","title":"Troubleshooting","text":"<p>\"access_denied\" on consent screen \u2014 your Google Workspace admin may have restricted third-party apps. Ask them to allow the CraftOS Client ID, or create your own under your project.</p> <p>\"invalid_grant\" on refresh \u2014 your refresh token was revoked. Run <code>/google logout &lt;email&gt;</code> then <code>/google login</code>.</p> <p>Emails not sending \u2014 check the <code>send_gmail</code> action's <code>to</code> field formatting; <code>\"Bob &lt;bob@example.com&gt;\"</code> vs bare addresses both work.</p>"},{"location":"connections/google-workspace/#related","title":"Related","text":"<ul> <li>Outlook \u2014 Microsoft's equivalent</li> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"connections/jira/","title":"Jira","text":"<p>Interact with Jira Cloud \u2014 issues, projects, boards, sprints, transitions.</p>"},{"location":"connections/jira/#available-actions","title":"Available actions","text":"<ul> <li>List / search issues (JQL supported)</li> <li>Get issue details (description, comments, links)</li> <li>Create issue</li> <li>Transition issue (To Do \u2192 In Progress \u2192 Done)</li> <li>Add comment</li> <li>Assign issue</li> </ul> <p>See Actions catalogue for exact names and schemas.</p>"},{"location":"connections/jira/#connect","title":"Connect","text":"Command What it does <code>/jira login &lt;email&gt; &lt;api_token&gt; &lt;workspace_url&gt;</code> Connect with API token <code>/jira status</code> Show connected workspaces <code>/jira logout [workspace]</code> Remove workspace"},{"location":"connections/jira/#prerequisites","title":"Prerequisites","text":"<ol> <li>id.atlassian.com/manage-profile/security/api-tokens \u2192 Create API token</li> <li>Note your Jira URL (<code>https://&lt;your-workspace&gt;.atlassian.net</code>)</li> <li>Run <code>/jira login &lt;your-email&gt; &lt;api-token&gt; &lt;your-workspace&gt;.atlassian.net</code></li> </ol>"},{"location":"connections/jira/#jql-quick-reference","title":"JQL quick reference","text":"<p>Most search actions accept JQL:</p> <pre><code>project = PROJ AND status = \"In Progress\" AND assignee = currentUser()\n</code></pre> <p>The agent can also be given natural-language queries \u2014 it'll translate them to JQL internally via the action's description.</p>"},{"location":"connections/jira/#troubleshooting","title":"Troubleshooting","text":"<p>401 Unauthorized \u2014 the API token is for your own user; it can't impersonate. Make sure the email matches the account that generated the token.</p> <p>\"Issue does not exist\" on a known issue \u2014 the user token may not have \"Browse projects\" permission for that project. Ask a project admin.</p>"},{"location":"connections/jira/#related","title":"Related","text":"<ul> <li>GitHub \u2014 dev workflow alternative</li> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"connections/linkedin/","title":"LinkedIn","text":"<p>View profile, post updates, search jobs, fetch connections, send messages.</p>"},{"location":"connections/linkedin/#available-actions","title":"Available actions","text":"<ul> <li><code>get_linkedin_profile</code> \u2014 your profile + connections count</li> <li><code>create_linkedin_post</code> \u2014 publish a text / image post</li> <li><code>search_linkedin_jobs</code> \u2014 search postings</li> <li><code>get_linkedin_connections</code> \u2014 list your connections</li> <li><code>send_linkedin_message</code> \u2014 DM a connection</li> <li><code>send_message_to_recipients</code> \u2014 message multiple people</li> <li><code>respond_to_invitation</code> \u2014 accept/ignore connection requests</li> </ul>"},{"location":"connections/linkedin/#connect","title":"Connect","text":"Command What it does <code>/linkedin login</code> OAuth via browser <code>/linkedin status</code> Show connected accounts <code>/linkedin logout [linkedin_id]</code> Remove account"},{"location":"connections/linkedin/#prerequisites","title":"Prerequisites","text":"<p>OAuth 2.0 with client secret (no PKCE).</p> <p>Set <code>LINKEDIN_CLIENT_ID</code> and <code>LINKEDIN_CLIENT_SECRET</code> in <code>.env</code>, or use embedded.</p> <p>Your own OAuth app:</p> <ol> <li>linkedin.com/developers</li> <li>Create an app associated with a company page</li> <li>Under \"Auth\" add the redirect URL <code>http://localhost:8765</code></li> <li>Request the scopes: <code>openid</code>, <code>profile</code>, <code>email</code>, <code>w_member_social</code>, <code>r_basicprofile</code></li> <li>Copy Client ID + Client Secret to <code>.env</code></li> </ol>"},{"location":"connections/linkedin/#troubleshooting","title":"Troubleshooting","text":"<p>\"unauthorized_scope_error\" \u2014 LinkedIn has tightened which apps can request <code>r_basicprofile</code> and similar scopes. You may need to apply for LinkedIn Marketing Developer Platform access.</p> <p>Post fails with \"TEXT_TOO_LONG\" \u2014 LinkedIn limits posts to 3000 chars. Trim before calling <code>create_linkedin_post</code>.</p>"},{"location":"connections/linkedin/#related","title":"Related","text":"<ul> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"connections/mcp/","title":"MCP servers","text":"<p>The Model Context Protocol is an open standard for exposing tools (actions) from external processes. Plug an MCP server into CraftBot, and every tool it exposes becomes a native action the agent can call \u2014 with the same router, same prompts, same events.</p>"},{"location":"connections/mcp/#the-core-idea","title":"The core idea","text":"<ul> <li>Any program that speaks MCP over stdio, WebSocket, or SSE can be a tool provider.</li> <li>CraftBot's MCP adapter (<code>MCPActionAdapter</code>) wraps each MCP tool as a CraftBot action at load time.</li> <li>Tools appear in the registry alongside built-ins \u2014 the router treats them identically.</li> </ul>"},{"location":"connections/mcp/#scenarios","title":"Scenarios","text":""},{"location":"connections/mcp/#1-local-filesystem-sqlite-shell-mcp","title":"1) Local filesystem / SQLite / shell MCP","text":"<p>Use community-built MCPs for filesystem access, database queries, shell execution. Configure in <code>mcp_config.json</code>, start the agent, the tools become available.</p>"},{"location":"connections/mcp/#2-remote-saas-via-mcp-bridge","title":"2) Remote SaaS via MCP bridge","text":"<p>Some SaaS providers expose MCP endpoints (e.g. Sentry, Linear). Configure with <code>transport: \"sse\"</code> or <code>transport: \"websocket\"</code> pointing at their endpoint.</p>"},{"location":"connections/mcp/#3-your-own-mcp-server","title":"3) Your own MCP server","text":"<p>Write a small program that implements the MCP spec; plug it in. Useful for exposing internal APIs or proprietary data to the agent without touching CraftBot's code.</p>"},{"location":"connections/mcp/#command-flow","title":"Command flow","text":"<pre><code>                \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                \u2502  app/config/mcp_config.json \u2502  (hot-reloadable)\n                \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                           \u2193\n                \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                \u2502  MCPClient per server \u2502\n                \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                           \u2193 connects via stdio/ws/sse\n                \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                \u2502  MCPActionAdapter     \u2502  wraps tools as actions\n                \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                           \u2193\n                \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                \u2502  Action registry      \u2502\n                \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                           \u2193\n                Agent router picks + executes\n</code></pre>"},{"location":"connections/mcp/#configuration","title":"Configuration","text":"<p><code>app/config/mcp_config.json</code>:</p> <pre><code>{\n  \"mcp_servers\": [\n    {\n      \"name\": \"filesystem\",\n      \"description\": \"Read/write files on the local machine\",\n      \"transport\": \"stdio\",\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@modelcontextprotocol/server-filesystem\", \"/home/user\"],\n      \"env\": {},\n      \"enabled\": true\n    },\n    {\n      \"name\": \"sentry\",\n      \"description\": \"Query Sentry issues\",\n      \"transport\": \"sse\",\n      \"url\": \"https://sentry.example.com/mcp/sse\",\n      \"headers\": { \"Authorization\": \"Bearer &lt;token&gt;\" },\n      \"enabled\": true\n    }\n  ]\n}\n</code></pre> <p>Fields:</p> Field Values <code>name</code> Unique identifier. Becomes the tool prefix. <code>description</code> Shown in <code>/mcp list</code> <code>transport</code> <code>stdio</code> / <code>websocket</code> / <code>sse</code> <code>command</code> + <code>args</code> + <code>env</code> For <code>stdio</code> transport \u2014 how to launch the server <code>url</code> + <code>headers</code> For <code>websocket</code> / <code>sse</code> transports <code>enabled</code> Master switch per server <p>Hot-reloaded \u2014 no restart needed after editing.</p>"},{"location":"connections/mcp/#managing-servers-via-command","title":"Managing servers via command","text":"<pre><code>/mcp list                    # show configured servers\n/mcp add                     # interactive add\n/mcp remove &lt;name&gt;           # remove\n/mcp enable &lt;name&gt;           # toggle on\n/mcp disable &lt;name&gt;          # toggle off\n</code></pre>"},{"location":"connections/mcp/#tool-naming","title":"Tool naming","text":"<p>MCP tools are prefixed with the server name to avoid collisions:</p> <pre><code>filesystem:read_file\nfilesystem:write_file\nsentry:list_issues\n</code></pre> <p>The router sees them as normal action names.</p>"},{"location":"connections/mcp/#credential-precedence-for-mcp","title":"Credential precedence for MCP","text":"<p>Same as other credentials:</p> <ol> <li>Environment variables referenced in <code>env: {...}</code> (stdio) or <code>headers: {...}</code> (SSE/WS).</li> <li>Literal strings in <code>mcp_config.json</code> (not recommended for secrets).</li> <li>(nothing) \u2014 tool calls fail at runtime.</li> </ol> <p>Prefer <code>env: { \"API_TOKEN\": \"\" }</code> and set <code>API_TOKEN</code> in <code>.env</code> \u2014 keeps secrets out of config.</p>"},{"location":"connections/mcp/#security-rules","title":"Security rules","text":"<ul> <li>Stdio transport runs the command with the user's privileges. Audit the server binary before enabling.</li> <li>Remote transports (WebSocket, SSE) may send your prompts and data to the remote. Review what the server does before trusting it.</li> <li>Disabled servers don't connect \u2014 use <code>enabled: false</code> to pin a config without running the server.</li> <li>Keep the config file out of public repos if it references private endpoints.</li> </ul>"},{"location":"connections/mcp/#adding-mcp-servers-from-the-agent","title":"Adding MCP servers from the agent","text":"<p>Because <code>mcp_config.json</code> is hot-reloaded, the agent itself can add MCP servers when it hits a capability gap. From <code>AGENT.md</code>:</p> <p>When you CANNOT complete a task due to missing capabilities: 1. IDENTIFY \u2014 What capability is missing? 2. SEARCH \u2014 <code>web_search</code> for MCP servers 3. INSTALL \u2014 edit <code>mcp_config.json</code> 4. WAIT \u2014 hot-reload detects the change 5. CONTINUE \u2014 proceed with the new tool</p> <p>Approve these edits before they run if the server requires credentials.</p>"},{"location":"connections/mcp/#related","title":"Related","text":"<ul> <li>Actions \u2014 how MCP tools become actions</li> <li>Skill &amp; action selection \u2014 how they get picked</li> <li>Credentials \u2014 how auth is handled</li> <li>Custom action \u2014 the native alternative</li> </ul>"},{"location":"connections/notion/","title":"Notion","text":"<p>Search pages and databases, create pages, query databases, update page properties.</p>"},{"location":"connections/notion/#available-actions","title":"Available actions","text":"<ul> <li><code>search_notion</code> \u2014 full-text search across your workspace</li> <li><code>get_notion_page</code> \u2014 read a page with blocks</li> <li><code>create_notion_page</code> \u2014 create in a parent page or database</li> <li><code>query_notion_database</code> \u2014 filter a database with Notion's query syntax</li> <li><code>update_notion_page</code> \u2014 update properties or add blocks</li> </ul>"},{"location":"connections/notion/#connect","title":"Connect","text":"Command What it does <code>/notion invite</code> Authorize the CraftOS integration (OAuth) <code>/notion login &lt;integration_token&gt;</code> Use your own Notion integration <code>/notion status</code> Show connected workspaces <code>/notion logout [workspace_id]</code> Remove a workspace"},{"location":"connections/notion/#prerequisites","title":"Prerequisites","text":"Invite (easy)Login (your own integration) <p>Requires <code>NOTION_SHARED_CLIENT_ID</code> and <code>NOTION_SHARED_CLIENT_SECRET</code>. Release builds have these embedded.</p> <ol> <li>Go to notion.so/my-integrations</li> <li>\"New integration\" \u2192 internal or public</li> <li>Copy the Integration Secret</li> <li>Share specific pages with the integration in Notion (integrations don't see pages by default)</li> <li>Run <code>/notion login &lt;secret&gt;</code></li> </ol>"},{"location":"connections/notion/#troubleshooting","title":"Troubleshooting","text":"<p>\"object_not_found\" \u2014 the integration doesn't have access to that page. In Notion, open the page \u2192 \"Add connections\" \u2192 select your integration.</p> <p>Database queries return empty \u2014 check the filter syntax. Notion uses its own filter object format (not SQL). See the Notion API docs.</p>"},{"location":"connections/notion/#related","title":"Related","text":"<ul> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"connections/outlook/","title":"Outlook","text":"<p>Microsoft 365 / Outlook via the Microsoft Graph API.</p>"},{"location":"connections/outlook/#available-actions","title":"Available actions","text":"<ul> <li>Send, read, search emails</li> <li>List calendar events, create / update events</li> <li>List / upload / download OneDrive files</li> <li>Access contacts</li> </ul> <p>(Exact action names match Gmail/Drive counterparts with <code>outlook_</code> / <code>graph_</code> prefix \u2014 see Actions catalogue.)</p>"},{"location":"connections/outlook/#connect","title":"Connect","text":"Command What it does <code>/outlook login</code> OAuth via browser <code>/outlook status</code> Show connected accounts <code>/outlook logout [email]</code> Remove an account"},{"location":"connections/outlook/#prerequisites","title":"Prerequisites","text":"<p>Uses PKCE \u2014 only a Client ID needed.</p> Embedded (easy)Your own OAuth app <p>Release builds include a CraftOS Client ID.</p> <ol> <li>Azure Portal \u2192 App registrations</li> <li>\"New registration\" \u2014 single tenant or multi-tenant (for personal + work accounts)</li> <li>Authentication \u2192 add platform \u2192 Mobile/desktop \u2192 redirect URI <code>http://localhost:8765</code></li> <li>API permissions \u2192 add Microsoft Graph scopes: <code>Mail.ReadWrite</code>, <code>Mail.Send</code>, <code>Calendars.ReadWrite</code>, <code>Files.ReadWrite</code>, <code>User.Read</code></li> <li>Overview \u2192 copy Application (client) ID</li> <li>Set <code>OUTLOOK_CLIENT_ID</code> in <code>.env</code></li> <li>Run <code>/outlook login</code></li> </ol>"},{"location":"connections/outlook/#troubleshooting","title":"Troubleshooting","text":"<p>\"AADSTS9002313\" \u2014 you registered the app as single-tenant but are signing in with a personal account. Switch the app to multi-tenant or register a second Client ID.</p> <p>Token refresh fails \u2014 Microsoft tokens have short lives; refresh happens automatically. If it keeps failing, try <code>/outlook logout</code> then <code>/outlook login</code>.</p>"},{"location":"connections/outlook/#related","title":"Related","text":"<ul> <li>Google Workspace \u2014 Google's equivalent</li> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"connections/slack/","title":"Slack","text":"<p>Post messages, list channels and users, search messages, read history, upload files.</p>"},{"location":"connections/slack/#available-actions","title":"Available actions","text":"<ul> <li><code>send_slack_message</code> \u2014 post to a channel or DM</li> <li><code>list_slack_channels</code> \u2014 list channels in the workspace</li> <li><code>get_slack_channel_history</code> \u2014 read recent messages</li> <li><code>list_slack_users</code> \u2014 list workspace members</li> <li><code>search_slack_messages</code> \u2014 search across channels</li> <li><code>upload_slack_file</code> \u2014 post a file</li> </ul>"},{"location":"connections/slack/#connect","title":"Connect","text":"Command What it does <code>/slack invite</code> Install the CraftOS app to your workspace (OAuth flow) <code>/slack login &lt;bot_token&gt; [workspace_name]</code> Connect your own Slack bot token <code>/slack status</code> Show connected workspaces <code>/slack logout [workspace_id]</code> Remove a workspace"},{"location":"connections/slack/#prerequisites","title":"Prerequisites","text":"Invite (easy)Login (your own bot) <p>Requires <code>SLACK_SHARED_CLIENT_ID</code> and <code>SLACK_SHARED_CLIENT_SECRET</code>. Release builds have these embedded. Redirect URI: <code>https://localhost:8765</code>.</p> <ol> <li>Go to api.slack.com/apps</li> <li>Create an app</li> <li>Add OAuth scopes: <code>chat:write</code>, <code>channels:read</code>, <code>users:read</code>, <code>channels:history</code>, <code>search:read</code>, <code>files:write</code></li> <li>Install to workspace</li> <li>Copy the Bot User OAuth Token (<code>xoxb-...</code>)</li> <li>Run <code>/slack login &lt;token&gt;</code></li> </ol>"},{"location":"connections/slack/#troubleshooting","title":"Troubleshooting","text":"<p>OAuth redirect fails \u2014 Slack requires HTTPS. CraftBot uses <code>https://localhost:8765</code> \u2014 a self-signed cert is served. Click through the browser warning.</p> <p>\"not_authed\" errors \u2014 the bot token is invalid. Re-run <code>/slack login</code>.</p>"},{"location":"connections/slack/#related","title":"Related","text":"<ul> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"connections/telegram-bot/","title":"Telegram (Bot)","text":"<p>Connect a Telegram bot (created via @BotFather). Best for public channels, groups, and notifications.</p> <p>For personal messaging via your own Telegram account, see Telegram (User) instead.</p>"},{"location":"connections/telegram-bot/#available-actions","title":"Available actions","text":"<ul> <li><code>send_telegram_bot_message</code> \u2014 send text</li> <li><code>send_telegram_photo</code> \u2014 send image</li> <li><code>get_telegram_updates</code> \u2014 poll new messages</li> <li><code>get_telegram_chat</code> \u2014 look up chat by id/username</li> <li><code>search_telegram_contact</code> \u2014 search contacts by name</li> </ul>"},{"location":"connections/telegram-bot/#connect","title":"Connect","text":"Command What it does <code>/telegram invite</code> Connect the shared CraftOS Telegram bot <code>/telegram login &lt;bot_token&gt;</code> Connect your own bot from @BotFather <code>/telegram status</code> Show all Telegram connections <code>/telegram logout [id]</code> Remove a connection"},{"location":"connections/telegram-bot/#prerequisites","title":"Prerequisites","text":"Invite (easy)Login (your own bot) <p>Requires <code>TELEGRAM_SHARED_BOT_TOKEN</code> and <code>TELEGRAM_SHARED_BOT_USERNAME</code>. Set these in <code>.env</code> or use embedded credentials.</p> <ol> <li>Message @BotFather on Telegram</li> <li><code>/newbot</code> \u2192 follow prompts \u2192 save the token</li> <li>Run <code>/telegram login &lt;token&gt;</code></li> <li>In Telegram, <code>/start</code> your bot to initialize chat</li> </ol>"},{"location":"connections/telegram-bot/#troubleshooting","title":"Troubleshooting","text":"<p>Bot doesn't see messages in groups \u2014 by default, bots only see messages addressed to them. In @BotFather, disable Privacy Mode to see all group messages.</p> <p><code>get_telegram_updates</code> returns nothing \u2014 Telegram's bot API uses long-polling. The first call after a gap returns immediately; subsequent calls hold open for up to ~30s waiting for new events.</p>"},{"location":"connections/telegram-bot/#related","title":"Related","text":"<ul> <li>Telegram (User) \u2014 use your own account via MTProto</li> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"connections/telegram-user/","title":"Telegram (User)","text":"<p>Connect CraftBot as you \u2014 your personal Telegram account, via Telegram's MTProto API. Lets CraftBot send messages on your behalf, read DMs, and access private groups.</p> <p>For public-bot use cases, see Telegram (Bot) instead.</p>"},{"location":"connections/telegram-user/#available-actions","title":"Available actions","text":"<ul> <li><code>send_telegram_user_message</code> \u2014 send as yourself</li> <li><code>send_telegram_user_file</code> \u2014 send a file</li> <li><code>get_telegram_chats</code> \u2014 list your recent chats</li> <li><code>read_telegram_messages</code> \u2014 read messages from a chat</li> <li><code>search_telegram_user_contacts</code> \u2014 search your contacts</li> </ul>"},{"location":"connections/telegram-user/#connect","title":"Connect","text":"<p>Two-step phone auth:</p> <pre><code>/telegram login-user &lt;phone_number&gt;\n# Telegram sends a code to your phone\n/telegram login-user &lt;phone_number&gt; &lt;code&gt; [2fa_password]\n</code></pre> Command What it does <code>/telegram login-user &lt;phone&gt;</code> Step 1: request verification code <code>/telegram login-user &lt;phone&gt; &lt;code&gt; [2fa]</code> Step 2: verify and authenticate <code>/telegram status</code> Show user-account connections <code>/telegram logout [phone]</code> Remove connection"},{"location":"connections/telegram-user/#prerequisites","title":"Prerequisites","text":"<ul> <li>Telegram API credentials (not a bot token):</li> <li>Go to my.telegram.org</li> <li>\u2192 API development tools \u2192 Create new application</li> <li>Save <code>api_id</code> and <code>api_hash</code></li> <li>Set <code>TELEGRAM_API_ID</code> and <code>TELEGRAM_API_HASH</code> in <code>.env</code></li> <li><code>telethon</code> package: <code>pip install telethon</code></li> </ul> <p>Session files (<code>*.session</code>) are stored in the credential store \u2014 back these up to avoid re-verifying on every machine.</p>"},{"location":"connections/telegram-user/#troubleshooting","title":"Troubleshooting","text":"<p>\"Telethon not installed\" \u2014 run <code>pip install telethon</code> in the CraftBot env.</p> <p>\"Wrong 2FA password\" \u2014 2FA is separate from the SMS code. Check if you have 2FA enabled in Telegram and include the password as the third argument.</p> <p>Session lost after restart \u2014 make sure the session file persists. Some containers nuke <code>/tmp/*</code> on restart \u2014 mount the credential dir.</p>"},{"location":"connections/telegram-user/#security-note","title":"Security note","text":"<p>A user-account connection lets CraftBot send messages as you to any contact. Review actions carefully before enabling tier-2+ automations that send Telegram messages.</p>"},{"location":"connections/telegram-user/#related","title":"Related","text":"<ul> <li>Telegram (Bot) \u2014 bot alternative (safer, more limited)</li> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"connections/twitter/","title":"Twitter","text":"<p>Post tweets, read timelines, search, manage followers via the Twitter / X API v2.</p>"},{"location":"connections/twitter/#available-actions","title":"Available actions","text":"<ul> <li>Post / delete tweet</li> <li>Read user timeline</li> <li>Search tweets</li> <li>Follow / unfollow</li> <li>Reply to tweet</li> </ul> <p>Exact names in Actions catalogue.</p>"},{"location":"connections/twitter/#connect","title":"Connect","text":"Command What it does <code>/twitter login &lt;api_key&gt; &lt;api_secret&gt; &lt;access_token&gt; &lt;access_secret&gt;</code> Connect with developer app creds <code>/twitter status</code> Show connection status <code>/twitter logout</code> Remove credentials"},{"location":"connections/twitter/#prerequisites","title":"Prerequisites","text":"<ol> <li>developer.twitter.com \u2192 create a project and app</li> <li>Enable \"User authentication settings\" \u2014 OAuth 2.0 or OAuth 1.0a (this page assumes 1.0a for <code>POST</code> endpoints)</li> <li>Permissions: Read + Write (or Read + Write + DM)</li> <li>Keys and tokens tab \u2192 copy:<ul> <li>API Key + API Secret (app-level)</li> <li>Access Token + Secret (user-level)</li> </ul> </li> <li>Run <code>/twitter login</code> with all four</li> </ol>"},{"location":"connections/twitter/#pricing-note","title":"Pricing note","text":"<p>Twitter/X API is tiered. Free tier has strict limits (~1500 tweets/month). Read endpoints need paid tier for volume.</p>"},{"location":"connections/twitter/#troubleshooting","title":"Troubleshooting","text":"<p>403 Forbidden on POST \u2014 app permissions are Read-only. In the portal \u2192 User authentication settings, set Read + Write, then regenerate access tokens (old ones still have Read-only).</p> <p>\"Rate limit exceeded\" \u2014 free tier resets every 15 minutes (different windows per endpoint). Check <code>x-rate-limit-reset</code> headers in errors.</p>"},{"location":"connections/twitter/#related","title":"Related","text":"<ul> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"connections/whatsapp-business/","title":"WhatsApp Business","text":"<p>Connect via Meta's official WhatsApp Business Platform (Cloud API). Higher limits, production-grade, requires a Meta app and verified phone number.</p> <p>For quick personal use without Meta setup, see WhatsApp Web.</p>"},{"location":"connections/whatsapp-business/#available-actions","title":"Available actions","text":"<ul> <li><code>send_whatsapp_business_message</code> \u2014 send text</li> <li><code>send_whatsapp_business_template</code> \u2014 send templated messages (required for 24-hour-window exits)</li> <li><code>send_whatsapp_business_media</code> \u2014 send image / document / audio</li> <li><code>get_whatsapp_business_messages</code> \u2014 list incoming messages (via webhook)</li> </ul>"},{"location":"connections/whatsapp-business/#connect","title":"Connect","text":"Command What it does <code>/whatsapp_business login &lt;access_token&gt; &lt;phone_number_id&gt;</code> Connect with Meta API credentials <code>/whatsapp_business status</code> Show connection status <code>/whatsapp_business logout</code> Remove credentials"},{"location":"connections/whatsapp-business/#prerequisites","title":"Prerequisites","text":"<ol> <li>Meta for Developers \u2192 create an app \u2192 Business type</li> <li>Add WhatsApp product</li> <li>Configure a phone number (test or verified production)</li> <li>Copy the temporary access token (lasts 23 hours) \u2014 for permanent setup, generate a System User token</li> <li>Copy the Phone Number ID</li> <li>Run <code>/whatsapp_business login &lt;token&gt; &lt;phone_number_id&gt;</code></li> </ol>"},{"location":"connections/whatsapp-business/#24-hour-message-window","title":"24-hour message window","text":"<p>Meta limits free-form messages to 24 hours after the user last messaged you. Outside that window, you must use template messages pre-approved in Meta Business Manager. The agent uses <code>send_whatsapp_business_template</code> for those.</p>"},{"location":"connections/whatsapp-business/#troubleshooting","title":"Troubleshooting","text":"<p>\"Recipient not opted in\" \u2014 the user needs to message your business number first (or you need a template in the right category and their consent).</p> <p>\"Token expired\" \u2014 temporary tokens last 23 hours. Use a System User with a permanent token for production.</p> <p>Webhook for inbound messages \u2014 not configured by default. To receive messages, set up a webhook in Meta and point it at your CraftBot instance.</p>"},{"location":"connections/whatsapp-business/#related","title":"Related","text":"<ul> <li>WhatsApp Web \u2014 simpler alternative</li> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"connections/whatsapp-web/","title":"WhatsApp Web","text":"<p>Connect your personal WhatsApp via the web client (Playwright-driven). No official API fees, no Meta app setup \u2014 just scan a QR code.</p> <p>For the Business API with Meta app credentials, see WhatsApp Business.</p>"},{"location":"connections/whatsapp-web/#available-actions","title":"Available actions","text":"<ul> <li><code>send_whatsapp_web_text_message</code> \u2014 send a text message</li> <li><code>send_whatsapp_web_media_message</code> \u2014 send an image / file</li> <li><code>get_whatsapp_chat_history</code> \u2014 read recent messages from a chat</li> <li><code>get_whatsapp_unread_chats</code> \u2014 list unread chats</li> <li><code>search_whatsapp_contact</code> \u2014 find a contact by name</li> </ul>"},{"location":"connections/whatsapp-web/#connect","title":"Connect","text":"Command What it does <code>/whatsapp login [phone_number]</code> Open WhatsApp Web and show QR <code>/whatsapp status</code> Show all WhatsApp connections <code>/whatsapp logout [id]</code> Remove a connection"},{"location":"connections/whatsapp-web/#prerequisites","title":"Prerequisites","text":"<ul> <li> <p>Playwright with Chromium:</p> <pre><code>pip install playwright\nplaywright install chromium\n</code></pre> </li> <li> <p>The <code>/whatsapp login</code> command launches WhatsApp Web in a controlled Chromium instance and displays the QR. Scan it with your phone (WhatsApp \u2192 \u22ef \u2192 Linked devices \u2192 Link a device).</p> </li> </ul>"},{"location":"connections/whatsapp-web/#session-persistence","title":"Session persistence","text":"<p>The browser profile is saved in the credential store so you don't re-scan on every run. If your phone ends the linked-device session (WhatsApp does this periodically), you'll need to re-scan.</p>"},{"location":"connections/whatsapp-web/#troubleshooting","title":"Troubleshooting","text":"<p>\"Playwright not installed\" \u2014 <code>pip install playwright &amp;&amp; playwright install chromium</code>. On Linux you may also need system libraries (<code>playwright install-deps</code>).</p> <p>QR expired \u2014 the WhatsApp Web QR rotates every ~1 minute. If you miss the window, re-run <code>/whatsapp login</code>.</p> <p>Chromium can't launch \u2014 headless mode is used by default. For debugging, launch with a visible window by setting <code>PLAYWRIGHT_HEADLESS=false</code>.</p>"},{"location":"connections/whatsapp-web/#limitations","title":"Limitations","text":"<ul> <li>WhatsApp Web has rate limits that kick in at moderate volume \u2014 don't spam.</li> <li>Reading messages works but doesn't mark them as read on your phone's WhatsApp unless you actually open the chat.</li> <li>Voice/video calls are not supported.</li> </ul>"},{"location":"connections/whatsapp-web/#related","title":"Related","text":"<ul> <li>WhatsApp Business \u2014 official API route</li> <li>Credentials</li> <li>Connections overview</li> </ul>"},{"location":"develop/","title":"Develop","text":"<p>Extend CraftBot. Five paths, all with a scaffold \u2192 write \u2192 register \u2192 reload \u2192 test flow.</p> <ul> <li> <p> Skills overview</p> <p>What skills are, when to create one, where they live.</p> </li> <li> <p> Write a CraftBot skill</p> <p>Step-by-step: scaffold a skill, define tools, reload, test.</p> </li> <li> <p> External skills</p> <p>Load third-party skills from the <code>skills/</code> directory.</p> </li> <li> <p> Custom action</p> <p>Write a single action with input/output schemas.</p> </li> <li> <p> Custom agent</p> <p>Subclass the base agent into a bundle (<code>config.yaml</code> + role + RAG + custom actions).</p> </li> </ul>"},{"location":"develop/#what-to-build","title":"What to build","text":"Goal Build One-off capability (e.g. \"query our internal API\") Custom action Reusable set of actions + prompt + metadata CraftBot skill Dedicated agent with its own personality/knowledge Custom agent Import tools from an existing MCP server MCP servers"},{"location":"develop/#related","title":"Related","text":"<ul> <li>Actions \u2014 the primitive every extension extends</li> <li>Skill &amp; action selection \u2014 how the agent picks between what you built</li> <li>Actions catalogue \u2014 every built-in action as a baseline</li> </ul>"},{"location":"develop/custom-action/","title":"Custom action","text":"<p>Actions are Python functions with a schema. One file, one <code>@action</code> decorator, and the agent can call your code.</p>"},{"location":"develop/custom-action/#what-youre-building","title":"What you're building","text":"<p>A function the action router can pick, with:</p> <ul> <li>A name the LLM will reference</li> <li>A description that teaches the LLM when to pick it</li> <li>Input schema (JSON schema lite) for parameters</li> <li>Output schema for the return dict</li> <li>An optional test_payload for local dry-runs</li> </ul>"},{"location":"develop/custom-action/#step-1-drop-a-file-in-appdataaction","title":"Step 1 \u2014 Drop a file in <code>app/data/action/</code>","text":"<pre><code>touch app/data/action/check_weather.py\n</code></pre> <p>Any <code>.py</code> under <code>app/data/action/</code> is auto-discovered on startup.</p>"},{"location":"develop/custom-action/#step-2-write-the-action","title":"Step 2 \u2014 Write the action","text":"<pre><code># app/data/action/check_weather.py\nfrom app.action.action_framework.registry import action\nimport urllib.request\nimport json\n\n@action(\n    name=\"check_weather\",\n    description=(\n        \"Return current weather for a city. \"\n        \"Use when the user asks about weather, temperature, or outdoor conditions.\"\n    ),\n    mode=\"ALL\",                          # CLI | GUI | ALL\n    execution_mode=\"internal\",           # internal (in-process) | sandboxed\n    action_sets=[\"core\"],                # appears in the core set\n    platforms=[\"linux\", \"windows\", \"darwin\"],\n    input_schema={\n        \"city\": {\n            \"type\": \"string\",\n            \"example\": \"Tokyo\",\n            \"description\": \"City name to look up\",\n        },\n    },\n    output_schema={\n        \"status\":      {\"type\": \"string\",  \"example\": \"success\"},\n        \"temperature\": {\"type\": \"number\",  \"example\": 22.5},\n        \"conditions\":  {\"type\": \"string\",  \"example\": \"partly cloudy\"},\n    },\n    test_payload={\"city\": \"Tokyo\"},\n)\ndef check_weather(input_data: dict) -&gt; dict:\n    city = input_data[\"city\"]\n    url = f\"https://wttr.in/{city}?format=j1\"\n    with urllib.request.urlopen(url, timeout=5) as r:\n        data = json.loads(r.read())\n    c = data[\"current_condition\"][0]\n    return {\n        \"status\": \"success\",\n        \"temperature\": float(c[\"temp_C\"]),\n        \"conditions\": c[\"weatherDesc\"][0][\"value\"],\n    }\n</code></pre>"},{"location":"develop/custom-action/#step-3-register-with-the-right-action-set","title":"Step 3 \u2014 Register with the right action set","text":"<p><code>action_sets: [\"core\"]</code> makes it available to every task. Scope it more narrowly:</p> Action set Contains <code>core</code> Always available \u2014 use sparingly <code>web_research</code> Only tasks that select <code>web_research</code> <code>file_operations</code> File I/O tasks <code>shell</code> Shell-capable tasks <code>document_processing</code> PDF / DOCX Custom set name Create your own \u2014 tasks will opt in via <code>task_start</code>"},{"location":"develop/custom-action/#step-4-reload","title":"Step 4 \u2014 Reload","text":"<p>Stop the agent (<code>/exit</code>) and restart. On startup, the registry auto-discovers the new file. Actions aren't hot-reloaded \u2014 adding/removing requires restart.</p> <pre><code>[REGISTRY] Loaded 87 actions from app/data/action/\n</code></pre>"},{"location":"develop/custom-action/#step-5-test","title":"Step 5 \u2014 Test","text":""},{"location":"develop/custom-action/#from-the-agent","title":"From the agent","text":"<p>Ask the agent to use it:</p> <p>\"What's the weather in Tokyo?\"</p> <p>The router should pick <code>check_weather</code> and you'll see <code>action_start: check_weather</code> in logs.</p>"},{"location":"develop/custom-action/#from-python","title":"From Python","text":"<pre><code>from app.action.action_framework.registry import registry_instance\nfn = registry_instance.get_action(\"check_weather\")\nprint(fn({\"city\": \"Tokyo\"}))\n</code></pre>"},{"location":"develop/custom-action/#from-cli-dry-run","title":"From CLI (dry-run)","text":"<p>If you supplied <code>test_payload</code>, the agent's diagnostic CLI can dry-run:</p> <pre><code>python -m app.diagnostic.run_action check_weather\n</code></pre>"},{"location":"develop/custom-action/#metadata-reference","title":"Metadata reference","text":"Field Required Purpose <code>name</code> yes Action id the LLM uses <code>description</code> yes Teaches the LLM when to use \u2014 be specific <code>mode</code> no (default <code>ALL</code>) <code>CLI</code> / <code>GUI</code> / <code>ALL</code> visibility <code>execution_mode</code> no (default <code>internal</code>) <code>internal</code> runs in-process; <code>sandboxed</code> runs in a subprocess <code>action_sets</code> yes List of sets \u2014 how tasks scope to you <code>platforms</code> no List of OS families; omit = all <code>default</code> no If <code>True</code>, available even without explicit set selection <code>input_schema</code> yes JSON-schema-lite per input param <code>output_schema</code> yes JSON-schema-lite for the return dict <code>test_payload</code> no Example input for dry-run testing"},{"location":"develop/custom-action/#load-order-precedence","title":"Load order / precedence","text":"<ol> <li>Core built-ins (<code>app/data/action/*.py</code>)</li> <li>MCP tools (prefixed with server name)</li> <li>Skill-provided tools</li> <li>Agent bundle actions (e.g. <code>agents/dog_agent/data/action/</code>)</li> </ol> <p>Higher = overridable by lower. An agent bundle can override a core action by registering with the same <code>name</code>.</p>"},{"location":"develop/custom-action/#writing-good-descriptions","title":"Writing good descriptions","text":"<p>The description is what the LLM sees in the candidate list. It's the single most important field for getting your action picked correctly.</p> <p>Good:</p> <p>\"Return current weather for a city. Use when the user asks about weather, temperature, or outdoor conditions. Returns temperature in Celsius and a conditions string.\"</p> <p>Bad:</p> <p>\"Gets weather\"</p> <p>Include: what it does, when to use it, what it returns.</p>"},{"location":"develop/custom-action/#related","title":"Related","text":"<ul> <li>Actions concept</li> <li>Skill &amp; action selection</li> <li>Actions catalogue \u2014 browse built-ins for examples</li> <li>Custom agent \u2014 actions scoped to an agent bundle</li> <li>MCP servers \u2014 the external alternative</li> </ul>"},{"location":"develop/custom-agent/","title":"Custom agent","text":"<p>CraftBot ships an extensible <code>AgentBase</code> class. Subclass it, add a personality, bundle your own RAG docs and custom actions, and you have a specialized agent. The <code>agents/personal_assistant/</code> and <code>agents/dog_agent/</code> bundles are working references.</p>"},{"location":"develop/custom-agent/#what-youre-building","title":"What you're building","text":"<p>An agent bundle \u2014 a directory under <code>agents/&lt;your-agent&gt;/</code> containing:</p> <ul> <li><code>agent.py</code> \u2014 subclass of <code>AgentBase</code> with <code>_generate_role_info_prompt()</code> override</li> <li><code>config.yaml</code> \u2014 per-agent settings (model, provider, RAG dir)</li> <li><code>data/</code> \u2014 private agent file system with this agent's AGENT.md, USER.md, etc.</li> <li>(optional) <code>rag_docs/</code> \u2014 domain docs indexed into a private memory namespace</li> <li>(optional) <code>data/action/</code> \u2014 actions scoped to this agent</li> </ul>"},{"location":"develop/custom-agent/#step-1-scaffold-the-bundle","title":"Step 1 \u2014 Scaffold the bundle","text":"<pre><code>mkdir -p agents/research_agent/{data,rag_docs}\ntouch agents/research_agent/{__init__.py,agent.py,config.yaml}\n</code></pre> <p>Directory layout:</p> <pre><code>agents/research_agent/\n\u251c\u2500\u2500 __init__.py\n\u251c\u2500\u2500 agent.py\n\u251c\u2500\u2500 config.yaml\n\u251c\u2500\u2500 data/                    # Agent file system (AGENT.md, USER.md, \u2026)\n\u2502   \u2514\u2500\u2500 action/              # Optional: per-agent actions\n\u2514\u2500\u2500 rag_docs/                # Optional: RAG corpus\n</code></pre>"},{"location":"develop/custom-agent/#step-2-write-configyaml","title":"Step 2 \u2014 Write <code>config.yaml</code>","text":"<pre><code># agents/research_agent/config.yaml\ndata_dir: agents/research_agent/data\nrag_dir: rag_docs\nrag_namespace: research_agent_knowledge\n\nllm_provider: anthropic\nmodel: claude-sonnet-4-6\nmax_tokens: 16000\n</code></pre> <p>All fields are optional \u2014 unset ones fall back to <code>settings.json</code>.</p>"},{"location":"develop/custom-agent/#step-3-subclass-agentbase","title":"Step 3 \u2014 Subclass <code>AgentBase</code>","text":"<pre><code># agents/research_agent/agent.py\nfrom __future__ import annotations\nfrom pathlib import Path\nimport yaml\n\nfrom app.agent_base import AgentBase\n\n\nclass ResearchAgent(AgentBase):\n    \"\"\"An agent specialized in literature review and synthesis.\"\"\"\n\n    @classmethod\n    def from_bundle(cls, bundle_dir: str | Path) -&gt; \"ResearchAgent\":\n        bundle_path = Path(bundle_dir).resolve()\n        cfg = yaml.safe_load((bundle_path / \"config.yaml\").read_text())\n        return cls(cfg, bundle_path)\n\n    def __init__(self, cfg: dict, bundle_path: Path):\n        self._bundle_path = Path(bundle_path)\n        self._cfg = cfg\n        super().__init__(\n            data_dir=cfg.get(\"data_dir\", \"app/data\"),\n            chroma_path=str(self._bundle_path / cfg.get(\"rag_dir\", \"rag_docs\")),\n            llm_provider=cfg.get(\"llm_provider\", \"anthropic\"),\n        )\n\n    def _generate_role_info_prompt(self) -&gt; str:\n        return (\n            \"You are a research agent specialized in synthesis across academic papers, \"\n            \"industry reports, and primary sources. Always cite sources. When uncertain, \"\n            \"say so and recommend further reading. Use the agent file system to keep \"\n            \"notes across sessions.\"\n        )\n</code></pre>"},{"location":"develop/custom-agent/#step-4-register-in-runpy-or-call-directly","title":"Step 4 \u2014 Register in <code>run.py</code> (or call directly)","text":"<p>The simplest path is to call <code>from_bundle</code> in your own entry point:</p> <pre><code># run_research.py\nimport asyncio\nfrom agents.research_agent.agent import ResearchAgent\n\nasync def main():\n    agent = ResearchAgent.from_bundle(\"agents/research_agent\")\n    await agent.start()\n\nasyncio.run(main())\n</code></pre> <p>Or patch <code>run.py</code> to dispatch to your agent based on a CLI flag:</p> <pre><code>python run.py --agent research_agent\n</code></pre>"},{"location":"develop/custom-agent/#step-5-test","title":"Step 5 \u2014 Test","text":"<p>Send a message:</p> <p>\"Find recent papers on prompt caching strategies for LLMs and summarise.\"</p> <p>Watch the agent pull from your RAG corpus, cite sources, and save notes to your private <code>data/MEMORY.md</code>.</p>"},{"location":"develop/custom-agent/#overridable-hooks","title":"Overridable hooks","text":"<p><code>AgentBase</code> exposes several <code>_prefixed_</code> hooks your subclass can override:</p> Hook Purpose <code>_generate_role_info_prompt()</code> Fills <code>AGENT_ROLE_PROMPT</code> \u2014 your agent's persona + instructions <code>_load_extra_system_prompt()</code> Append extra static text to the system prompt <code>_register_extra_actions()</code> Register additional actions programmatically <code>_build_db_interface()</code> Point at a different database backend <code>_get_interface_capabilities_prompt()</code> Interface-aware prompt fragment (e.g. \"You're in browser mode, so you can receive attachments.\") <p>The <code>agents/dog_agent/</code> in the repo is a minimal working example \u2014 override <code>_generate_role_info_prompt</code> to replace all English with \"woof\".</p>"},{"location":"develop/custom-agent/#per-agent-actions","title":"Per-agent actions","text":"<p>Drop <code>.py</code> files under <code>agents/&lt;agent&gt;/data/action/</code> \u2014 they're discovered just like core actions but only registered when this agent runs.</p> <pre><code>agents/research_agent/data/action/citation_format.py   # @action(...)\n</code></pre>"},{"location":"develop/custom-agent/#per-agent-skills","title":"Per-agent skills","text":"<p>Set <code>selected_skills</code> in <code>config.yaml</code> to preload skills for every task created by this agent.</p>"},{"location":"develop/custom-agent/#per-agent-rag","title":"Per-agent RAG","text":"<p><code>rag_dir: rag_docs</code> tells memory to index <code>agents/&lt;agent&gt;/rag_docs/</code> into its own ChromaDB namespace (<code>rag_namespace</code>). Your agent can <code>memory_search(\"...\")</code> and get hits only from its private corpus.</p>"},{"location":"develop/custom-agent/#load-order","title":"Load order","text":"<p>When your agent starts:</p> <ol> <li>Read <code>config.yaml</code></li> <li>Initialize <code>AgentBase</code> with overrides from the config</li> <li>Discover core actions (<code>app/data/action/*.py</code>)</li> <li>Discover bundle actions (<code>agents/&lt;agent&gt;/data/action/*.py</code>) \u2014 can override core</li> <li>Load MCP servers from <code>mcp_config.json</code></li> <li>Build memory index: <code>agent_file_system/</code> PLUS <code>agents/&lt;agent&gt;/rag_docs/</code></li> <li>Register the agent's role prompt</li> <li>Start the agent loop</li> </ol>"},{"location":"develop/custom-agent/#related","title":"Related","text":"<ul> <li>Agent bundle config.yaml</li> <li>Agent loop \u2014 what you're subclassing</li> <li>Custom action \u2014 for bundling per-agent actions</li> <li>Skills overview \u2014 for bundling strategy into prompts</li> </ul>"},{"location":"develop/skills/","title":"Skills","text":"<p>A skill is a reusable bundle of actions, prompt instructions, and metadata that the agent can load on demand. Think of it as a plugin that teaches CraftBot a new capability.</p> <ul> <li> <p> Write a CraftBot skill</p> <p>Scaffold \u2192 <code>skill.md</code> \u2192 register \u2192 reload \u2192 test.</p> </li> <li> <p> External skills</p> <p>Drop third-party skill folders into <code>skills/</code> and CraftBot picks them up.</p> </li> </ul>"},{"location":"develop/skills/#skill-vs-action-vs-agent","title":"Skill vs action vs agent","text":"Build When Action Single capability, no instructions Skill Bundle of actions + prompt + metadata, loaded on demand Agent Dedicated persona + RAG + custom actions"},{"location":"develop/skills/#related","title":"Related","text":"<ul> <li>Actions</li> <li>Skill &amp; action selection</li> </ul>"},{"location":"develop/skills/craftbot-skill/","title":"Write a CraftBot skill","text":"<p>A skill is a bundle of strategy \u2014 a <code>skill.md</code> + optional scripts \u2014 that teaches the agent how to use existing actions for a specific domain. Unlike actions (which add capability), skills add guidance.</p>"},{"location":"develop/skills/craftbot-skill/#what-youre-building","title":"What you're building","text":"<p>A folder under <code>skills/&lt;your-skill&gt;/</code> containing:</p> <ul> <li><code>skill.md</code> \u2014 YAML frontmatter + markdown instructions</li> <li>(optional) <code>scripts/</code> \u2014 helper shell scripts the skill calls via <code>run_shell</code></li> </ul> <p>At runtime, the agent's SkillManager discovers your skill. Tasks opt in by setting <code>selected_skills: [\"&lt;name&gt;\"]</code> \u2014 the skill's instructions then inject into the system prompt for that task.</p>"},{"location":"develop/skills/craftbot-skill/#step-1-scaffold","title":"Step 1 \u2014 Scaffold","text":"<pre><code>mkdir -p skills/daily-standup-summary\ntouch skills/daily-standup-summary/skill.md\n</code></pre> <p>The folder name is your skill id.</p>"},{"location":"develop/skills/craftbot-skill/#step-2-write-skillmd","title":"Step 2 \u2014 Write <code>skill.md</code>","text":"<pre><code>---\nname: daily-standup-summary\ndescription: |\n  Generate a daily standup summary from recent events and commit activity.\n  Use for morning check-ins, team updates, and progress reports.\nos:                          # optional \u2014 restrict to specific OSes\n  - linux\n  - darwin\nbinaries:                    # optional \u2014 require binaries to be available\n  - git\nconfig_deps:                 # optional \u2014 require settings.json fields\n  - github.enabled\n---\n\n# Daily Standup Summary\n\nWhen this skill is active, follow these steps:\n\n1. **Read recent events** \u2014 `read_file agent_file_system/EVENT.md` and scan the last 24 hours.\n2. **Check git activity** \u2014 `run_shell \"git log --since='24 hours ago' --oneline\"` in each tracked repo.\n3. **Collate into three sections**:\n   - Done yesterday\n   - Plan for today\n   - Blockers\n4. **Keep the output terse** \u2014 bullet points, not paragraphs.\n5. **Send via `send_message`** \u2014 or the preferred platform action if the user has integrations connected.\n\n## Don'ts\n\n- Don't list every commit \u2014 only meaningful ones.\n- Don't invent activity if sources are empty; say \"no activity detected\" instead.\n</code></pre>"},{"location":"develop/skills/craftbot-skill/#step-3-register-auto-discovery","title":"Step 3 \u2014 Register (auto-discovery)","text":"<p>There is no manual registration. On startup the SkillManager scans:</p> <ul> <li><code>skills/</code> at the project root (user-provided skills)</li> <li>Plus any paths configured in <code>app/config/skills_config.json</code></li> </ul> <p>Your new skill is discovered automatically and available for selection.</p>"},{"location":"develop/skills/craftbot-skill/#step-4-reload-the-session","title":"Step 4 \u2014 Reload the session","text":"<p>Skills are hot-reloaded \u2014 saving <code>skill.md</code> makes the skill available on the next task. No restart needed.</p> <pre><code>[SkillManager] Loaded 42 skills (3 added, 0 removed)\n</code></pre>"},{"location":"develop/skills/craftbot-skill/#step-5-test","title":"Step 5 \u2014 Test","text":""},{"location":"develop/skills/craftbot-skill/#force-select-the-skill","title":"Force-select the skill","text":"<p>When creating a task via <code>/agent_command</code> or Python:</p> <pre><code>task_manager.create_task(\n    task_name=\"Morning standup\",\n    task_instruction=\"Produce today's standup summary\",\n    mode=\"simple\",\n    selected_skills=[\"daily-standup-summary\"],\n)\n</code></pre>"},{"location":"develop/skills/craftbot-skill/#from-chat","title":"From chat","text":"<p>\"Using the daily-standup-summary skill, give me today's update.\"</p> <p>The agent detects the skill reference, enables it for the task, and follows its instructions.</p>"},{"location":"develop/skills/craftbot-skill/#list-skills","title":"List skills","text":"<pre><code>/skill list\n</code></pre> <p>Shows your new skill with its enabled state.</p>"},{"location":"develop/skills/craftbot-skill/#metadata-reference","title":"Metadata reference","text":"Field Type Purpose <code>name</code> str Must match folder name <code>description</code> str Teaches the LLM when to opt in \u2014 similar to action description <code>os</code> list Restrict to <code>linux</code>, <code>darwin</code>, <code>windows</code> <code>binaries</code> list Required executables (skill is skipped if missing) <code>config_deps</code> list Required settings.json fields <code>allow_tools</code> list Optional allowlist of actions this skill can call (narrows the router)"},{"location":"develop/skills/craftbot-skill/#load-order-precedence","title":"Load order / precedence","text":"<ol> <li>Agent-bundle skills (via <code>selected_skills</code> in <code>config.yaml</code>) \u2014 always on for that agent</li> <li>Task-selected skills (<code>selected_skills</code> on the <code>Task</code> dataclass) \u2014 per-task</li> <li>Auto-detected by LLM \u2014 if a skill's description matches the user's query, the LLM can opt in via <code>task_update_todos</code> / <code>set_mode</code></li> </ol> <p>Multiple skills can be active simultaneously \u2014 their instructions are concatenated into the system prompt.</p>"},{"location":"develop/skills/craftbot-skill/#scripts","title":"Scripts","text":"<p>If your skill bundles shell scripts:</p> <pre><code>skills/daily-standup-summary/\n\u251c\u2500\u2500 skill.md\n\u2514\u2500\u2500 scripts/\n    \u2514\u2500\u2500 collect_repos.sh\n</code></pre> <p>Reference them from the skill.md:</p> <p>\"Run <code>skills/daily-standup-summary/scripts/collect_repos.sh</code> to find all tracked repos.\"</p> <p>The agent invokes them via <code>run_shell</code>.</p>"},{"location":"develop/skills/craftbot-skill/#browse-examples","title":"Browse examples","text":"<p>The <code>skills/</code> directory has dozens of working examples \u2014 <code>ai-ppt-generator</code>, <code>baidu-search</code>, <code>anki-connect</code>, <code>apple-notes</code>, etc. Read a few to learn idioms.</p>"},{"location":"develop/skills/craftbot-skill/#related","title":"Related","text":"<ul> <li>External skills \u2014 load third-party skills</li> <li>Custom action \u2014 add capability rather than strategy</li> <li>Skill &amp; action selection \u2014 how skills are picked</li> <li>Skills overview</li> </ul>"},{"location":"develop/skills/external-skill/","title":"External skills","text":"<p>Skills don't have to live in your repo. The SkillManager can load skills from anywhere on disk, letting you use community-built skills or share your own across projects.</p>"},{"location":"develop/skills/external-skill/#what-youre-building","title":"What you're building","text":"<p>A mechanism to point CraftBot at a directory of skills that lives outside your checkout.</p>"},{"location":"develop/skills/external-skill/#step-1-point-to-the-path","title":"Step 1 \u2014 Point to the path","text":"<p>Edit <code>app/config/skills_config.json</code>:</p> <pre><code>{\n  \"enabled\": true,\n  \"skill_paths\": [\n    \"~/shared-skills\",\n    \"/opt/craftos/skills\",\n    \"git+https://github.com/alice/skills-pack#my-skills-subdir\"\n  ]\n}\n</code></pre> <p>Paths resolve in order. Duplicate skill names \u2192 first path wins.</p>"},{"location":"develop/skills/external-skill/#step-2-drop-in-or-clone-skills","title":"Step 2 \u2014 Drop in (or clone) skills","text":"<p>If you have a skill-pack repo:</p> <pre><code>mkdir ~/shared-skills\ncd ~/shared-skills\ngit clone https://github.com/alice/skills-pack.git\n</code></pre> <p>Or install a specific skill:</p> <pre><code>mkdir -p ~/shared-skills/awesome-skill\n# copy skill.md + any scripts\n</code></pre> <p>The path structure should match the in-repo format \u2014 one folder per skill, <code>skill.md</code> at the root.</p>"},{"location":"develop/skills/external-skill/#step-3-reload","title":"Step 3 \u2014 Reload","text":"<p><code>skills_config.json</code> is hot-reloaded. Save the file and new skills appear within ~1 second.</p> <pre><code>[SkillManager] Discovered 3 new skills from ~/shared-skills\n</code></pre>"},{"location":"develop/skills/external-skill/#step-4-verify","title":"Step 4 \u2014 Verify","text":"<pre><code>/skill list\n</code></pre> <p>Your external skills appear alongside built-ins. Enable / disable with <code>/skill enable &lt;name&gt;</code> / <code>/skill disable &lt;name&gt;</code>.</p>"},{"location":"develop/skills/external-skill/#where-to-find-skills","title":"Where to find skills","text":"<ul> <li>Official \u2014 <code>skills/</code> subfolder in the CraftBot repo (dozens of reference skills)</li> <li>Community \u2014 search GitHub for <code>craftbot skill</code> or <code>claude skill</code></li> <li>Your own \u2014 version-controlled in your own repo or synced via cloud folder</li> </ul>"},{"location":"develop/skills/external-skill/#safety-considerations","title":"Safety considerations","text":"<ul> <li>Read the skill.md before enabling \u2014 skills can instruct the agent to run shell commands.</li> <li>Review <code>scripts/</code> \u2014 bundled shell scripts run with your user privileges.</li> <li>Pin versions \u2014 if you use <code>git+https://...</code>, specify a commit or tag, not a branch.</li> </ul>"},{"location":"develop/skills/external-skill/#per-agent-external-skills","title":"Per-agent external skills","text":"<p>An agent bundle can have its own skill paths in <code>config.yaml</code>:</p> <pre><code># agents/research_agent/config.yaml\nskill_paths:\n  - agents/research_agent/skills\n  - ~/shared-skills/research\n</code></pre>"},{"location":"develop/skills/external-skill/#load-order","title":"Load order","text":"<ol> <li>Built-in skills (<code>skills/</code> in repo root)</li> <li><code>skill_paths</code> from <code>skills_config.json</code>, in order</li> <li>Agent-bundle skills from the agent's <code>config.yaml</code></li> </ol> <p>First match wins on duplicates.</p>"},{"location":"develop/skills/external-skill/#related","title":"Related","text":"<ul> <li>Write a CraftBot skill \u2014 the skill format</li> <li>Skills overview</li> <li>Skill &amp; action selection</li> </ul>"},{"location":"interfaces/","title":"Interfaces","text":"<p>CraftBot has four user-facing interfaces and an internal UI layer that unifies them. Pick based on your environment: browser for the best UX, TUI for terminal work, CLI for scripting, GUI for desktop automation.</p> <ul> <li> <p> Browser</p> <p>Modern web UI (default). Requires Node.js 18+.</p> </li> <li> <p> CLI</p> <p>Lightweight command-line interface. No dependencies.</p> </li> <li> <p> TUI</p> <p>Rich terminal UI powered by Textual. No Node.js needed.</p> </li> <li> <p> GUI / Vision</p> <p>Desktop automation via vision models. Experimental.</p> </li> <li> <p> UI layer</p> <p>The adapter layer that lets one agent drive all four interfaces.</p> </li> <li> <p> Living UI</p> <p>Dynamic UI state binding between agent and adapter.</p> </li> </ul>"},{"location":"interfaces/#which-interface-should-i-use","title":"Which interface should I use?","text":"You want Use The best out-of-the-box experience Browser A terminal-native UI TUI Scripting, piping, minimal footprint CLI Desktop automation (move mouse, click, screenshot) GUI <p>Launch examples are on each page and on the interfaces overview.</p>"},{"location":"interfaces/#related","title":"Related","text":"<ul> <li>Commands \u2014 the slash commands available in every interface</li> <li>Service mode \u2014 run the browser interface as a background daemon</li> </ul>"},{"location":"interfaces/browser/","title":"Browser mode","text":"<p>CraftBot's default interface \u2014 a modern web UI served from a local Node.js dev server, with live event stream rendering, rich chat, task visualization, and a full settings surface.</p>"},{"location":"interfaces/browser/#quick-start","title":"Quick start","text":"<pre><code>python run.py\n</code></pre> <p>Opens <code>http://localhost:xxxx</code> in your default browser automatically.</p>"},{"location":"interfaces/browser/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10+.</li> <li>Node.js 18+ (installed separately \u2014 see Install).</li> <li>An LLM API key.</li> </ul> <p>No Node.js? Use TUI mode \u2014 <code>python run.py --tui</code>.</p>"},{"location":"interfaces/browser/#features","title":"Features","text":"<ul> <li>Live action cards \u2014 every action renders as a card with inputs/outputs.</li> <li>Streaming chat \u2014 responses stream token-by-token.</li> <li>Task tree \u2014 see todos, status, and ETA.</li> <li>Integration setup \u2014 click-through OAuth flows.</li> <li>Skill marketplace \u2014 browse, install, toggle.</li> <li>MCP manager \u2014 add/edit/remove MCP servers with live validation.</li> <li>Multi-task view \u2014 watch multiple running tasks side by side.</li> </ul>"},{"location":"interfaces/browser/#running-as-a-service","title":"Running as a service","text":"<p>For an always-on assistant that survives terminal close:</p> <pre><code>python service.py install\n</code></pre> <p>See Service mode for the full runbook.</p>"},{"location":"interfaces/browser/#remote-access","title":"Remote access","text":"<p>By default the browser binds to <code>localhost</code> only. To access from another machine, see Service mode \u2014 SSH tunnel and Tailscale are both supported.</p>"},{"location":"interfaces/browser/#layout","title":"Layout","text":"<ul> <li><code>app/browser/interface.py</code> \u2014 Python backend</li> <li><code>app/ui_layer/browser/</code> \u2014 WebSocket bridge, frontend handshake</li> <li>Frontend lives in a separate Node.js subproject (Next.js-style), launched by <code>run.py</code></li> </ul>"},{"location":"interfaces/browser/#knobs","title":"Knobs","text":"<pre><code>// settings.json (partial)\n{ \"general\": { \"agent_name\": \"CraftBot\" } }\n</code></pre> <p>The agent name appears in the browser title bar and header.</p>"},{"location":"interfaces/browser/#theming","title":"Theming","text":"<p>Coal + orange palette matches the TUI and documentation. Custom themes go in the frontend subproject \u2014 see Living UI for the state-sync layer that powers them.</p>"},{"location":"interfaces/browser/#related","title":"Related","text":"<ul> <li>TUI \u2014 terminal alternative (no Node.js)</li> <li>UI layer \u2014 the shared adapter layer</li> <li>Living UI \u2014 dynamic state sync</li> <li>Service mode \u2014 run it as a daemon</li> </ul>"},{"location":"interfaces/cli/","title":"CLI mode","text":"<p>The lightest interface CraftBot ships. No Node.js, no Textual, no browser \u2014 just stdin, stdout, and the UI layer. Best for scripting, piping, SSH sessions, and CI.</p>"},{"location":"interfaces/cli/#quick-start","title":"Quick start","text":"<pre><code>python run.py --cli\n</code></pre>"},{"location":"interfaces/cli/#features","title":"Features","text":"<ul> <li>Line-by-line I/O.</li> <li>All slash commands work.</li> <li>Streaming output for action events.</li> <li>ANSI color via the built-in <code>formatter</code>.</li> <li>Pipe-friendly \u2014 redirect <code>stdin</code> / <code>stdout</code> for automation.</li> </ul>"},{"location":"interfaces/cli/#non-interactive-mode","title":"Non-interactive mode","text":"<p>You can drive CLI mode with a script:</p> <pre><code>echo \"what is 2+2\" | python run.py --cli\n</code></pre> <p>Output goes to stdout; logs still go to <code>logs/</code>.</p>"},{"location":"interfaces/cli/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10+.</li> <li>An LLM API key.</li> <li>No Node.js.</li> <li>No Textual / rich \u2014 just stdlib.</li> </ul>"},{"location":"interfaces/cli/#layout","title":"Layout","text":"<p>The CLI interface lives in:</p> <ul> <li><code>app/cli/interface.py</code> \u2014 <code>CLIInterface</code> adapter</li> <li><code>app/cli/formatter.py</code> \u2014 ANSI color + pretty-print</li> <li><code>app/cli/onboarding.py</code> \u2014 first-run wizard</li> </ul>"},{"location":"interfaces/cli/#when-to-use-cli-over-tui","title":"When to use CLI over TUI","text":"Use CLI when Use TUI when Scripting / automation You want a live view of actions SSH with minimal setup Interactive rich UI is OK Piping to another tool You need status bars / panels CI test runs You're iterating locally"},{"location":"interfaces/cli/#knobs","title":"Knobs","text":"<pre><code>// settings.json\n{ \"general\": { \"os_language\": \"en\" } }\n</code></pre> <p>CLI formatter respects <code>os_language</code> for greetings and help text.</p>"},{"location":"interfaces/cli/#related","title":"Related","text":"<ul> <li>TUI \u2014 richer terminal alternative</li> <li>UI layer \u2014 the adapter layer</li> <li>Built-in commands \u2014 same commands work here</li> <li>Interfaces overview</li> </ul>"},{"location":"interfaces/gui-vision/","title":"GUI / Vision mode","text":"<p>GUI mode lets CraftBot see your desktop and drive it with mouse and keyboard \u2014 like a human operator. The agent takes screenshots, asks a VLM where to click, and executes mouse/keyboard actions via <code>pyautogui</code>. It's how you get an agent that can operate apps with no API.</p> <p>Experimental</p> <p>GUI mode is still being hardened. Expect imprecision, especially on dense UIs. For most tasks, browser or TUI mode plus integrations get the job done without vision.</p>"},{"location":"interfaces/gui-vision/#quick-start","title":"Quick start","text":"<p>Install the GUI components (downloads ~4GB of model weights):</p> <pre><code>python install.py --gui\n</code></pre> <p>Then launch:</p> <pre><code>python run.py --gui\n</code></pre>"},{"location":"interfaces/gui-vision/#enable-disable","title":"Enable / disable","text":"<pre><code>// settings.json\n{ \"gui\": { \"enabled\": true } }\n</code></pre> <p>When disabled, the agent loop's GUI branch is never entered \u2014 vision tasks fall back to text reasoning.</p>"},{"location":"interfaces/gui-vision/#two-pipelines","title":"Two pipelines","text":""},{"location":"interfaces/gui-vision/#raw-vlm","title":"Raw VLM","text":"<p>Send the full screenshot to a VLM (Claude, GPT-4o, Gemini, etc.) with a prompt asking \"where should I click to accomplish X?\" \u2014 the VLM returns pixel coordinates or a structured action.</p> <p>Simpler to set up, but pricey per call and less accurate on dense UIs.</p>"},{"location":"interfaces/gui-vision/#omniparser-pipeline","title":"OmniParser pipeline","text":"<pre><code>{ \"gui\": { \"use_omniparser\": true, \"omniparser_url\": \"http://127.0.0.1:7861\" } }\n</code></pre> <p>OmniParser runs as a separate Gradio server. Pipeline:</p> <ol> <li>Screenshot \u2192 OmniParser returns structured elements (buttons, labels, icons with bounding boxes).</li> <li>VLM reasons over the structured element list (not raw pixels).</li> <li>VLM returns element id or coordinates.</li> <li>Mouse/keyboard executes.</li> </ol> <p>More accurate, cheaper per VLM call, but needs OmniParser running.</p>"},{"location":"interfaces/gui-vision/#workflow","title":"Workflow","text":"<p>GUI tasks enter a special loop (see Special workflows):</p> <ol> <li><code>_is_gui_task_mode()</code> \u2014 returns true when task is complex AND <code>STATE.gui_mode == True</code></li> <li>Each iteration calls <code>GUIHandler.gui_module.perform_gui_task_step(...)</code></li> <li>Inside <code>perform_gui_task_step</code>:</li> <li>Take screenshot</li> <li>Call VLM (raw or OmniParser path)</li> <li>Parse the action: <code>click(x, y)</code>, <code>type(\"text\")</code>, <code>drag(x1,y1,x2,y2)</code>, <code>scroll</code>, or <code>screenshot</code> (no-op for observing)</li> <li>Execute via <code>pyautogui</code></li> <li>Log <code>action_start</code> / <code>action_end</code> to the event stream</li> <li>Repeat until the task's current todo is complete</li> </ol>"},{"location":"interfaces/gui-vision/#gui-action-space","title":"GUI action space","text":"Action Inputs <code>click</code> <code>x</code>, <code>y</code>, <code>button</code> (<code>left</code>/<code>right</code>/<code>middle</code>) <code>double_click</code> <code>x</code>, <code>y</code> <code>type</code> <code>text</code> <code>key</code> key name (<code>enter</code>, <code>tab</code>, <code>ctrl+c</code>, \u2026) <code>drag</code> <code>x1, y1, x2, y2</code> <code>scroll</code> <code>direction</code>, <code>amount</code> <code>screenshot</code> \u2014 (for observation) <p>Defined by <code>GUI_ACTION_SPACE_PROMPT</code> in the prompt registry.</p>"},{"location":"interfaces/gui-vision/#dependencies","title":"Dependencies","text":"<p><code>python install.py --gui</code> installs:</p> <ul> <li><code>pyautogui</code> \u2014 mouse/keyboard control</li> <li><code>mss</code> \u2014 fast screenshots</li> <li><code>Pillow</code> \u2014 image processing</li> <li>Platform-specific X11 utils on Linux</li> <li>OmniParser weights (~4GB, optional)</li> </ul> <p>Release builds can bundle these. For CPU-only systems: <code>python install.py --gui --cpu-only</code>.</p>"},{"location":"interfaces/gui-vision/#requirements","title":"Requirements","text":"<ul> <li>A graphical display (X11 on Linux, native on macOS/Windows, or Xvfb in Docker).</li> <li>A VLM-capable provider (see VLM providers).</li> <li>Optional: OmniParser on port 7861.</li> </ul>"},{"location":"interfaces/gui-vision/#docker-headless","title":"Docker / headless","text":"<p>For running GUI mode in a container, use a virtual framebuffer:</p> <pre><code>docker run --rm -it --env-file .env craftbot \\\n  bash -lc \"Xvfb :99 -screen 0 1920x1080x24 &amp; export DISPLAY=:99 &amp;&amp; exec python run.py --gui\"\n</code></pre> <p>Or attach to your host display on Linux:</p> <pre><code>docker run --rm -it \\\n  -e DISPLAY=$DISPLAY \\\n  -v /tmp/.X11-unix:/tmp/.X11-unix \\\n  craftbot python run.py --gui\n</code></pre>"},{"location":"interfaces/gui-vision/#security-considerations","title":"Security considerations","text":"<p>GUI mode gives the agent full control over your mouse and keyboard. Don't leave it running unattended on a desktop with sensitive apps open unless you're sure the task scope is safe.</p>"},{"location":"interfaces/gui-vision/#related","title":"Related","text":"<ul> <li>VLM providers \u2014 the models that see the screen</li> <li>Special workflows \u2014 the GUI workflow handler</li> <li>settings.json <code>gui.*</code> \u2014 full config</li> <li>Install \u2014 <code>--gui</code> flag</li> </ul>"},{"location":"interfaces/living-ui/","title":"Living UI","text":"<p>Limited source access</p> <p>Living UI ships as compiled bytecode (<code>.pyc</code>) only \u2014 public <code>.py</code> source isn't currently accessible for this page. The documentation below is from observable behaviour and integration points. If you're extending it, reach out to @zfoong for the source.</p> <p>The Living UI is CraftBot's dynamic state-sync layer \u2014 it keeps the UI in lockstep with the agent across reconnects, tab switches, and multi-task scenarios. Without it, browser mode would need to re-fetch everything on every reload; with it, the UI stays live even while the agent is mid-action.</p>"},{"location":"interfaces/living-ui/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>A persistent reactive state that survives UI reconnects (e.g. browser refresh).</li> <li>Binds the agent's running state \u2014 current task, active events, pending approvals \u2014 to whatever adapter is rendering.</li> <li>Observable via integration hooks in <code>app/data/action/</code> and <code>app/ui_layer/settings/</code> where adjacent <code>living_ui_*.pyc</code> files live.</li> </ul>"},{"location":"interfaces/living-ui/#integration-surface","title":"Integration surface","text":"<p>The living UI exposes its behaviour to the rest of the code through two compiled modules:</p> <ul> <li><code>app/ui_layer/settings/living_ui_settings.pyc</code> \u2014 settings panel for living-UI behaviour (enable, reconnect timeout, diffing strategy)</li> <li><code>app/data/action/living_ui_actions.pyc</code> \u2014 actions the agent can call to update living-UI state (e.g. request approval, show a popup)</li> </ul>"},{"location":"interfaces/living-ui/#what-it-binds","title":"What it binds","text":"Surface Living UI role Chat transcript Diffed on reconnect \u2014 only new messages transferred Active task + todos Kept in sync across browser tabs Action cards Persist state across UI restarts Pending approvals Re-surface after reconnect Settings dirty-state Unsaved changes preserved"},{"location":"interfaces/living-ui/#enabling","title":"Enabling","text":"<p>Enabled by default in browser mode. Not used in CLI (stateless) or TUI (state is in-process). Configurable via the <code>living_ui_settings</code> module.</p>"},{"location":"interfaces/living-ui/#when-to-disable","title":"When to disable","text":"<ul> <li>You're running in an environment without a persistent WebSocket (e.g. serverless).</li> <li>You want minimum memory footprint (state store adds modest overhead).</li> <li>You're debugging a UI freeze and suspect living UI is the cause.</li> </ul>"},{"location":"interfaces/living-ui/#related","title":"Related","text":"<ul> <li>UI layer \u2014 the parent architecture</li> <li>Browser \u2014 primary consumer</li> <li>Event stream \u2014 the data source for diffs</li> </ul>"},{"location":"interfaces/tui/","title":"TUI mode","text":"<p>A rich terminal interface built on Textual. No Node.js required \u2014 just Python. Gets you a full-screen UI with panels, live action streams, scrollable history, and interactive settings.</p>"},{"location":"interfaces/tui/#quick-start","title":"Quick start","text":"<pre><code>python run.py --tui\n</code></pre>"},{"location":"interfaces/tui/#features","title":"Features","text":"<ul> <li>Live action panel \u2014 watch every action fire as the agent works.</li> <li>Chat panel \u2014 conversation transcript, updated live.</li> <li>Task panel \u2014 active task with todos and status.</li> <li>Settings tabs \u2014 integrations, MCP, skills, credentials, models (all the slash commands, but with forms).</li> <li>Keyboard-first \u2014 every action has a keybinding.</li> </ul>"},{"location":"interfaces/tui/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10+.</li> <li>An LLM API key.</li> <li>No Node.js.</li> <li>Textual (installed automatically by <code>python install.py</code>).</li> </ul>"},{"location":"interfaces/tui/#layout","title":"Layout","text":"<ul> <li><code>app/tui/interface.py</code> \u2014 main TUI adapter</li> <li><code>app/tui/app.py</code> \u2014 Textual app definition</li> <li><code>app/tui/widgets.py</code> \u2014 custom widgets</li> <li><code>app/tui/styles.py</code> \u2014 TCSS theme</li> <li><code>app/tui/onboarding/</code> \u2014 first-run wizard</li> </ul>"},{"location":"interfaces/tui/#settings-panels","title":"Settings panels","text":"<p>The TUI surfaces five settings panels that match the <code>/menu</code> command groups:</p> Panel Matches command File Credentials <code>/cred status</code> <code>credential_commands.py</code> Integrations <code>/integrations</code> <code>integration_settings.py</code> MCP servers <code>/mcp list</code> <code>mcp_settings.py</code> Skills <code>/skill list</code> <code>skill_settings.py</code> General settings <code>/menu</code> <code>settings.py</code>"},{"location":"interfaces/tui/#keyboard-shortcuts","title":"Keyboard shortcuts","text":"Key Action <code>Ctrl+C</code> Quit (clean shutdown) <code>Ctrl+L</code> Clear view <code>Tab</code> Focus next panel <code>Shift+Tab</code> Focus previous panel <code>\u2191</code> / <code>\u2193</code> in input Command history <code>F1</code> Help <p>Full list: press <code>F1</code> inside the TUI.</p>"},{"location":"interfaces/tui/#theme","title":"Theme","text":"<p>Coal + orange to match the rest of the CraftBot brand. Edit <code>app/tui/styles.py</code> to theme it yourself.</p>"},{"location":"interfaces/tui/#when-to-use-tui-over-browser","title":"When to use TUI over browser","text":"Use TUI when Use Browser when No Node.js available You want the richest UI SSH / remote shell Local or tunneled to localhost You prefer keyboard You prefer mouse + rich widgets Resource-constrained Desktop with browser handy"},{"location":"interfaces/tui/#related","title":"Related","text":"<ul> <li>CLI \u2014 even lighter weight</li> <li>Browser \u2014 richer alternative (needs Node.js)</li> <li>UI layer \u2014 the shared adapter layer</li> <li>Onboarding \u2014 first-run wizard</li> </ul>"},{"location":"interfaces/ui-layer/","title":"UI layer","text":"<p>The UI layer is the abstraction between the agent and the interface. It's how one agent can drive CLI, TUI, browser, and GUI modes without the agent knowing which one is active.</p>"},{"location":"interfaces/ui-layer/#beginner-mental-model","title":"Beginner mental model","text":"<p>Three pieces:</p> <ul> <li><code>UIController</code> \u2014 central orchestrator. Knows about the agent, the running task, and the active interface.</li> <li><code>InterfaceAdapter</code> (abstract) \u2014 one per interface. Renders chat, shows actions, displays the task panel, etc.</li> <li><code>EventBus</code> + <code>UIStateStore</code> \u2014 the reactive layer. Events flow from agent \u2192 UIController \u2192 adapter; state changes flow the other way.</li> </ul> <p>Swap the adapter, get a different interface. Every other layer stays identical.</p>"},{"location":"interfaces/ui-layer/#quick-start","title":"Quick start","text":"<pre><code>python run.py          # Browser adapter\npython run.py --tui    # TUI adapter\npython run.py --cli    # CLI adapter\n</code></pre> <p><code>run.py</code> picks the right <code>InterfaceAdapter</code> and hands it to <code>UIController</code>.</p>"},{"location":"interfaces/ui-layer/#layout","title":"Layout","text":"<pre><code>app/ui_layer/\n\u251c\u2500\u2500 controller/ui_controller.py    # UIController \u2014 the orchestrator\n\u251c\u2500\u2500 adapters/\n\u2502   \u251c\u2500\u2500 base.py                     # InterfaceAdapter \u2014 abstract protocol\n\u2502   \u251c\u2500\u2500 cli_adapter.py              # CLI-specific rendering\n\u2502   \u251c\u2500\u2500 tui_adapter.py              # Textual widgets\n\u2502   \u2514\u2500\u2500 browser_adapter.py          # WebSocket bridge\n\u251c\u2500\u2500 components/protocols.py         # ChatComponentProtocol, ActionPanelProtocol, \u2026\n\u251c\u2500\u2500 events/event_bus.py             # Pub/sub for UI updates\n\u251c\u2500\u2500 state/store.py                  # Reactive state store\n\u251c\u2500\u2500 commands/                       # Slash-command registry + builtin commands\n\u2502   \u251c\u2500\u2500 base.py\n\u2502   \u251c\u2500\u2500 executor.py\n\u2502   \u251c\u2500\u2500 registry.py\n\u2502   \u2514\u2500\u2500 builtin/                    # /help, /cred, /skill, /mcp, \u2026\n\u251c\u2500\u2500 settings/                        # Settings panel handlers\n\u251c\u2500\u2500 onboarding/                      # First-run wizard shared code\n\u251c\u2500\u2500 themes/                          # Cross-adapter theming\n\u251c\u2500\u2500 metrics/                         # Usage / performance metrics\n\u2514\u2500\u2500 local_llm_setup.py               # Helper for Ollama setup flow\n</code></pre>"},{"location":"interfaces/ui-layer/#component-protocols","title":"Component protocols","text":"<p>Adapters implement a fixed set of component protocols:</p> Protocol Renders <code>ChatComponentProtocol</code> Chat transcript, user + agent messages <code>ActionPanelProtocol</code> Live action cards (start / running / done) <code>TaskComponentProtocol</code> Active task summary + todos <code>SettingsPanelProtocol</code> Integrations / skills / MCP / models tabs <code>OnboardingProtocol</code> First-run wizard <code>CommandInputProtocol</code> The input box with slash-command handling <p>Each protocol is an abstract base; each adapter fills them in differently (ANSI strings for CLI, Textual widgets for TUI, WebSocket messages for browser).</p>"},{"location":"interfaces/ui-layer/#events","title":"Events","text":"<p>The UI layer bridges three directions:</p> <pre><code>Agent / event stream  \u2192  EventBus  \u2192  Adapter components  (render)\nAdapter UI events     \u2192  EventBus  \u2192  UIController         (intent)\nSettings changes      \u2192  UIStateStore                       (reactive)\n</code></pre> <p>Events are typed \u2014 <code>chat.message</code>, <code>action.start</code>, <code>action.end</code>, <code>task.updated</code>, <code>todo.transition</code>. Adding a new event is a one-liner in the bus and a handler per adapter.</p>"},{"location":"interfaces/ui-layer/#commands","title":"Commands","text":"<p>Slash-command dispatch (see Built-in commands) lives here because it's interface-agnostic. The <code>CommandRegistry</code> is populated at startup by importing every file in <code>commands/builtin/</code>. Third-party commands register by calling <code>register_command(...)</code>.</p>"},{"location":"interfaces/ui-layer/#state-store","title":"State store","text":"<p><code>UIStateStore</code> tracks things like:</p> <ul> <li>Current theme</li> <li>Active settings panel</li> <li>Agent status (idle / running / error)</li> <li>Pending approval prompts</li> </ul> <p>It's reactive \u2014 adapters subscribe and re-render on change.</p>"},{"location":"interfaces/ui-layer/#adding-a-new-interface","title":"Adding a new interface","text":"<p>See the \"UI layer\" section of Custom agent for a walkthrough. TL;DR:</p> <ol> <li>Subclass <code>InterfaceAdapter</code>.</li> <li>Implement each component protocol.</li> <li>Register your adapter in <code>run.py</code>'s dispatcher.</li> <li>Add a CLI flag to <code>run.py</code>.</li> </ol>"},{"location":"interfaces/ui-layer/#related","title":"Related","text":"<ul> <li>CLI, TUI, Browser, GUI \u2014 the four adapters</li> <li>Living UI \u2014 the reactive layer under the state store</li> <li>Commands \u2014 dispatch lives here</li> <li>Event stream \u2014 the source of render events</li> </ul>"},{"location":"modes/","title":"Task modes","text":"<p>CraftBot runs tasks in one of four modes. The agent picks the mode automatically based on the task, but you can override with directives. Each mode is a different variant of the agent loop.</p> <ul> <li> <p> Simple task</p> <p>Linear todo list, run to completion. Best for short, well-defined requests.</p> </li> <li> <p> Complex task</p> <p>Iterative action loop with dynamic todos. For multi-step, open-ended work.</p> </li> <li> <p> Special workflows</p> <p>Named workflows for memory processing, proactive handling, GUI control.</p> </li> <li> <p> Proactive</p> <p>Heartbeat, planner, and scheduler \u2014 the agent initiates tasks on its own.</p> </li> </ul>"},{"location":"modes/#which-mode-runs","title":"Which mode runs?","text":"Signal Mode One-shot question or quick action Simple task \"Research X, write a report, send it to Bob\" Complex task Incoming event, midnight memory distillation, GUI-required task Special workflow Idle tick, scheduled event, learned preference Proactive"},{"location":"modes/#related","title":"Related","text":"<ul> <li>Agent loop \u2014 the primitive every mode extends</li> <li>Triggers \u2014 what starts a task in each mode</li> <li>Actions \u2014 the vocabulary used in every mode</li> </ul>"},{"location":"modes/complex-task/","title":"Complex task mode","text":"<p>Complex task mode is the planning variant of the agent loop. The agent breaks the work into a todo list, iterates through it, and updates its own progress \u2014 one action beat per todo, with room to revise the plan as it learns. This is how CraftBot handles multi-step work like \"research X, write a summary, DM it to Bob.\"</p>"},{"location":"modes/complex-task/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>A task is created with <code>mode: \"complex\"</code>.</li> <li>The first iteration populates <code>todos: [TodoItem, ...]</code> via the <code>task_update_todos</code> action.</li> <li>Each subsequent iteration picks the current in-progress todo (or the next pending one), selects an action to advance it, and either marks it completed or updates the plan.</li> <li>The task completes when all todos are <code>completed</code> and the agent calls <code>task_complete</code>.</li> </ul> <p>The agent owns the plan. You can see it at any time in the UI or in the task's <code>todos</code> field.</p>"},{"location":"modes/complex-task/#inspect-it-now","title":"Inspect it now","text":"<p>Watch the workflow transition and todo updates:</p> <pre><code>tail -f logs/*.log | grep -E \"COMPLEX TASK|task_update_todos\"\n</code></pre>"},{"location":"modes/complex-task/#example","title":"Example","text":"<p>You: \"Research Q2 earnings, compare to Q1, DM Bob the summary\"</p> <pre><code>[WORKFLOW: COMPLEX TASK] Query: Research Q2 earnings, ...\n[PARALLEL] Complex task mode selected 1 action(s): ['task_update_todos']\n[ACTION] task_update_todos -&gt; ok (4 todos set)\n  [&gt;] Fetch Q2 earnings report\n  [ ] Fetch Q1 earnings for comparison\n  [ ] Draft summary with key deltas\n  [ ] DM Bob on Slack\n\n[WORKFLOW: COMPLEX TASK] Query: current todo: Fetch Q2 earnings report\n[PARALLEL] Complex task mode selected 1 action(s): ['web_search']\n[ACTION] web_search -&gt; ok (5 results)\n[ACTION] task_update_todos -&gt; ok (todo 1 completed, todo 2 in-progress)\n...\n</code></pre>"},{"location":"modes/complex-task/#todoitem","title":"TodoItem","text":"<p>The unit of plan. Each is a small dataclass:</p> <pre><code>@dataclass\nclass TodoItem:\n    content: str                                    # Imperative: \"Run tests\"\n    status: Literal[\"pending\", \"in_progress\", \"completed\"]\n    active_form: Optional[str]                      # Present-continuous: \"Running tests\"\n    id: str                                         # UUID\n</code></pre> <p>Exactly one todo should be <code>in_progress</code> at any time. The agent is trained to mark a todo <code>in_progress</code> when it starts, and <code>completed</code> as soon as it finishes \u2014 no batching.</p>"},{"location":"modes/complex-task/#the-action-beat","title":"The action beat","text":"<p>Same four phases as simple task, but with <code>SELECT_ACTION_IN_TASK_PROMPT</code> which teaches the agent to:</p> <ol> <li>Look at the current todo</li> <li>Pick an action that advances it</li> <li>Possibly update todos in the same turn (parallel action)</li> </ol>"},{"location":"modes/complex-task/#current-todo-selection","title":"Current-todo selection","text":"<p><code>Task.get_current_todo()</code> picks the next todo to work on:</p> <ol> <li>Prefer <code>in_progress</code> \u2014 continue where you left off</li> <li>Fallback to first <code>pending</code> \u2014 if no in-progress exists</li> <li>Return None \u2014 all complete \u2192 agent should call <code>task_complete</code></li> </ol>"},{"location":"modes/complex-task/#plan-revision","title":"Plan revision","text":"<p>The agent can call <code>task_update_todos</code> at any time to:</p> <ul> <li>Add new todos (the plan got bigger)</li> <li>Reorder pending todos (priorities changed)</li> <li>Remove obsolete todos (the user pivoted)</li> <li>Edit content of a todo (clearer wording)</li> </ul> <p>This is how an agent that hits an obstacle adapts \u2014 e.g. \"I can't find the Q2 report\" \u2192 adds <code>Check our shared drive for Q2 report</code> as a new todo.</p>"},{"location":"modes/complex-task/#session-caching","title":"Session caching","text":"<p>Complex tasks benefit most from session-level prompt caching. Because the iteration loop can span dozens of turns, reusing the cached prefix is dramatic \u2014 10-20\u00d7 cost reduction over non-cached calls on supporting providers (Anthropic, Gemini, BytePlus). See Context engine.</p>"},{"location":"modes/complex-task/#loop-safety","title":"Loop safety","text":"<p>Two guards prevent runaway tasks:</p> Limit Default Where <code>MAX_ACTIONS_PER_TASK</code> <code>500</code> <code>app/config.py</code> <code>MAX_TOKEN_PER_TASK</code> <code>12_000_000</code> <code>app/config.py</code> <p>Hitting either limit ends the task with an error.</p>"},{"location":"modes/complex-task/#promoting-from-simple-complex","title":"Promoting from simple \u2192 complex","text":"<p>Mid-task, if the agent calls <code>task_update_todos</code> on a simple-mode task, the task is promoted to complex. This is the most common way complex tasks get created \u2014 not upfront, but on demand when the agent realises the work needs planning.</p>"},{"location":"modes/complex-task/#demoting-complex-simple","title":"Demoting complex \u2192 simple","text":"<p>Not supported. Once a task has todos, it stays complex until completion.</p>"},{"location":"modes/complex-task/#related","title":"Related","text":"<ul> <li>Simple task \u2014 when no todos needed</li> <li>Task sessions \u2014 the underlying data model</li> <li>Skill &amp; action selection \u2014 the <code>SELECT_ACTION_IN_TASK_PROMPT</code></li> <li>Agent loop \u2014 the shared cycle</li> </ul>"},{"location":"modes/proactive/","title":"Proactive","text":"<p>Proactive mode lets CraftBot act on its own \u2014 reminding you, checking inboxes, drafting summaries, running audits \u2014 without you asking. It's built from three pieces: a heartbeat that fires every 30 minutes, a planner that updates the plan periodically, and a scheduler that fires cron-style.</p>"},{"location":"modes/proactive/#the-core-idea","title":"The core idea","text":"<p>Proactive behaviour is driven by two files:</p> <ul> <li><code>agent_file_system/PROACTIVE.md</code> \u2014 human-readable YAML-in-markdown listing recurring tasks (hourly/daily/weekly/monthly). Edited by you or the planner.</li> <li><code>app/config/scheduler_config.json</code> \u2014 the scheduler's registry of when the agent wakes up (heartbeat, planner, one-off schedules).</li> </ul> <p>Every 30 minutes the heartbeat fires, reads PROACTIVE.md, finds due tasks, and executes them. Once a day/week/month, the planner fires and updates PROACTIVE.md based on what happened recently.</p>"},{"location":"modes/proactive/#scenarios","title":"Scenarios","text":""},{"location":"modes/proactive/#1-workstation-assistant-the-default","title":"1) Workstation assistant (the default)","text":"<p>Heartbeat every 30 min + daily planner. The agent runs on your laptop; schedules only fire when it's running. Best for a personal assistant you interact with daily.</p>"},{"location":"modes/proactive/#2-always-on-server","title":"2) Always-on server","text":"<p>Same config but on a persistent machine (VPS, home server) via service mode. Heartbeats fire reliably 24/7, so <code>daily</code> tasks fire even when you're not using the agent.</p>"},{"location":"modes/proactive/#3-bounded-proactive-eg-only-work-hours","title":"3) Bounded proactive (e.g. only work hours)","text":"<p>Add <code>weekdays_only</code> and <code>market_hours_only</code> conditions to tasks in PROACTIVE.md. Heartbeat still fires every 30 min, but conditions gate which tasks actually run.</p>"},{"location":"modes/proactive/#command-flow","title":"Command flow","text":"<pre><code>                  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                  \u2502 scheduler_config   \u2502\n                  \u2502 (every 30min)      \u2502\n                  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                             \u2193 fires trigger (proactive_heartbeat)\n                  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                  \u2502 _handle_proactive_workflow\u2502\n                  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                             \u2193\n              reads PROACTIVE.md, collects due tasks\n                             \u2193\n        creates one \"Heartbeat\" simple-mode task (skill: heartbeat-processor)\n                             \u2193\n                   queued to trigger queue\n                             \u2193\n                   agent loop runs the task\n                             \u2193\n           task executes due proactive tasks, updates outcome_history\n</code></pre> <p>The planner follows the same flow with <code>proactive_planner</code> trigger type and a <code>{scope}-planner</code> skill.</p>"},{"location":"modes/proactive/#proactivemd-anatomy","title":"PROACTIVE.md anatomy","text":"<p><pre><code>### [DAILY] Morning inbox summary\n```yaml\nid: morning_inbox_summary\nfrequency: daily\ntime: \"08:30\"\nenabled: true\npriority: 50\npermission_tier: 1\nrun_count: 12\nconditions:\n  - weekdays_only\ninstruction: |\n  1. Connect via Gmail integration.\n  2. Fetch unread emails from last 24 hours.\n  3. Filter for emails from contacts in CONTACTS.md.\n  4. Compile summary with Urgent / Important / FYI sections.\n  5. Send summary as chat message.\noutcome_history:\n  - timestamp: 2026-04-22T08:30:12\n    success: true\n    result: \"Summarized 4 emails (1 urgent, 2 important, 1 FYI)\"\n</code></pre> <pre><code>Fields:\n\n| Field | Required | Options |\n|---|---|---|\n| `id` | yes | snake_case unique identifier |\n| `frequency` | yes | `hourly` / `daily` / `weekly` / `monthly` |\n| `time` | recommended | `HH:MM` 24-hour |\n| `day` | for weekly/monthly | weekday (`monday`-`sunday`) OR date (`1`-`31`) |\n| `enabled` | yes | `true` / `false` |\n| `priority` | yes | `1` (high) - `100` (low) |\n| `permission_tier` | yes | `0` Silent / `1` Notify / `2` Approval / `3` High-risk |\n| `conditions` | no | `market_hours_only`, `user_available`, `weekdays_only`, custom |\n| `instruction` | yes | Multi-line detailed task spec |\n\n## Decision rubric (proactive scoring)\n\nPROACTIVE.md ships with a five-dimension rubric the planner uses when proposing new tasks:\n\n| Dimension | 1 (Low) | 5 (High) |\n|---|---|---|\n| **Impact** | Negligible | Critical |\n| **Risk** | High risk | No risk |\n| **Cost** | Very high | Negligible |\n| **Urgency** | Not urgent | Immediate |\n| **Confidence** | Unlikely | Certain |\n\nScoring thresholds:\n\n- **18+** \u2014 Strong candidate, execute\n- **13\u201317** \u2014 May need user input first, consider asking\n- **&lt;13** \u2014 Skip or defer\n\n## Permission tiers\n\nHow a task interacts with the user before / during / after:\n\n| Tier | Level | Meaning |\n|---|---|---|\n| 0 | Silent | Searching, drafting, internal ops \u2014 no notification |\n| 1 | Notify | Inform user of execution and findings \u2014 no wait |\n| 2 | Approval | Ask for approval before proceeding |\n| 3 | High-risk | Explicit detailed approval required (email external parties, change configs) |\n\nUser-created proactive tasks are typically tier 0 or 1. System-level or consequential tasks should be tier 2 or 3.\n\n## Heartbeat timing\n\n- Heartbeats fire at `:00` and `:30` every hour (clock-aligned).\n- Tasks with a `time` field have a **30-minute grace period** after their target time \u2014 miss it, and the task skips until the next window (no catch-up runs).\n- `hourly` tasks fire on **every** heartbeat regardless of `time`.\n\nSee `RecurringTask.should_run()` in [`app/proactive/types.py`](../concepts/agent-file-system.md) for the exact logic.\n\n## Scheduler expressions\n\nThe scheduler supports five schedule types:\n\n| Type | Example raw expression |\n|---|---|\n| `daily` | `\"every day at 07:00\"` |\n| `weekly` | `\"every monday at 09:00\"` |\n| `interval` | `\"every 30 minutes\"` (this is how heartbeat is registered) |\n| `cron` | `\"0 3 * * *\"` (3 AM daily \u2014 memory distillation) |\n| `once` | `\"at 2026-05-01T12:00:00Z\"` |\n\nSchedules live in [`app/config/scheduler_config.json`](../configuration/config-json.md) and are hot-reloaded.\n\n## Master switch\n\n```json\n// settings.json\n{ \"proactive\": { \"enabled\": true } }\n</code></pre></p> <p>Disabling this:</p> <ul> <li>Heartbeat triggers log and skip \u2014 no tasks are created</li> <li>Planner triggers log and skip</li> <li>Memory distillation is not affected (its own toggle)</li> <li>One-off scheduled tasks still fire (they're scheduler-driven, not proactive-driven)</li> </ul>"},{"location":"modes/proactive/#outcome-history","title":"Outcome history","text":"<p>Each <code>RecurringTask</code> keeps the last 5 outcomes in <code>outcome_history</code> \u2014 timestamp, result summary, success flag. The planner reads these to decide whether a task is worth keeping, tuning, or retiring.</p>"},{"location":"modes/proactive/#security-rules","title":"Security rules","text":"<p>Don't remove HTML comment markers</p> <p><code>PROACTIVE.md</code> uses markers like <code>&lt;!-- PROACTIVE_TASKS_START --&gt;</code> that the parser depends on. Edit the YAML inside; don't touch the markers.</p> <ul> <li>Tier 2/3 tasks require explicit approval in the UI before each execution \u2014 don't rely on proactive-only tier 2+ tasks to run unattended.</li> <li>The agent can edit PROACTIVE.md itself via the planner \u2014 review its edits before enabling new tasks.</li> <li>Conditions are evaluated in-process; custom conditions that call external services should handle network failures gracefully.</li> </ul>"},{"location":"modes/proactive/#related","title":"Related","text":"<ul> <li>Special workflows \u2014 how proactive triggers are routed</li> <li>Agent file system \u2014 where PROACTIVE.md lives</li> <li>Memory \u2014 the other scheduled workflow</li> <li>Service mode \u2014 running the agent persistently for proactive</li> <li><code>settings.json</code> \u2014 <code>proactive.enabled</code> master switch</li> </ul>"},{"location":"modes/simple-task/","title":"Simple task mode","text":"<p>Simple task mode is the streamlined variant of the agent loop. The agent picks one (or a few parallel) actions, runs them, returns the result \u2014 no todos, no planning, no loops. Best for one-shots and quick lookups.</p>"},{"location":"modes/simple-task/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>A task is created with <code>mode: \"simple\"</code>.</li> <li>The agent enters <code>_handle_simple_task_workflow</code>, which runs one action beat (<code>select \u2192 prepare \u2192 execute \u2192 finalize</code>) and exits.</li> <li>If the agent needs another turn, a follow-up trigger is queued and another simple-task iteration runs.</li> <li>Todos exist on the Task dataclass but are never auto-populated \u2014 simple tasks usually finish before they'd be useful.</li> </ul>"},{"location":"modes/simple-task/#inspect-it-now","title":"Inspect it now","text":"<p>The workflow logs a single line when it enters:</p> <pre><code>tail -f logs/*.log | grep \"WORKFLOW: SIMPLE TASK\"\n</code></pre>"},{"location":"modes/simple-task/#example","title":"Example","text":"<p>You: \"send a Slack DM to Bob saying I'm running late\"</p> <pre><code>[WORKFLOW: CONVERSATION] Query: send a Slack DM to Bob ...\n[PARALLEL] Conversation mode selected 1 action(s): ['task_start']\n[TASK] Created task abc123 mode=simple action_sets=[core]\n[WORKFLOW: SIMPLE TASK] Query: send a Slack DM to Bob ...\n[PARALLEL] Simple task mode selected 1 action(s): ['send_slack_message']\n[ACTION] send_slack_message -&gt; ok (channel=D0123, ts=1713...)\n[TASK] abc123 completed\n</code></pre> <p>One task. One action. Done.</p>"},{"location":"modes/simple-task/#when-simple-mode-is-chosen","title":"When simple mode is chosen","text":"<p>The LLM picks <code>mode: \"simple\"</code> when creating the task via <code>task_start</code>. The decision is shaped by <code>SELECT_ACTION_PROMPT</code>, which trains the model toward:</p> <ul> <li>Single-request operations (<code>\"send X\"</code>, <code>\"look up Y\"</code>, <code>\"what's the weather\"</code>)</li> <li>Clear-cut integrations (<code>\"DM Alice on Telegram\"</code>)</li> <li>Small, well-scoped follow-ups (<code>\"also email her the Zoom link\"</code>)</li> </ul> <p>If the request is multi-step, research-heavy, or needs verification, the LLM picks <code>complex</code> instead.</p>"},{"location":"modes/simple-task/#the-action-beat","title":"The action beat","text":"<p>Simple-task iterations use the same four-phase beat as complex task:</p> <ol> <li><code>_select_action(trigger_data)</code> with <code>SELECT_ACTION_IN_SIMPLE_TASK_PROMPT</code></li> <li><code>_retrieve_and_prepare_actions</code></li> <li><code>_execute_actions</code></li> <li><code>_finalize_action_execution</code></li> </ol> <p>The difference is the prompt \u2014 simple mode's prompt omits todo management instructions, keeping the LLM focused on \"pick an action, run it, return the result.\"</p>"},{"location":"modes/simple-task/#session-caching","title":"Session caching","text":"<p>Simple tasks benefit from session-level prompt caching via the context engine. Each iteration only appends the new events delta to the cached prefix, not the full history. This keeps per-call cost low even across multiple turns.</p>"},{"location":"modes/simple-task/#completion","title":"Completion","text":"<p>A simple task completes when the agent calls <code>task_complete</code> \u2014 typically the second action picked after the work one (e.g. <code>send_slack_message</code> then <code>task_complete</code>). The agent can also end with a <code>send_message</code> reply to the user and skip <code>task_complete</code>, which transitions the session back to conversation mode.</p>"},{"location":"modes/simple-task/#promoting-to-complex","title":"Promoting to complex","text":"<p>If mid-task the agent realises the work is deeper than expected, it can call <code>task_update_todos</code> with a todo list \u2014 this effectively promotes the task to complex mode for subsequent iterations. The task's <code>mode</code> field is updated.</p>"},{"location":"modes/simple-task/#knobs","title":"Knobs","text":"<p>Simple task mode has no dedicated knobs beyond the task-level settings:</p> <ul> <li><code>action_sets</code> \u2014 scope of available actions</li> <li><code>selected_skills</code> \u2014 prompt fragments injected per iteration</li> <li><code>waiting_for_user_reply</code> \u2014 pauses trigger scheduling</li> </ul> <p>See Task sessions for the full field list.</p>"},{"location":"modes/simple-task/#related","title":"Related","text":"<ul> <li>Complex task \u2014 when you need todos + planning</li> <li>Agent loop \u2014 the shared cycle</li> <li>Actions \u2014 the vocabulary used in every iteration</li> </ul>"},{"location":"modes/special-workflows/","title":"Special workflows","text":"<p>\"Special workflows\" are the agent-loop branches that don't start from a user message: memory distillation, proactive processing, and GUI task execution. They get their own <code>payload.type</code> on the trigger, short-circuit the normal routing, and run on schedules or events rather than chat.</p>"},{"location":"modes/special-workflows/#beginner-mental-model","title":"Beginner mental model","text":"<p>Three workflow handlers live alongside the user-facing ones (conversation, simple task, complex task):</p> <ul> <li><code>_handle_memory_workflow</code> \u2014 runs when a trigger has <code>payload.type == \"memory_processing\"</code></li> <li><code>_handle_proactive_workflow</code> \u2014 runs when a trigger has <code>payload.type in (\"proactive_heartbeat\", \"proactive_planner\")</code></li> <li><code>_handle_gui_task_workflow</code> \u2014 runs when a task is complex AND <code>STATE.gui_mode == True</code></li> </ul> <p>All three create a regular task (simple or complex), queue a trigger for it, and let the normal loop do the work. They're just ways to start tasks automatically.</p>"},{"location":"modes/special-workflows/#inspect-it-now","title":"Inspect it now","text":"<pre><code>tail -f logs/*.log | grep -E \"MEMORY|PROACTIVE|GUI MODE\"\n</code></pre>"},{"location":"modes/special-workflows/#memory-workflow","title":"Memory workflow","text":"<p>Triggered by a scheduled memory-processing task (default 3 AM via <code>MEMORY_PROCESSING_SCHEDULE_HOUR</code>):</p> <ol> <li>Checks <code>memory.enabled</code> in settings.json. If off, skip.</li> <li>Creates a simple-mode task with the <code>memory-processor</code> skill, action sets <code>file_operations</code>, <code>memory</code>.</li> <li>Queues a trigger for that task.</li> <li>The task reads <code>EVENT_UNPROCESSED.md</code>, scores each event, writes keepers to <code>MEMORY.md</code>, clears the unprocessed file.</li> </ol> <p>See Memory for the distillation semantics.</p>"},{"location":"modes/special-workflows/#proactive-workflow","title":"Proactive workflow","text":"<p>Two variants, both firing from the scheduler:</p>"},{"location":"modes/special-workflows/#heartbeat","title":"Heartbeat","text":"<p>Fires every 30 minutes (<code>:00</code> and <code>:30</code>):</p> <ol> <li>Checks <code>proactive.enabled</code>. If off, skip.</li> <li>Collects all due tasks across every frequency (hourly, daily, weekly, monthly) via <code>proactive_manager.get_all_due_tasks()</code>.</li> <li>Creates one unified simple-mode task named <code>Heartbeat</code> with the <code>heartbeat-processor</code> skill.</li> <li>Task instruction summarizes what's due: <code>\"Execute all due proactive tasks from PROACTIVE.md. Due tasks: 2 daily, 1 weekly (3 total).\"</code></li> </ol>"},{"location":"modes/special-workflows/#planner","title":"Planner","text":"<p>Fires daily / weekly / monthly depending on scope:</p> <ol> <li>Creates a simple-mode task named <code>Day Planner</code> / <code>Week Planner</code> / <code>Month Planner</code>.</li> <li>Loads the <code>{scope}-planner</code> skill (<code>day-planner</code>, <code>week-planner</code>, <code>month-planner</code>).</li> <li>Instruction: review recent interactions, update PROACTIVE.md planner section with findings.</li> </ol> <p>See Proactive for the complete model.</p>"},{"location":"modes/special-workflows/#gui-workflow","title":"GUI workflow","text":"<p>Triggered when a complex task is already running AND GUI mode is enabled (settings.json \u2192 <code>gui.enabled</code>):</p> <ol> <li>Each iteration calls <code>GUIHandler.gui_module.perform_gui_task_step(...)</code>.</li> <li>The GUI module takes a screenshot, sends it to the VLM, and returns a structured action.</li> <li>Actions are pixel-level: click, type, drag, scroll, screenshot.</li> <li>The action is executed via <code>pyautogui</code> (or the equivalent platform library).</li> <li>Events are logged into the main event stream so the task can iterate.</li> </ol> <p>See GUI / Vision for requirements and setup.</p>"},{"location":"modes/special-workflows/#trigger-type-routing-table","title":"Trigger type routing table","text":"<code>payload.type</code> Workflow Skip if <code>\"memory_processing\"</code> Memory <code>memory.enabled == false</code> <code>\"proactive_heartbeat\"</code> Proactive heartbeat <code>proactive.enabled == false</code> OR no due tasks <code>\"proactive_planner\"</code> Proactive planner <code>proactive.enabled == false</code> <code>\"task_execution\"</code> / <code>\"scheduled\"</code> / (unset) Conversation/simple/complex (normal flow) \u2014 (implicit \u2014 based on STATE.gui_mode) GUI task <code>gui.enabled == false</code>"},{"location":"modes/special-workflows/#skills-used-by-special-workflows","title":"Skills used by special workflows","text":"Skill Workflow Purpose <code>memory-processor</code> Memory Step-by-step distillation of EVENT_UNPROCESSED.md <code>heartbeat-processor</code> Proactive heartbeat Read PROACTIVE.md, filter by time/day, execute <code>day-planner</code> / <code>week-planner</code> / <code>month-planner</code> Proactive planner Update PROACTIVE.md plan section <p>These skills ship bundled. If you're writing a custom agent, you can override or replace them.</p>"},{"location":"modes/special-workflows/#adding-your-own-special-workflow","title":"Adding your own special workflow","text":"<p>Two ways:</p> <ol> <li>Reuse triggers \u2014 post a trigger with your own <code>payload.type</code>, and inject handler logic in an agent subclass's <code>react()</code> override.</li> <li>Use the scheduler \u2014 add a <code>ScheduledTask</code> to <code>scheduler_config.json</code> that creates tasks on a cron schedule. Simpler and doesn't require a subclass.</li> </ol>"},{"location":"modes/special-workflows/#related","title":"Related","text":"<ul> <li>Proactive \u2014 the biggest producer of special triggers</li> <li>Memory \u2014 consumer of the memory workflow</li> <li>GUI / Vision \u2014 consumer of the GUI workflow</li> <li>Agent loop \u2014 the dispatch logic</li> </ul>"},{"location":"providers/","title":"Providers","text":"<p>CraftBot talks to two kinds of models:</p> <ul> <li>LLM providers \u2014 for reasoning, planning, and tool selection</li> <li>VLM providers \u2014 for screen understanding in GUI mode</li> </ul> <p>Both are Bring-Your-Own-Key. Switch providers from the onboarding wizard or <code>/provider</code> command.</p> <ul> <li> <p> LLM providers</p> <p>OpenAI, Google Gemini, Anthropic, BytePlus, Ollama.</p> </li> <li> <p> VLM providers</p> <p>Vision models for GUI/desktop automation.</p> </li> </ul>"},{"location":"providers/#related","title":"Related","text":"<ul> <li>Environment variables \u2014 API key names for each provider</li> <li>GUI / Vision \u2014 how VLMs plug into desktop automation</li> <li>Context engine \u2014 prompt caching strategies per provider</li> </ul>"},{"location":"providers/llm/","title":"LLM providers","text":"<p>CraftBot talks to large language models through a single <code>LLMInterface</code>. Five providers are wired in; switching is one setting change.</p>"},{"location":"providers/llm/#quick-start","title":"Quick start","text":"<p>Put a key in your environment and pick a provider:</p> <pre><code># .env\nANTHROPIC_API_KEY=sk-ant-api03-...\n</code></pre> <pre><code>// app/config/settings.json\n{ \"model\": { \"llm_provider\": \"anthropic\" } }\n</code></pre> <p>Restart the agent. Done.</p>"},{"location":"providers/llm/#supported-providers","title":"Supported providers","text":"Provider <code>llm_provider</code> value Key env var Default model Anthropic Claude <code>anthropic</code> <code>ANTHROPIC_API_KEY</code> Latest Sonnet OpenAI <code>openai</code> <code>OPENAI_API_KEY</code> <code>gpt-4o</code> family Google Gemini <code>google</code> / <code>gemini</code> <code>GOOGLE_API_KEY</code> Latest Gemini BytePlus (Volc Engine) <code>byteplus</code> <code>BYTEPLUS_API_KEY</code> Provider-configured Ollama (local / remote) <code>remote</code> \u2014 (uses <code>REMOTE_MODEL_URL</code>) Whatever the server serves"},{"location":"providers/llm/#switching-providers","title":"Switching providers","text":"<p>Three ways, in order of preference:</p> <ol> <li>Onboarding re-run \u2014 <code>/menu \u2192 Settings \u2192 Model</code> writes to settings.json atomically.</li> <li><code>/provider &lt;name&gt;</code> command \u2014 changes the active provider for the session.</li> <li>Hand-edit settings.json \u2014 change <code>model.llm_provider</code> and restart.</li> </ol>"},{"location":"providers/llm/#model-override","title":"Model override","text":"<p>By default each provider picks its own recommended model. Override with <code>model.llm_model</code>:</p> <pre><code>{ \"model\": { \"llm_provider\": \"openai\", \"llm_model\": \"gpt-4o-mini\" } }\n</code></pre>"},{"location":"providers/llm/#prompt-caching-support","title":"Prompt caching support","text":"<p>Providers differ on prompt caching \u2014 the single biggest cost/latency lever:</p> Provider Prefix cache Session cache (delta) Anthropic Yes (native) Yes (via delta appends) Google Gemini Yes (explicit) Yes BytePlus Yes (explicit) Yes OpenAI Limited (implicit only) No Ollama Depends on server No <p>See Context engine for how CraftBot exploits this.</p>"},{"location":"providers/llm/#slow-mode","title":"Slow mode","text":"<p>Providers with strict per-minute quotas (e.g. BytePlus) can hit rate limits. Enable slow mode:</p> <pre><code>{ \"model\": { \"slow_mode\": true, \"slow_mode_tpm_limit\": 30000 } }\n</code></pre> <p>Requests queue to stay under the TPM limit.</p>"},{"location":"providers/llm/#ollama-remote-servers","title":"Ollama / remote servers","text":"<p>Set <code>REMOTE_MODEL_URL</code> to point at your Ollama instance:</p> <pre><code># .env\nREMOTE_MODEL_URL=http://192.168.1.10:11434\n</code></pre> <pre><code>{ \"model\": { \"llm_provider\": \"remote\", \"llm_model\": \"llama3.1:70b\" } }\n</code></pre> <p>Any OpenAI-compatible <code>/v1/chat/completions</code> endpoint should work.</p>"},{"location":"providers/llm/#connection-test","title":"Connection test","text":"<p>The agent tests provider connectivity at startup. Per-provider test models live in <code>app/config/connection_test_models.json</code>. If the test fails, the agent logs the error and falls back to whatever else is configured.</p>"},{"location":"providers/llm/#related","title":"Related","text":"<ul> <li>VLM providers \u2014 vision-language models for GUI mode</li> <li>Environment variables \u2014 the full env-var list</li> <li>settings.json \u2014 schema for <code>model.*</code></li> <li>Context engine \u2014 how prompts are built and cached</li> </ul>"},{"location":"providers/vlm/","title":"VLM providers","text":"<p>Vision-language models (VLMs) let CraftBot see the screen in GUI mode. The agent sends a screenshot, asks \"where should I click?\", and the VLM returns pixel coordinates, a reason, or a structured action.</p>"},{"location":"providers/vlm/#quick-start","title":"Quick start","text":"<p>Pick a VLM provider in settings.json:</p> <pre><code>{ \"model\": { \"vlm_provider\": \"anthropic\", \"vlm_model\": null } }\n</code></pre> <p>Set <code>null</code> to use the provider's default. VLM inherits from <code>llm_provider</code> if <code>vlm_provider</code> is unset.</p>"},{"location":"providers/vlm/#supported-providers","title":"Supported providers","text":"<p>Same roster as LLM providers \u2014 VLMs use the same API keys:</p> Provider <code>vlm_provider</code> value Notes Anthropic Claude <code>anthropic</code> Sonnet/Opus with vision OpenAI <code>openai</code> GPT-4o family Google Gemini <code>google</code> / <code>gemini</code> Gemini 1.5 Pro / 2.x with vision BytePlus <code>byteplus</code> Provider-dependent Ollama (remote) <code>remote</code> Any vision model the server serves (e.g. llava)"},{"location":"providers/vlm/#omniparser-specialized-pipeline","title":"OmniParser (specialized pipeline)","text":"<p>For dense UI screenshots, a dedicated VLM often under-performs a combination of detector + VLM. CraftBot supports OmniParser as an optional front-end:</p> <pre><code>{\n  \"gui\": {\n    \"enabled\": true,\n    \"use_omniparser\": true,\n    \"omniparser_url\": \"http://127.0.0.1:7861\"\n  }\n}\n</code></pre> <p>OmniParser runs separately as a Gradio server and returns a structured element list (buttons, labels, icons with bounding boxes). The VLM then operates on that structured data instead of raw pixels \u2014 more accurate on complex UIs.</p> <p>Install via <code>python install.py --gui</code>. See GUI / Vision for full setup.</p>"},{"location":"providers/vlm/#prompt-families","title":"Prompt families","text":"<p>GUI mode uses specialized prompts from the registry:</p> Prompt When <code>GUI_REASONING_PROMPT</code> Raw pixel VLM \u2014 reason about what's on screen <code>GUI_REASONING_PROMPT_OMNIPARSER</code> OmniParser path \u2014 reason over parsed elements <code>GUI_QUERY_FOCUSED_PROMPT</code> Focused query mode \u2014 \"find the Submit button\" <code>GUI_PIXEL_POSITION_PROMPT</code> Pixel coordinate estimation <code>GUI_ACTION_SPACE_PROMPT</code> Lists available GUI actions (click, type, drag, scroll, screenshot) <p>Override any of these via <code>register_prompt(...)</code> \u2014 see Prompts.</p>"},{"location":"providers/vlm/#vlm-vs-llm-costs","title":"VLM vs LLM costs","text":"<p>VLMs cost more per call than text-only LLMs (images are token-heavy). In GUI mode every step is a VLM call, so runtime can be expensive. Mitigations:</p> <ul> <li>Enable OmniParser \u2014 the VLM gets text, not pixels, much cheaper.</li> <li>Set a smaller VLM (e.g. <code>gpt-4o-mini</code> or Haiku) for routine clicks; escalate to larger models only for tricky screens.</li> <li>Keep GUI tasks short \u2014 break long workflows into CLI-mode tasks where possible.</li> </ul>"},{"location":"providers/vlm/#related","title":"Related","text":"<ul> <li>GUI / Vision \u2014 the consumer of VLMs</li> <li>LLM providers \u2014 same providers, text mode</li> <li>settings.json <code>gui.*</code> \u2014 the toggle and OmniParser URL</li> </ul>"},{"location":"reference/","title":"Reference","text":"<p>Lookup tables. Catalogues. Schemas. Read this when you need to look something up, not when you're learning.</p> <ul> <li> <p> Actions catalogue</p> <p>Every built-in action, grouped by domain.</p> </li> <li> <p> Event types</p> <p>Every event the event stream can emit.</p> </li> <li> <p> Agent MD files</p> <p>Schema and purpose of each file under <code>agent_file_system/</code>.</p> </li> </ul>"},{"location":"reference/#related","title":"Related","text":"<ul> <li>Actions \u2014 concept + mental model</li> <li>Event stream \u2014 concept + how to subscribe</li> <li>Agent file system \u2014 concept + how it's used</li> </ul>"},{"location":"reference/actions/","title":"Actions catalogue","text":"<p>Every built-in action grouped by domain. For detailed schemas, inspect the source in <code>app/data/action/</code> \u2014 each file is one action with its full <code>@action(...)</code> metadata.</p> <p>For how actions fit the architecture, see the Actions concept.</p>"},{"location":"reference/actions/#core-task-management","title":"Core task management","text":"Action Purpose <code>task_start</code> Create a new task session (simple or complex) <code>task_update_todos</code> Add / reorder / edit todos in a complex task <code>task_end</code> Mark the task complete <code>set_mode</code> Switch task mode mid-flight (e.g. simple \u2192 complex) <code>send_message</code> Reply to the user <code>send_message_with_attachment</code> Reply with a file attached <code>ignore</code> Skip this trigger without doing anything (used in conversation mode) <code>wait</code> Sleep for N seconds before the next action"},{"location":"reference/actions/#file-operations-file_operations","title":"File operations (<code>file_operations</code>)","text":"Action Purpose <code>read_file</code> Paginated read with offset/limit <code>write_file</code> Create/overwrite a file <code>stream_edit</code> Line-range edits <code>stream_read</code> Line-by-line read for large files <code>grep_files</code> ripgrep-style search with context <code>find_files</code> Glob-style file discovery <code>list_folder</code> Directory listing"},{"location":"reference/actions/#web-web_research","title":"Web (<code>web_research</code>)","text":"Action Purpose <code>web_search</code> Search via Google CSE (requires <code>google_cse_id</code>) <code>web_fetch</code> HTTP GET with HTML-to-markdown conversion <code>open_browser</code> Launch a URL in the user's default browser <code>http_request</code> Arbitrary HTTP call (GET/POST/etc.)"},{"location":"reference/actions/#shell-code-shell","title":"Shell &amp; code (<code>shell</code>)","text":"Action Purpose <code>run_shell</code> Universal shell exec \u2014 see CLI-anything <code>run_python</code> Execute a Python snippet"},{"location":"reference/actions/#documents-document_processing","title":"Documents (<code>document_processing</code>)","text":"Action Purpose <code>read_pdf</code> Extract text from a PDF <code>create_pdf</code> Generate a PDF <code>convert_to_markdown</code> Any \u2192 markdown (docx, html, rtf) <code>describe_image</code> VLM-powered image description <code>generate_image</code> Text \u2192 image via the configured image model"},{"location":"reference/actions/#clipboard-clipboard","title":"Clipboard (<code>clipboard</code>)","text":"<ul> <li><code>clipboard_read</code> \u00b7 <code>clipboard_write</code></li> </ul>"},{"location":"reference/actions/#memory","title":"Memory","text":"<ul> <li><code>memory_search</code> \u2014 query the memory system</li> </ul>"},{"location":"reference/actions/#proactive-scheduler","title":"Proactive &amp; scheduler","text":"Action Purpose <code>recurring_read</code> Read tasks from <code>PROACTIVE.md</code> <code>recurring_add</code> Add a new proactive task <code>recurring_update_task</code> Modify an existing task <code>recurring_remove</code> Delete a proactive task <code>schedule_task</code> Add a one-off / recurring entry to the scheduler <code>schedule_task_toggle</code> Enable / disable a scheduled task <code>scheduled_task_list</code> List all scheduled tasks <code>remove_scheduled_task</code> Remove a scheduled task"},{"location":"reference/actions/#gui-gui_interaction","title":"GUI (<code>gui_interaction</code>)","text":"<p>Only available when GUI mode is on:</p> <ul> <li><code>mouse_click</code> \u00b7 <code>mouse_move</code> \u00b7 <code>mouse_drag</code> \u00b7 <code>mouse_trace</code></li> <li><code>keyboard_type</code> \u00b7 <code>keyboard_hotkey</code></li> <li><code>scroll</code></li> <li><code>open_application</code> \u00b7 <code>open_browser</code></li> </ul>"},{"location":"reference/actions/#integration-actions","title":"Integration actions","text":"<p>Dynamic \u2014 each integration contributes its own. Enabled when the integration is connected.</p> Integration Action prefix / directory Discord <code>send_discord_*</code>, <code>get_discord_*</code>, <code>add_discord_*</code> Slack <code>*_slack_*</code> Telegram <code>*_telegram_*</code> Notion <code>*_notion_*</code> Google Workspace <code>send_gmail</code>, <code>list_gmail</code>, <code>get_gmail</code>, <code>create_google_meet</code>, <code>check_calendar_availability</code>, <code>list_drive_files</code>, <code>create_drive_folder</code>, <code>move_drive_file</code> LinkedIn <code>*_linkedin_*</code> Outlook <code>*_outlook_*</code> WhatsApp <code>*_whatsapp_*</code> GitHub (see <code>app/data/action/github/</code>) Jira (see <code>app/data/action/jira/</code>) Twitter (see <code>app/data/action/twitter/</code>)"},{"location":"reference/actions/#integration-management","title":"Integration management","text":"<p>Always available \u2014 lets the agent help users connect/disconnect services:</p> Action Purpose <code>list_available_integrations</code> List every integration + connection state <code>connect_integration</code> Start OAuth / token flow <code>disconnect_integration</code> Remove credentials <code>check_integration_status</code> Status of one integration"},{"location":"reference/actions/#mcp-tools","title":"MCP tools","text":"<p>Dynamically added when MCP servers connect. Named <code>&lt;server_name&gt;:&lt;tool_name&gt;</code>. See MCP servers.</p>"},{"location":"reference/actions/#action-sets-summary","title":"Action sets summary","text":"Set Contents <code>core</code> Task management + send_message + integration mgmt + run_shell <code>file_operations</code> File I/O <code>web_research</code> Web search + fetch <code>document_processing</code> PDF / DOCX / image <code>clipboard</code> Clipboard read/write <code>shell</code> Shell + Python exec <code>gui_interaction</code> Mouse/keyboard/screenshot (GUI mode only)"},{"location":"reference/actions/#related","title":"Related","text":"<ul> <li>Actions concept</li> <li>Skill &amp; action selection</li> <li>Custom action \u2014 build your own</li> <li>MCP servers \u2014 actions from MCP</li> </ul>"},{"location":"reference/agent-md-files/","title":"Agent MD files","text":"<p>Schema and writing conventions for every file in <code>agent_file_system/</code>.</p> <p>All files use plain markdown \u2014 no JSON, no YAML (except where noted). This is deliberate: the files are meant to be readable and editable by humans and the agent equally.</p>"},{"location":"reference/agent-md-files/#agentmd","title":"AGENT.md","text":"<p>Written by: humans / agent \u00b7 Read by: every LLM call (via <code>AGENT_INFO_PROMPT</code>)</p> <p>Defines the agent's identity and protocols. No fixed schema \u2014 the file is free-form markdown. Typical sections:</p> <ul> <li>Identity (who the agent is, what it can do)</li> <li>Error handling protocol</li> <li>File handling rules</li> <li>Self-improvement workflow (when to install MCP servers / skills)</li> <li>Policies (what not to do)</li> </ul> <p>The default AGENT.md shipped with CraftBot has all of these. Custom agents override the identity section for their persona.</p>"},{"location":"reference/agent-md-files/#usermd","title":"USER.md","text":"<p>Written by: onboarding / user \u00b7 Read by: every LLM call (via <code>USER_PROFILE_PROMPT</code>)</p> <p>Fixed schema \u2014 the onboarding wizard fills this on first launch:</p> <pre><code>## Identity\n- **Full Name:** ...\n- **Preferred Name:** ...\n- **Email:** ...\n- **Location:** ...\n- **Timezone:** (inferred from location)\n- **Job:** ...\n\n## Communication Preferences\n- **Language:** en\n- **Preferred Tone:** (casual | formal | concise | verbose)\n- **Response Style:** (bullet points | prose | short | long)\n- **Preferred Messaging Platform:** (Slack | Telegram | WhatsApp | CraftBot UI)\n\n## Agent Interaction\n- **Prefer Proactive Assistance:** (yes | no)\n- **Approval Required For:** (list of sensitive operations)\n\n## Life Goals\n...\n\n## Personality\n...\n</code></pre>"},{"location":"reference/agent-md-files/#memorymd","title":"MEMORY.md","text":"<p>Written by: daily memory distillation task \u00b7 Read by: via <code>memory_search</code></p> <p>Freeform markdown with sections. The distiller appends new entries under headers like:</p> <pre><code>### &lt;YYYY-MM-DD&gt; &lt;short title&gt;\n&lt;body describing what happened / was learned&gt;\n</code></pre> <p>The memory indexer chunks by section header, so keep each entry under one H3.</p>"},{"location":"reference/agent-md-files/#eventmd","title":"EVENT.md","text":"<p>Written by: agent (continuously) \u00b7 Read by: humans + memory distiller</p> <p>Raw chronological log. Every significant interaction \u2014 user messages, tasks started/completed, integrations connected \u2014 gets a line. Not for the LLM to re-read; for humans auditing what the agent has been doing.</p> <p>Grows unbounded unless you truncate it periodically.</p>"},{"location":"reference/agent-md-files/#event_unprocessedmd","title":"EVENT_UNPROCESSED.md","text":"<p>Written by: agent \u00b7 Read by: daily memory distillation</p> <p>Events the distiller has not yet reviewed. The distillation task:</p> <ol> <li>Reads this file</li> <li>Scores each event for long-term value</li> <li>Writes keepers to MEMORY.md</li> <li>Clears EVENT_UNPROCESSED.md</li> </ol> <p>You should not edit this file manually.</p>"},{"location":"reference/agent-md-files/#task_historymd","title":"TASK_HISTORY.md","text":"<p>Written by: TaskManager on <code>task_end</code> \u00b7 Read by: humans + memory search</p> <p>One section per completed task:</p> <pre><code>### &lt;YYYY-MM-DD HH:MM&gt; &lt;task name&gt; [id=&lt;uuid&gt;]\n- **Instruction:** &lt;original instruction&gt;\n- **Mode:** simple | complex\n- **Duration:** N minutes\n- **Outcome:** &lt;final_summary&gt;\n- **Todos:**\n  - [x] Step 1\n  - [x] Step 2\n</code></pre>"},{"location":"reference/agent-md-files/#conversation_historymd","title":"CONVERSATION_HISTORY.md","text":"<p>Written by: UI layer \u00b7 Read by: context for follow-ups</p> <p>Multi-turn transcripts for context continuation. Format:</p> <pre><code>### &lt;YYYY-MM-DD HH:MM&gt; conversation\n\n**user:** question\n**agent:** answer\n\n**user:** follow-up\n**agent:** reply\n</code></pre>"},{"location":"reference/agent-md-files/#proactivemd","title":"PROACTIVE.md","text":"<p>Written by: humans / proactive planner \u00b7 Read by: proactive heartbeat</p> <p>See Proactive for the full schema \u2014 it uses YAML fenced inside markdown, with HTML comment markers (<code>&lt;!-- PROACTIVE_TASKS_START --&gt;</code>, etc.) that the parser depends on. Don't remove the markers.</p>"},{"location":"reference/agent-md-files/#soulmd","title":"SOUL.md","text":"<p>Written by: humans / agent \u00b7 Read by: every LLM call (via <code>SOUL_PROMPT</code>)</p> <p>Persistent personality traits that persist across all tasks. Keep it terse \u2014 just 3\u20135 paragraphs defining tone, quirks, and values. Unlike AGENT.md (which is about what to do), SOUL.md is about how the agent feels.</p>"},{"location":"reference/agent-md-files/#formatmd","title":"FORMAT.md","text":"<p>Written by: humans \u00b7 Read by: agent when formatting output</p> <p>Output conventions \u2014 bullet styles, heading depth, emoji usage, code-block fencing. The agent reads this when deciding how to render <code>send_message</code> output.</p>"},{"location":"reference/agent-md-files/#mission_index_templatemd","title":"MISSION_INDEX_TEMPLATE.md","text":"<p>Template used when the agent starts a multi-session mission (research projects that span days). Copied into a subdirectory and filled in over time.</p>"},{"location":"reference/agent-md-files/#workspace","title":"workspace/","text":"<p>A scratch directory. No schema. Tasks drop temp files here (drafts, downloaded content, screenshots). Lifecycle:</p> <ul> <li>Per-task <code>temp_dir</code> subdirectories are cleaned up when the task ends</li> <li>Files written directly to <code>workspace/</code> persist until manually removed</li> </ul>"},{"location":"reference/agent-md-files/#agent-bundle-overrides","title":"Agent-bundle overrides","text":"<p>A custom agent with <code>data_dir: agents/my_agent/data/</code> uses its own files at that path. The schemas are identical.</p>"},{"location":"reference/agent-md-files/#related","title":"Related","text":"<ul> <li>Agent file system concept \u2014 what / why / how it's used</li> <li>Memory \u2014 how these files get indexed</li> <li>Context engine \u2014 how the files are injected into prompts</li> </ul>"},{"location":"reference/events/","title":"Event types","text":"<p>Every <code>kind</code> that can appear in the event stream. Custom <code>kind</code> values are also allowed \u2014 this is the built-in catalogue.</p> <p>Each event has:</p> Field Type <code>kind</code> str \u2014 from this catalogue or any custom string <code>message</code> str \u2014 full event payload (may be externalized to disk if &gt; 200k chars) <code>severity</code> <code>DEBUG</code> / <code>INFO</code> / <code>WARN</code> / <code>ERROR</code> <code>ts</code> datetime <code>display_message</code> optional UI-friendly variant <code>action_name</code> optional \u2014 for action_* kinds"},{"location":"reference/events/#action-lifecycle","title":"Action lifecycle","text":"<code>kind</code> When <code>message</code> shape <code>action_start</code> Just before an action runs <code>\"&lt;action_name&gt;\"</code> <code>action_end</code> After action completes <code>\"&lt;action_name&gt; -&gt; &lt;status&gt; (&lt;extras&gt;)\"</code> <code>action_error</code> Exception raised during action <code>\"&lt;action_name&gt;: &lt;error_class&gt;: &lt;message&gt;\"</code> <code>action_timeout</code> Action exceeded timeout (e.g. <code>run_shell</code>) <code>\"&lt;action_name&gt;: timed out after &lt;N&gt;s\"</code>"},{"location":"reference/events/#task-lifecycle","title":"Task lifecycle","text":"<code>kind</code> When <code>task_start</code> Task created <code>task_todo_added</code> Todo appended via <code>task_update_todos</code> <code>task_todo_in_progress</code> Todo moved to in_progress <code>task_todo_completed</code> Todo marked completed <code>task_end</code> Task completed <code>task_error</code> Task failed <code>task_paused</code> Waiting for user / approval"},{"location":"reference/events/#conversation","title":"Conversation","text":"<code>kind</code> When <code>user_message</code> User sent a message (local UI or routed from external platform) <code>agent_message</code> Agent sent a message via <code>send_message</code> <code>agent_reasoning</code> Chain-of-thought emitted by the LLM (visible per <code>/reasoning</code> directive)"},{"location":"reference/events/#trigger-queue","title":"Trigger &amp; queue","text":"<code>kind</code> When <code>trigger_fired</code> Trigger dequeued and about to run <code>trigger_merged</code> Multiple triggers for the same session were combined <code>trigger_routed</code> LLM routed a triggerless message to an existing session"},{"location":"reference/events/#integrations","title":"Integrations","text":"<code>kind</code> When <code>integration_connected</code> OAuth / token login succeeded <code>integration_disconnected</code> <code>/xxx logout</code> or token expiry <code>integration_error</code> Provider API returned an error <code>external_message_received</code> Message arrived from Slack / Telegram / etc."},{"location":"reference/events/#memory","title":"Memory","text":"<code>kind</code> When <code>memory_chunk_added</code> Indexer added a chunk to ChromaDB <code>memory_retrieval</code> <code>memory_search</code> completed <code>memory_summary_written</code> Daily distillation wrote to <code>MEMORY.md</code>"},{"location":"reference/events/#event-stream-meta","title":"Event stream meta","text":"<code>kind</code> When <code>stream_summarized</code> Rollup into <code>head_summary</code> triggered <code>stream_pruned</code> Fallback prune without summary (LLM unavailable) <code>stream_externalized</code> Long message written to temp file"},{"location":"reference/events/#proactive","title":"Proactive","text":"<code>kind</code> When <code>proactive_heartbeat</code> Heartbeat trigger fired <code>proactive_planner</code> Planner trigger fired <code>proactive_task_executed</code> One of PROACTIVE.md's tasks ran <code>proactive_task_skipped</code> Task failed its conditions or grace window"},{"location":"reference/events/#severities-filtering","title":"Severities &amp; filtering","text":"Severity Used for <code>DEBUG</code> Low-signal; hidden by default in UI <code>INFO</code> Default for lifecycle events <code>WARN</code> Recoverable anomaly (retry, degraded path) <code>ERROR</code> Action/task failed <p>UIs filter by severity \u2014 the TUI has toggles; the browser has a severity filter.</p>"},{"location":"reference/events/#compact-line-format","title":"Compact-line format","text":"<p>Events appear in <code>to_prompt_snapshot()</code> as:</p> <pre><code>[2026-04-22 10:14:02] action_start: send_gmail\n[2026-04-22 10:14:04] action_end: send_gmail -&gt; ok (message_id=18a2f3e)\n</code></pre> <p>This is what the LLM sees.</p>"},{"location":"reference/events/#related","title":"Related","text":"<ul> <li>Event stream \u2014 the producer</li> <li>Logs \u2014 the adjacent operator-facing stream</li> <li>Actions catalogue \u2014 actions that produce action_* events</li> </ul>"},{"location":"start/","title":"Getting started","text":"<p>Install CraftBot, run through onboarding, send your first task, and optionally set it up as a background service. Five pages, roughly 15 minutes end-to-end.</p> <ul> <li> <p> Install</p> <p>Python 3.10+, <code>python install.py</code>, and your LLM API key. Windows, Linux, and macOS.</p> </li> <li> <p> Quickstart</p> <p>From zero to a running agent in 5 minutes. Browser, TUI, or CLI.</p> </li> <li> <p> Onboarding</p> <p>First-launch wizard: agent name, API keys, skills, MCP servers.</p> </li> <li> <p> Your first task</p> <p>Send a message, watch CraftBot plan and execute.</p> </li> <li> <p> Service mode</p> <p>Run CraftBot as an always-on background service with auto-start.</p> </li> </ul>"},{"location":"start/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10+ \u2014 check with <code>python --version</code></li> <li><code>git</code> to clone the repo</li> <li>An API key from OpenAI, Google Gemini, Anthropic, BytePlus, or a local Ollama server</li> <li>(optional) Node.js 18+ for browser mode</li> </ul>"},{"location":"start/#recommended-reading-order","title":"Recommended reading order","text":"<ol> <li>Install</li> <li>Quickstart \u2014 runs through onboarding automatically</li> <li>Your first task</li> <li>Pick an interface from Interfaces</li> <li>(Optional) Set up service mode for always-on operation</li> </ol>"},{"location":"start/#related","title":"Related","text":"<ul> <li>Concepts \u2014 what CraftBot is made of</li> <li>Troubleshooting \u2014 if install or first run fails</li> </ul>"},{"location":"start/hello-task/","title":"Your first task","text":"<p>Onboarding is done. Now make CraftBot do something useful.</p>"},{"location":"start/hello-task/#a-one-shot-request","title":"A one-shot request","text":"<p>Simplest thing first \u2014 a one-liner:</p> <p>\"What's the weather in Tokyo right now?\"</p> <p>Expected flow:</p> <ol> <li>Agent parses the message \u2192 conversation workflow</li> <li>Picks <code>task_start</code> with <code>mode: \"simple\"</code></li> <li>New task starts \u2192 picks <code>web_search</code> (or a weather skill if enabled)</li> <li>Gets the result, calls <code>send_message</code>, task completes</li> </ol> <p>You see the weather in the chat window. Time: ~5-10 seconds.</p>"},{"location":"start/hello-task/#a-multi-step-task","title":"A multi-step task","text":"<p>Now something where the agent has to plan:</p> <p>\"Research three AI-agent frameworks, compare them by popularity, license, and extensibility, and save a summary as <code>frameworks.md</code> in the workspace.\"</p> <p>Expected flow:</p> <ol> <li>Conversation \u2192 <code>task_start</code> with <code>mode: \"complex\"</code> (the agent detects the work is multi-step)</li> <li>First iteration: <code>task_update_todos</code> creates a plan:<ul> <li>[ ] Search for popular AI-agent frameworks</li> <li>[ ] Fetch comparisons from top 3</li> <li>[ ] Draft the summary</li> <li>[ ] Save to workspace</li> </ul> </li> <li>Iterates through the todos, one action per iteration</li> <li>Final: <code>write_file</code> \u2192 <code>agent_file_system/workspace/frameworks.md</code></li> </ol> <p>You can watch the todos update in the UI in real-time.</p>"},{"location":"start/hello-task/#slash-commands-youll-use-early","title":"Slash commands you'll use early","text":"<pre><code>/help                # list every command\n/menu                # open the settings panel (models, integrations, skills, MCP)\n/cred status         # see connected integrations\n/skill list          # see available skills\n/reset               # clear the current session\n</code></pre> <p>Full catalogue: Built-in commands.</p>"},{"location":"start/hello-task/#connect-an-integration","title":"Connect an integration","text":"<p>Most users want to connect one integration on day one. The easiest is Google:</p> <pre><code>/google login\n</code></pre> <p>A browser tab opens, you click Approve, and Gmail / Calendar / Drive actions become available. Try:</p> <p>\"Summarize my unread emails from the last 24 hours.\"</p> <p>See Connections for every integration.</p>"},{"location":"start/hello-task/#run-a-proactive-task","title":"Run a proactive task","text":"<p>Open <code>agent_file_system/PROACTIVE.md</code>, add:</p> <p><pre><code>### [DAILY] Morning summary\n```yaml\nid: morning_summary\nfrequency: daily\ntime: \"08:00\"\nenabled: true\npriority: 50\npermission_tier: 1\ninstruction: |\n  Summarize my unread emails, open PRs, and today's calendar events.\n  Send via chat.\n</code></pre> ```</p> <p>Save the file. With proactive mode enabled, the heartbeat will pick this up tomorrow at 8 AM.</p>"},{"location":"start/hello-task/#what-to-try-next","title":"What to try next","text":"Want to\u2026 Go to Understand how it works Concepts Run it always-on Service mode Add your own capability Custom action Connect Slack / Telegram / \u2026 Connections"},{"location":"start/hello-task/#if-it-breaks","title":"If it breaks","text":"<ul> <li><code>tail -f logs/*.log</code> for raw logs</li> <li><code>/menu</code> \u2192 Logs panel for the UI version</li> <li>Troubleshooting for common failure patterns</li> </ul>"},{"location":"start/install/","title":"Install CraftBot","text":"<p>Five minutes, Python 3.10+, and at least one LLM API key.</p>"},{"location":"start/install/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10+ \u2014 check with <code>python --version</code></li> <li>git \u2014 to clone the repo</li> <li>An LLM API key from OpenAI, Google Gemini, Anthropic, BytePlus, or a local Ollama server</li> <li>(optional) Node.js 18+ \u2014 only for browser mode</li> <li>(optional) conda \u2014 <code>install.py</code> offers to auto-install Miniconda if you want it</li> </ul>"},{"location":"start/install/#quick-install","title":"Quick install","text":"<pre><code>git clone https://github.com/zfoong/CraftBot.git\ncd CraftBot\npython install.py\npython run.py\n</code></pre> <p>On first run, the onboarding wizard asks for your API key and a few preferences. Done.</p>"},{"location":"start/install/#install-flags","title":"Install flags","text":"<p><code>install.py</code> supports several flags for different setups:</p> Simple (pip, no conda, no GUI)With GUI supportWith conda environment <pre><code>python install.py\n</code></pre> <pre><code># Full GUI with GPU support (downloads ~4GB of OmniParser weights)\npython install.py --gui\n\n# GUI on CPU-only systems\npython install.py --gui --cpu-only\n</code></pre> <pre><code>python install.py --conda\n# or with GUI\npython install.py --gui --conda\n</code></pre> Flag Effect (none) pip install into current environment, no GUI, no conda <code>--gui</code> Install OmniParser + vision deps (\u22484GB) <code>--cpu-only</code> CPU-only PyTorch (use with <code>--gui</code> if no NVIDIA GPU) <code>--conda</code> Create a <code>craftbot</code> conda env (offers to install Miniconda if missing)"},{"location":"start/install/#choose-your-interface","title":"Choose your interface","text":"<p>After install, pick a mode:</p> Mode Command Needs Browser <code>python run.py</code> Node.js 18+ TUI <code>python run.py --tui</code> Nothing extra CLI <code>python run.py --cli</code> Nothing extra GUI <code>python run.py --gui</code> <code>install.py --gui</code> completed <p>See Interfaces overview for the full comparison.</p>"},{"location":"start/install/#no-nodejs-no-problem","title":"No Node.js? No problem","text":"<p>If you don't have Node.js, the installer will say so. Use <code>python run.py --tui</code> \u2014 the terminal UI has the same features as the browser UI, just in your terminal.</p>"},{"location":"start/install/#docker","title":"Docker","text":"<p>From the repo root:</p> <pre><code>docker build -t craftbot .\ndocker run --rm -it --env-file .env craftbot\n</code></pre> <p>For GUI mode in Docker, see GUI / Vision.</p>"},{"location":"start/install/#platform-notes","title":"Platform notes","text":"macOS / LinuxWindows <p>Nothing special. <code>python install.py</code> handles everything. If Homebrew Python gives trouble, use <code>pyenv</code> or the version in the CraftBot conda env.</p> <ul> <li>Use PowerShell or Git Bash.</li> <li>If <code>python</code> is not on PATH, use <code>py</code> (Windows launcher).</li> <li><code>install.py --gui</code> on Windows installs GPU PyTorch by default; add <code>--cpu-only</code> if no CUDA GPU.</li> </ul>"},{"location":"start/install/#troubleshooting-install","title":"Troubleshooting install","text":"<ul> <li><code>npm not found</code> \u2014 install Node.js from nodejs.org or use TUI mode.</li> <li>Playwright install fails \u2014 it's optional (for WhatsApp Web). Skip it; install later with <code>playwright install chromium</code>.</li> <li>CUDA issues \u2014 the installer falls back to CPU automatically. Force with <code>--cpu-only</code>.</li> <li>Python version too low \u2014 upgrade to Python 3.10+. Python 3.9 won't work.</li> </ul> <p>See Runtime issues for more.</p>"},{"location":"start/install/#next","title":"Next","text":"<ul> <li>Quickstart \u2014 walk through first run</li> <li>Your first task \u2014 talk to the agent</li> <li>Service mode \u2014 run it as a background service</li> </ul>"},{"location":"start/onboarding/","title":"Onboarding","text":"<p>The first time you run <code>python run.py</code>, CraftBot walks you through a wizard. Two phases:</p> <ul> <li>Hard onboarding \u2014 a must-complete form: API keys, agent name, interface modes.</li> <li>Soft onboarding \u2014 a conversational follow-up where the agent fills in USER.md over your first few exchanges.</li> </ul>"},{"location":"start/onboarding/#beginner-mental-model","title":"Beginner mental model","text":"<ul> <li>Onboarding writes to disk: <code>settings.json</code>, <code>agent_file_system/USER.md</code>, and credential files under <code>app/credentials/</code>.</li> <li>It's idempotent \u2014 re-run it with <code>/menu</code> \u2192 Re-onboard to change anything.</li> <li>It's resumable \u2014 close the app mid-wizard, it picks up where you left off.</li> </ul>"},{"location":"start/onboarding/#hard-onboarding-steps","title":"Hard onboarding steps","text":"Step What you're asked Stored in Welcome Accept the license, choose OS language <code>settings.json</code> \u2192 <code>general.os_language</code> Agent name Name your agent (default: \"CraftBot\") <code>settings.json</code> \u2192 <code>general.agent_name</code> LLM provider Pick from <code>anthropic</code>, <code>openai</code>, <code>google</code>, <code>byteplus</code>, <code>remote</code> (Ollama) <code>settings.json</code> \u2192 <code>model.llm_provider</code> API key Paste your key <code>settings.json</code> \u2192 <code>api_keys.*</code> (or env var if set) VLM provider (Optional) different provider for vision <code>settings.json</code> \u2192 <code>model.vlm_provider</code> Memory Enable the memory system? <code>settings.json</code> \u2192 <code>memory.enabled</code> Proactive Enable proactive mode? <code>settings.json</code> \u2192 <code>proactive.enabled</code> Skills Pick which skills to enable <code>skills_config.json</code> MCP (Optional) add MCP servers <code>mcp_config.json</code> Integrations (Optional) connect Google / Slack / etc. per-integration credential store <p>Each step has a skip button \u2014 you can always come back via <code>/menu</code>.</p>"},{"location":"start/onboarding/#soft-onboarding","title":"Soft onboarding","text":"<p>After hard onboarding, the agent asks you a few questions over chat:</p> <ul> <li>Your name, preferred name, email</li> <li>Location and timezone</li> <li>Communication preferences (tone, style, platform)</li> <li>Life goals</li> <li>Agent interaction preferences (proactive approval tier)</li> </ul> <p>Answers populate <code>USER.md</code> \u2014 the agent reads this file in every prompt, so tailoring here noticeably shifts its style.</p> <p>Soft onboarding doesn't block \u2014 you can start tasks before finishing it. The agent will nudge you periodically with the next question.</p>"},{"location":"start/onboarding/#re-running","title":"Re-running","text":"<pre><code>/menu \u2192 Settings \u2192 Re-run onboarding\n</code></pre> <p>Or manually:</p> <pre><code>rm app/config/settings.json   # to redo hard onboarding from scratch\nrm agent_file_system/USER.md  # to redo soft onboarding\n</code></pre> <p>(First re-run after a <code>rm</code> still preserves other data \u2014 only the wiped files are re-initialized.)</p>"},{"location":"start/onboarding/#inspect-it-now","title":"Inspect it now","text":"<pre><code>/menu\n</code></pre> <p>opens the settings panel, which is the onboarding UI but with \"save\" instead of \"next.\"</p>"},{"location":"start/onboarding/#under-the-hood","title":"Under the hood","text":"<p>Hard onboarding lives in <code>app/onboarding/</code>. Each step is a <code>StepProtocol</code> with:</p> <ul> <li>A question text</li> <li>A handler that validates input</li> <li>A \"persist\" hook that writes to disk</li> </ul> <p>Add a new step by implementing the protocol \u2014 see Custom agent for when this is useful.</p>"},{"location":"start/onboarding/#related","title":"Related","text":"<ul> <li>Install \u2014 what you do before onboarding</li> <li>USER.md reference \u2014 what soft onboarding fills in</li> <li>settings.json \u2014 what hard onboarding writes</li> <li>Your first task \u2014 what to do after onboarding</li> </ul>"},{"location":"start/quickstart/","title":"Quickstart","text":"<p>Zero to running agent in 5 minutes.</p>"},{"location":"start/quickstart/#prerequisites","title":"Prerequisites","text":"<ul> <li>CraftBot installed</li> <li>An LLM API key in hand</li> </ul>"},{"location":"start/quickstart/#5-step-flow","title":"5-step flow","text":""},{"location":"start/quickstart/#1-clone-install","title":"1. Clone + install","text":"<pre><code>git clone https://github.com/zfoong/CraftBot.git\ncd CraftBot\npython install.py\n</code></pre>"},{"location":"start/quickstart/#2-run","title":"2. Run","text":"<pre><code>python run.py           # browser (needs Node.js)\n# OR\npython run.py --tui     # terminal UI (no Node.js)\n</code></pre>"},{"location":"start/quickstart/#3-onboarding-wizard","title":"3. Onboarding wizard","text":"<p>First launch opens the onboarding wizard:</p> <ul> <li>Pick an LLM provider (Anthropic / OpenAI / Google / BytePlus / Ollama)</li> <li>Paste your API key</li> <li>Give the agent a name</li> <li>(Optional) Enable memory, proactive mode, and skills</li> </ul> <p>Click Finish. The agent starts up.</p>"},{"location":"start/quickstart/#4-send-a-message","title":"4. Send a message","text":"<pre><code>You: what's the weather in Tokyo\n</code></pre> <p>Watch the agent plan, pick actions (<code>web_search</code> or a weather skill), and reply.</p>"},{"location":"start/quickstart/#5-try-a-complex-task","title":"5. Try a complex task","text":"<pre><code>You: research the top 3 Python web frameworks, summarize the differences,\n     and save the summary as frameworks.md in the workspace\n</code></pre> <p>Now you'll see todos appear, actions fire in sequence, and a file written to <code>agent_file_system/workspace/frameworks.md</code>.</p>"},{"location":"start/quickstart/#what-just-happened","title":"What just happened?","text":"Step What CraftBot did 1\u20132 Installed deps, launched one of the interfaces 3 Wrote settings.json with your provider + keys 4 Routed the message through the agent loop \u2192 Conversation workflow \u2192 <code>task_start</code> 5 Created a complex task with todos, iterated through actions"},{"location":"start/quickstart/#next","title":"Next","text":"<ul> <li>Your first task \u2014 more examples</li> <li>Service mode \u2014 run it as a background daemon</li> <li>Concepts \u2014 understand what's happening under the hood</li> <li>Connections \u2014 connect Slack, Google, Discord, \u2026</li> </ul>"},{"location":"start/quickstart/#stuck","title":"Stuck?","text":"<ul> <li>Troubleshooting</li> <li>Discord community</li> <li>GitHub issues</li> </ul>"},{"location":"start/service-mode/","title":"Service mode","text":"<p>Run CraftBot as a background service that starts on login, survives terminal close, and keeps proactive mode firing 24/7. One command installs it; a desktop shortcut reopens the browser whenever you want.</p>"},{"location":"start/service-mode/#the-core-idea","title":"The core idea","text":"<p>The <code>service.py</code> entry point wraps <code>run.py</code> with:</p> <ul> <li>Auto-start registration \u2014 per-OS hook that relaunches CraftBot on login.</li> <li>Detached process \u2014 the agent runs independently of the terminal that started it.</li> <li>Desktop shortcut \u2014 double-click to reopen the browser UI.</li> <li>Log rotation \u2014 keep recent logs, discard old ones.</li> </ul> <p>Under the hood it's the same agent as <code>python run.py</code> \u2014 just with lifecycle management around it.</p>"},{"location":"start/service-mode/#scenarios","title":"Scenarios","text":""},{"location":"start/service-mode/#1-personal-workstation-the-default","title":"1) Personal workstation (the default)","text":"<p>Install once, CraftBot auto-starts on every login. You use the browser UI when you want; it's running quietly the rest of the time so proactive tasks and integrations stay live.</p> <pre><code>python service.py install\n</code></pre>"},{"location":"start/service-mode/#2-always-on-home-server","title":"2) Always-on home server","text":"<p>Run on a persistent box (Raspberry Pi, home server, cheap VPS). Daily/weekly proactive tasks always fire even when your laptop's closed. Access via SSH port-forward or Tailscale.</p> <pre><code># On the server:\npython service.py install\n\n# From your laptop:\nssh -N -L 8080:localhost:8080 you@server\n# Open http://localhost:8080 in your browser\n</code></pre>"},{"location":"start/service-mode/#3-per-user-install-on-a-shared-machine","title":"3) Per-user install on a shared machine","text":"<p><code>service.py</code> installs into your user account \u2014 no sudo needed. Each user on the same machine can install their own CraftBot service with separate config and credentials.</p>"},{"location":"start/service-mode/#command-flow","title":"Command flow","text":"<pre><code>   service.py install\n         \u2502\n         \u251c\u2500&gt; install deps (pip install -r requirements.txt)\n         \u251c\u2500&gt; register auto-start (Windows Task Scheduler / systemd user / LaunchAgent)\n         \u251c\u2500&gt; start CraftBot detached (browser mode by default)\n         \u251c\u2500&gt; create desktop shortcut\n         \u2514\u2500&gt; close the terminal\n</code></pre>"},{"location":"start/service-mode/#cli-reference","title":"CLI reference","text":"Command What it does <code>python service.py install</code> Install deps + register auto-start + start + create shortcut + close terminal <code>python service.py start</code> Start in background (auto-restart if already running) <code>python service.py stop</code> Stop the running service <code>python service.py restart</code> Stop + start <code>python service.py status</code> Running? Auto-start enabled? <code>python service.py logs [-n N]</code> Show last N log lines (default 50) <code>python service.py uninstall</code> Stop + remove auto-start + uninstall pip packages + purge pip cache"},{"location":"start/service-mode/#configuration","title":"Configuration","text":"<p>Service mode uses the same <code>settings.json</code> as <code>run.py</code>. No separate config.</p> <p>Browser mode is the default interface. To use TUI or CLI mode as a service, edit the command in the launcher script that <code>service.py install</code> creates (it's OS-specific \u2014 see platform deep-dives below).</p>"},{"location":"start/service-mode/#credential-precedence","title":"Credential precedence","text":"<p>Credentials resolve in the same order as regular <code>run.py</code>:</p> <ol> <li>Embedded (release builds)</li> <li>Environment variables \u2014 critical: the service runs under your user, so <code>.env</code> in the repo is loaded. System env vars work too.</li> <li><code>settings.json</code> \u2192 <code>api_keys</code></li> <li>(nothing) \u2014 connection unavailable</li> </ol> <p>Env vars and auto-start</p> <p>On Linux with systemd user, the service does not inherit your shell env. If you rely on env vars, put them in <code>.env</code> (the launcher loads it) or in <code>~/.config/systemd/user/craftbot.service</code> via <code>Environment=</code>.</p>"},{"location":"start/service-mode/#browser-ui-access","title":"Browser UI access","text":"<ul> <li>Local \u2014 <code>http://localhost:&lt;port&gt;</code> (port shown in <code>service.py status</code>).</li> <li>Remote over SSH \u2014 port-forward from client to server. See Scenario 2.</li> <li>Remote over Tailscale / VPN \u2014 access the server's tailnet IP directly.</li> <li>Public internet \u2014 not recommended. If you must, put a reverse proxy with auth in front.</li> </ul>"},{"location":"start/service-mode/#security-rules","title":"Security rules","text":"<ul> <li>Default bind is <code>localhost</code> \u2014 no external access unless you tunnel or proxy.</li> <li>No built-in auth \u2014 the browser UI trusts whoever can reach the port. Don't expose it directly to the internet.</li> <li>Credentials are local \u2014 the service reads from the same credential store as <code>run.py</code>.</li> <li>OAuth refresh keeps tokens fresh; revoke in the provider's dashboard if a machine is compromised.</li> </ul>"},{"location":"start/service-mode/#platform-deep-dive","title":"Platform deep-dive","text":"WindowsmacOSLinux (systemd user) <p>Auto-start via Task Scheduler:</p> <pre><code>python service.py install\n</code></pre> <p>Creates a scheduled task <code>CraftBot</code> with trigger \"At log on of any user\" and action running <code>run.py</code> detached.</p> <p>Manual inspection:</p> <pre><code># List the task\nschtasks /Query /TN CraftBot /V /FO LIST\n\n# Remove manually if uninstall gets stuck\nschtasks /Delete /TN CraftBot /F\n</code></pre> <p>Shortcut location: <code>%USERPROFILE%\\Desktop\\CraftBot.lnk</code></p> <p>Troubleshooting:</p> <ul> <li>Service won't start after reboot: check <code>schtasks /Query /TN CraftBot</code> returns <code>Ready</code>. If it's <code>Disabled</code>, re-enable in Task Scheduler UI.</li> <li>Double-click shortcut does nothing: the browser picks the last-used window. Check <code>http://localhost:&lt;port&gt;</code> in your address bar.</li> </ul> <p>Auto-start via LaunchAgent \u2014 <code>python service.py install</code> writes <code>~/Library/LaunchAgents/com.craftos.craftbot.plist</code> and loads it.</p> <p>Manual inspection:</p> <pre><code>launchctl list | grep craftbot\nlaunchctl print gui/$(id -u)/com.craftos.craftbot\n</code></pre> <p>Restart manually:</p> <pre><code>launchctl kickstart -k gui/$(id -u)/com.craftos.craftbot\n</code></pre> <p>Uninstall the LaunchAgent manually:</p> <pre><code>launchctl bootout gui/$(id -u)/com.craftos.craftbot\nrm ~/Library/LaunchAgents/com.craftos.craftbot.plist\n</code></pre> <p>Auto-start \u2014 writes <code>~/.config/systemd/user/craftbot.service</code> + enables linger:</p> <pre><code>loginctl enable-linger $USER\nsystemctl --user enable craftbot\nsystemctl --user start craftbot\n</code></pre> <p>Manual inspection:</p> <pre><code>systemctl --user status craftbot\njournalctl --user -u craftbot -f\n</code></pre> <p>Uninstall:</p> <pre><code>systemctl --user disable craftbot\nsystemctl --user stop craftbot\nrm ~/.config/systemd/user/craftbot.service\nloginctl disable-linger $USER   # optional\n</code></pre>"},{"location":"start/service-mode/#logs","title":"Logs","text":"<p>Logs go to the same <code>logs/</code> directory as interactive runs (<code>logs/&lt;timestamp&gt;.log</code>), rotating at 50 MB, kept 14 days. See Logs.</p>"},{"location":"start/service-mode/#troubleshooting","title":"Troubleshooting","text":"Symptom Fix <code>service.py install</code> runs but nothing happens on login Check OS auto-start UI (Task Scheduler / Login Items / <code>systemctl --user status</code>) Browser shortcut opens blank tab CraftBot isn't running \u2014 <code>service.py start</code> Proactive tasks not firing Verify <code>proactive.enabled: true</code> in settings.json AND <code>service.py status</code> shows running Can't reach UI remotely Default is localhost-only \u2014 use SSH port-forward (see Scenario 2) <p>See Runtime issues for more.</p>"},{"location":"start/service-mode/#related","title":"Related","text":"<ul> <li>Install \u2014 what you do first</li> <li>Interfaces overview \u2014 pick which mode the service runs</li> <li>Proactive \u2014 the main reason to run as a service</li> <li><code>service.py</code> \u2014 the source</li> </ul>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>First, check <code>logs/</code> \u2014 most problems leave a trail there.</p> <ul> <li> <p> Runtime issues</p> <p>Install failures, missing Node.js, CUDA errors, Playwright hiccups.</p> </li> <li> <p> Connection issues</p> <p>OAuth refresh failures, expired tokens, webhook mismatches.</p> </li> <li> <p> Provider issues</p> <p>Rate limits, model errors, context-window overruns.</p> </li> </ul>"},{"location":"troubleshooting/#quick-checks","title":"Quick checks","text":"Symptom Check Agent hangs after \"Thinking\u2026\" Provider issues \u2014 rate limit or API outage <code>/google login</code> opens browser but returns error Connection issues \u2014 OAuth redirect URI mismatch Agent says \"I don't have that capability\" <code>/skill list</code> \u2014 skill may not be enabled Running out of memory or context Provider issues \u2014 check context window Browser mode shows blank page Runtime issues \u2014 Node.js missing or wrong version"},{"location":"troubleshooting/#related","title":"Related","text":"<ul> <li>Logs</li> <li>Getting started</li> </ul>"},{"location":"troubleshooting/connections/","title":"Connection issues","text":"<p>OAuth, token, and integration failures.</p>"},{"location":"troubleshooting/connections/#oauth-redirect-fails","title":"OAuth redirect fails","text":"<p>Symptom: Browser opens, you approve, but CraftBot says \"OAuth timed out.\"</p> <p>Causes:</p> <ul> <li>Firewall / security software blocks the local listener on port 8765. Temporarily disable, retry, re-enable.</li> <li>HTTPS mismatch \u2014 Slack requires <code>https://localhost:8765</code>. The self-signed cert causes a browser warning \u2014 click through (Advanced \u2192 Proceed).</li> <li>Port in use \u2014 another process is on 8765. Stop it or restart CraftBot.</li> </ul>"},{"location":"troubleshooting/connections/#oauth-succeeded-but-action-fails","title":"OAuth succeeded but action fails","text":"<p>Symptom: <code>/google status</code> says connected, but <code>send_gmail</code> errors with \"unauthorized.\"</p> <p>Causes:</p> <ul> <li>Scope mismatch \u2014 you approved a limited scope. Re-run <code>/google logout</code> + <code>/google login</code> and approve everything.</li> <li>Token revoked \u2014 Google revoked the token (user account changed, password reset, suspicious activity). Re-login.</li> <li>Wrong Client ID \u2014 if you set a custom <code>GOOGLE_CLIENT_ID</code> but are using embedded tokens, remove the env var or re-login.</li> </ul>"},{"location":"troubleshooting/connections/#token-refresh-fails","title":"Token refresh fails","text":"<p>Symptom: Integration worked yesterday; today all calls error with \"invalid_grant.\"</p> <p>Fix: The refresh token was revoked. Run <code>/xxx logout</code> then <code>/xxx login</code>.</p> <p>Per-provider refresh lifetimes:</p> Provider Refresh token lifetime Google 6 months of inactivity / 200 refresh tokens per client per user Outlook 90 days Slack Rotating \u2014 refresh on every use Notion Long-lived until revoked LinkedIn 60 days"},{"location":"troubleshooting/connections/#bot-token-invalid-discord-telegram-bot","title":"Bot token invalid (Discord / Telegram bot)","text":"<p>Symptom: <code>/discord login &lt;token&gt;</code> succeeds but messages fail.</p> <p>Fix:</p> <ul> <li>Regenerate the token in the provider's developer portal.</li> <li><code>/discord logout</code> + <code>/discord login &lt;new_token&gt;</code>.</li> <li>For Discord: ensure the bot was invited to the server with correct permissions.</li> <li>For Telegram: <code>/start</code> your bot in Telegram to initialize chat.</li> </ul>"},{"location":"troubleshooting/connections/#whatsapp-qr-code-doesnt-appear","title":"WhatsApp QR code doesn't appear","text":"<p>Symptom: <code>/whatsapp login</code> hangs.</p> <p>Causes:</p> <ul> <li>Playwright not installed \u2014 <code>pip install playwright &amp;&amp; playwright install chromium</code>.</li> <li>Chromium headless fails \u2014 Linux may need <code>playwright install-deps</code> for system libraries.</li> <li>Browser profile corrupted \u2014 delete the WhatsApp session folder in the credential store, retry.</li> </ul>"},{"location":"troubleshooting/connections/#telegram-user-auth-wrong-code","title":"Telegram User auth: \"Wrong code\"","text":"<p>Symptom: Step 2 of <code>login-user</code> rejects the SMS code.</p> <p>Causes:</p> <ul> <li>Code expired \u2014 Telegram codes are short-lived. Request a new one.</li> <li>2FA enabled \u2014 include the password as the third argument: <code>/telegram login-user &lt;phone&gt; &lt;code&gt; &lt;2fa_pwd&gt;</code>.</li> <li>Wrong phone format \u2014 include country code with <code>+</code>: <code>+81...</code> not <code>0...</code>.</li> </ul>"},{"location":"troubleshooting/connections/#mcp-server-wont-connect","title":"MCP server won't connect","text":"<p>Symptom: MCP server in <code>mcp_config.json</code> shows \"disconnected.\"</p> <p>Debug steps:</p> <ol> <li>Check the server runs manually: <code>npx -y @modelcontextprotocol/server-filesystem /path</code></li> <li>Check <code>enabled: true</code> in the config</li> <li>Tail logs for <code>[MCP]</code> errors</li> <li>Verify <code>env: {...}</code> has all required vars set</li> <li>For SSE/WebSocket: test the URL with <code>curl</code> or <code>websocat</code></li> </ol> <p>See MCP servers.</p>"},{"location":"troubleshooting/connections/#webhook-not-receiving-messages-whatsapp-business","title":"Webhook not receiving messages (WhatsApp Business)","text":"<p>Symptom: Outbound works; inbound is silent.</p> <p>Fix: Webhooks require Meta to reach your CraftBot. Options:</p> <ul> <li>Tunnel \u2014 expose your localhost via ngrok / Cloudflare Tunnel, configure the public URL in Meta.</li> <li>Serverless forwarder \u2014 have Meta hit a Lambda/Worker that forwards to CraftBot over your tunnel.</li> <li>Skip webhooks \u2014 use polling via <code>get_whatsapp_business_messages</code> on a schedule.</li> </ul>"},{"location":"troubleshooting/connections/#cred-status-says-embedded-but-login-fails","title":"<code>/cred status</code> says \"embedded\" but login fails","text":"<p>You're on a release build with embedded credentials, but the embedded Client ID expired or was revoked. Options:</p> <ul> <li>Wait for the next release, or</li> <li>Create your own OAuth app and set <code>GOOGLE_CLIENT_ID</code> (etc.) in <code>.env</code>. See Credentials.</li> </ul>"},{"location":"troubleshooting/connections/#related","title":"Related","text":"<ul> <li>Credentials \u2014 precedence rules</li> <li>Connections overview \u2014 per-platform pages</li> <li>Logs \u2014 where auth errors appear</li> </ul>"},{"location":"troubleshooting/providers/","title":"Provider issues","text":"<p>LLM / VLM provider problems \u2014 rate limits, model errors, context window overruns.</p>"},{"location":"troubleshooting/providers/#rate-limited","title":"Rate limited","text":"<p>Symptom: <code>LLMConsecutiveFailureError</code> or <code>429 Too Many Requests</code> in logs.</p> <p>Fixes:</p> <ul> <li> <p>Enable slow mode \u2014 adds rate limiting client-side:</p> <pre><code>{ \"model\": { \"slow_mode\": true, \"slow_mode_tpm_limit\": 30000 } }\n</code></pre> </li> <li> <p>Lower TPM limit if you hit mid-minute limits.</p> </li> <li>Switch providers \u2014 some providers (Anthropic, Gemini) have generous free tiers; BytePlus has strict per-minute limits on trial tiers.</li> </ul>"},{"location":"troubleshooting/providers/#consecutive-failures","title":"Consecutive failures","text":"<p>Symptom: Logs say <code>[LLM] consecutive failures: 5</code>. Event-stream summarization falls back to prune-without-summary.</p> <p>Why: The LLM returned an error (rate limit, outage, invalid key) 5 times in a row. CraftBot backs off.</p> <p>Fix:</p> <ul> <li>Check the provider's status page</li> <li>Verify the API key is valid (<code>/menu</code> \u2192 Models \u2192 test)</li> <li>Wait a few minutes for the circuit breaker to reset, or restart the agent</li> </ul>"},{"location":"troubleshooting/providers/#context-window-overrun","title":"Context window overrun","text":"<p>Symptom: <code>context_length_exceeded</code> error from the LLM.</p> <p>Fixes:</p> <ul> <li>Event stream should auto-summarize at 30k tokens. If it hasn't kicked in, check <code>[EventStream] Triggering summarization</code> in logs.</li> <li>Large task with many todos? Break it into sub-tasks.</li> <li>Using a small-context model (e.g. GPT-3.5) \u2014 switch to a bigger one.</li> </ul>"},{"location":"troubleshooting/providers/#model-not-found","title":"Model not found","text":"<p>Symptom: <code>model_not_found</code> on startup.</p> <p>Fix: <code>model.llm_model</code> in settings.json is misspelled or not available to your account. Leave it <code>null</code> to use the provider's default, or set a known model:</p> Provider Safe default Anthropic <code>claude-sonnet-4-6</code> OpenAI <code>gpt-4o</code> Google <code>gemini-2.0-flash</code> BytePlus (provider-configured) Ollama Whatever is <code>ollama list</code>'d on the server"},{"location":"troubleshooting/providers/#api-key-invalid","title":"API key invalid","text":"<p>Symptom: <code>authentication_error</code> / <code>invalid_api_key</code> immediately on startup.</p> <p>Fix: Re-check the key. Common mistakes:</p> <ul> <li>Extra whitespace \u2014 trim leading/trailing spaces</li> <li>Wrong env var \u2014 <code>OPENAI_API_KEY</code> vs <code>ANTHROPIC_API_KEY</code></li> <li>Wrong provider selected \u2014 key is for Anthropic but <code>llm_provider: \"openai\"</code></li> <li>Scope missing (Google) \u2014 check your GCP project has the right APIs enabled</li> </ul>"},{"location":"troubleshooting/providers/#prompt-caching-not-helping","title":"Prompt caching not helping","text":"<p>Symptom: Each LLM call bills the full prompt; no cost reduction over long tasks.</p> <p>Check:</p> <ul> <li>Provider supports caching? OpenAI has only implicit caching. Switch to Anthropic / Gemini / BytePlus for explicit caching.</li> <li>System prompt churn \u2014 is <code>AGENT.md</code> or <code>USER.md</code> being edited mid-task? That invalidates the prefix cache.</li> <li>Session cache reset \u2014 event-stream summarization invalidates session caches. Happens every ~30k tokens; expected.</li> </ul> <p>See Context engine concept.</p>"},{"location":"troubleshooting/providers/#vlm-calls-fail-in-gui-mode","title":"VLM calls fail in GUI mode","text":"<p>Symptom: <code>content_policy_violation</code> or <code>image_processing_error</code> on every GUI iteration.</p> <p>Fixes:</p> <ul> <li>Wrong VLM model \u2014 set <code>vlm_model</code> to a known vision-capable model (e.g. <code>gpt-4o</code>, <code>gemini-2.0-flash</code>).</li> <li>Image too large \u2014 some providers cap at ~5 MB. Lower your screen resolution or enable OmniParser.</li> <li>Policy block \u2014 some providers block images of certain UIs (e.g. screens showing phone numbers). Check the error message.</li> </ul>"},{"location":"troubleshooting/providers/#ollama-server-unreachable","title":"Ollama server unreachable","text":"<p>Symptom: <code>Connection refused</code> to <code>REMOTE_MODEL_URL</code>.</p> <p>Fixes:</p> <ul> <li>Server not running \u2014 <code>ollama serve</code> on the host machine.</li> <li>Wrong URL \u2014 default Ollama port is 11434. Use the full URL: <code>http://192.168.1.10:11434</code>.</li> <li>Firewall \u2014 Ollama by default binds to <code>127.0.0.1</code> only. Set <code>OLLAMA_HOST=0.0.0.0:11434</code> on the server to allow remote access.</li> <li>Model not pulled \u2014 <code>ollama pull &lt;model&gt;</code> first.</li> </ul>"},{"location":"troubleshooting/providers/#byteplus-regional-issues","title":"BytePlus regional issues","text":"<p>Symptom: BytePlus works intermittently or returns timeout.</p> <p>Check: <code>endpoints.byteplus_base_url</code>. Default is <code>https://ark.ap-southeast.bytepluses.com/api/v3</code> (Singapore). Pick the closest region to your machine.</p>"},{"location":"troubleshooting/providers/#related","title":"Related","text":"<ul> <li>LLM providers</li> <li>VLM providers</li> <li>Context engine \u2014 where caching lives</li> <li>Logs</li> </ul>"},{"location":"troubleshooting/runtime/","title":"Runtime issues","text":"<p>Install failures, launch issues, and hot-path problems.</p>"},{"location":"troubleshooting/runtime/#install-npm-not-found","title":"Install: <code>npm not found</code>","text":"<p>Browser mode needs Node.js 18+. Either:</p> <ol> <li>Install Node.js from nodejs.org (LTS) and restart your terminal, or</li> <li>Use TUI mode instead: <code>python run.py --tui</code></li> </ol>"},{"location":"troubleshooting/runtime/#install-playwright-install-fails","title":"Install: Playwright install fails","text":"<p>Playwright (used for WhatsApp Web) is optional. If it fails:</p> <ul> <li>The rest of the agent still works.</li> <li>Install later: <code>playwright install chromium</code></li> <li>On Linux, you may also need system libraries: <code>playwright install-deps</code></li> </ul>"},{"location":"troubleshooting/runtime/#install-cuda-gpu-issues-with-gui","title":"Install: CUDA / GPU issues with <code>--gui</code>","text":"<p>The installer auto-detects GPU. If CUDA install fails, it falls back to CPU. Force CPU-only:</p> <pre><code>python install.py --gui --cpu-only\n</code></pre>"},{"location":"troubleshooting/runtime/#install-python-version-too-low","title":"Install: Python version too low","text":"<p>You need 3.10+. Upgrade via <code>pyenv</code>, your OS package manager, or use the conda path (<code>python install.py --conda</code> installs a 3.10 env).</p>"},{"location":"troubleshooting/runtime/#launch-agent-hangs-on-startup","title":"Launch: agent hangs on startup","text":"<p>Check the log for the last line \u2014 <code>tail -f logs/*.log</code>. Common culprits:</p> <ul> <li>Invalid API key \u2014 the connection test fails. Re-run onboarding.</li> <li>Ollama URL unreachable \u2014 if <code>llm_provider: \"remote\"</code>, verify <code>REMOTE_MODEL_URL</code>.</li> <li>ChromaDB lock \u2014 if the previous run crashed, ChromaDB can hold a lock. <code>rm -rf chroma_db_memory/</code> as last resort (this wipes memory, not your agent file system).</li> </ul>"},{"location":"troubleshooting/runtime/#launch-menu-or-commands-dont-work","title":"Launch: <code>/menu</code> or commands don't work","text":"<p>The UI layer failed to register commands. Look for <code>[CommandRegistry]</code> errors in logs. Most common:</p> <ul> <li>A custom command module has a syntax error \u2014 fix it or remove it.</li> <li>A required settings field is missing \u2014 re-run onboarding.</li> </ul>"},{"location":"troubleshooting/runtime/#runtime-agent-loop-stops-after-one-action","title":"Runtime: agent loop stops after one action","text":"<p>If the agent stops after one action in a complex task, it likely hit <code>MAX_ACTIONS_PER_TASK</code> (default 500). Check <code>[TASK]</code> logs for \"action budget exceeded\" and either:</p> <ul> <li>Break the work into smaller tasks, or</li> <li>Edit <code>app/config.py</code> to raise the limit.</li> </ul>"},{"location":"troubleshooting/runtime/#runtime-memory-search-returns-empty","title":"Runtime: memory search returns empty","text":"<ul> <li>Check <code>memory.enabled</code> in settings.json is <code>true</code>.</li> <li>Verify <code>chroma_db_memory/</code> exists and isn't zero-sized.</li> <li>Trigger a re-index: restart the agent \u2014 the <code>MemoryFileWatcher</code> re-scans on startup.</li> </ul>"},{"location":"troubleshooting/runtime/#runtime-event-stream-grows-forever-tokens-balloon","title":"Runtime: event stream grows forever, tokens balloon","text":"<p>Summarization should kick in at 30k tokens. If it's not:</p> <ul> <li>Check logs for <code>[EventStream] Triggering summarization</code> \u2014 if absent, the threshold is likely too high.</li> <li>LLM might be in a consecutive-failure state \u2014 streams fall back to prune-without-summary. Check provider connectivity.</li> </ul> <p>See Event stream concept.</p>"},{"location":"troubleshooting/runtime/#runtime-gui-mode-cant-take-screenshots","title":"Runtime: GUI mode can't take screenshots","text":"<ul> <li>Ensure a graphical display is available (X11 on Linux).</li> <li>On Linux headless: use <code>Xvfb :99 -screen 0 1920x1080x24 &amp; export DISPLAY=:99</code>.</li> <li>Verify <code>pyautogui</code> is installed: <code>pip show pyautogui</code>.</li> <li>OmniParser unreachable: check <code>omniparser_url</code> in settings.json \u2192 <code>gui.*</code>.</li> </ul>"},{"location":"troubleshooting/runtime/#runtime-browser-mode-shows-blank-page","title":"Runtime: browser mode shows blank page","text":"<ul> <li><code>npm</code> build probably failed. Re-run <code>python install.py</code>.</li> <li>Port conflict: check logs for the port \u2014 another service may be bound.</li> <li>DevTools network tab: if WebSocket connection fails, the Python backend isn't up.</li> </ul>"},{"location":"troubleshooting/runtime/#service-mode-doesnt-auto-start","title":"Service mode: doesn't auto-start","text":"<p>See Service mode platform deep-dive for OS-specific checks.</p>"},{"location":"troubleshooting/runtime/#getting-help","title":"Getting help","text":"<ul> <li>Logs: <code>logs/&lt;timestamp&gt;.log</code></li> <li>Discord</li> <li>GitHub issues</li> </ul>"},{"location":"troubleshooting/runtime/#related","title":"Related","text":"<ul> <li>Logs</li> <li>Connection issues</li> <li>Provider issues</li> </ul>"}]}